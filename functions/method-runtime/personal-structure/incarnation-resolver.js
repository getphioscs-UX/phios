/** MIR-3 Incarnation Configuration resolver: structural gates only, no legacy public name. */
export function resolveIncarnationConfiguration(activations){const get=(layer,body)=>activations.find(a=>a.layer===layer&&a.bodyCode===body&&a.status!=='UNKNOWN');const ps=get('PERSONALITY','SUN'),pe=get('PERSONALITY','EARTH'),ds=get('DESIGN','SUN'),de=get('DESIGN','EARTH');if(!ps||!pe||!ds||!de)return Object.freeze({status:'UNKNOWN',personalitySun:null,personalityEarth:null,designSun:null,designEarth:null,configurationCode:null});return Object.freeze({status:'CALCULATED',personalitySun:ps.gate,personalityEarth:pe.gate,designSun:ds.gate,designEarth:de.gate,configurationCode:`${ps.gate}/${pe.gate}|${ds.gate}/${de.gate}`,legacyTitleIncluded:false});}
