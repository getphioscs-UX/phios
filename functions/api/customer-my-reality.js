import {buildCurrentRealityBundle} from '../reality-orchestration/reality-orchestrator.js';
import {projectRealityForCustomer} from '../customer-projection/reality-customer-projection.js';
const H={'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff','referrer-policy':'no-referrer'};const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:H});
const clean=v=>String(v??'').trim();
export async function onRequestGet(context){const locale=new URL(context.request.url).searchParams.get('locale')==='zh-Hans'?'zh-Hans':'en';const sources=context?.data?.cxRealitySources||{};return json({ok:true,view:projectRealityForCustomer({...sources,locale}),governance:{persisted:false,rawRuntimeExposed:false}})}
export async function onRequestPost(context){let body;try{body=await context.request.json()}catch{return json({ok:false,error:'INVALID_JSON'},400)};if(body?.consent!==true)return json({ok:false,error:'REALITY_PROCESSING_CONSENT_REQUIRED'},403);const locale=body?.locale==='zh-Hans'?'zh-Hans':'en';const question=clean(body?.question)||clean(body?.whatMattersMostNow)||'Current reality';const reportedContext=[body?.whatIsHappening,body?.howLong,body?.whatChanged,body?.whatMattersMostNow].map(clean).filter(Boolean);try{const bundle=await buildCurrentRealityBundle({sourceType:'ASK',locale,source:{question,reportedContext,unknown:[]}});const view=projectRealityForCustomer({bundle,locale});return json({ok:true,view,governance:{persisted:false,canonicalRealityCreated:false,rawRuntimeExposed:false}})}catch(error){return json({ok:false,error:error?.code||error?.message||'CUSTOMER_REALITY_PROJECTION_FAILED'},error?.status||422)}}
