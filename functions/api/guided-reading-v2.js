import {normalizeMir8GuidedReadingRequest,runMir8Eligibility,composeAcceptedGuidedReading} from '../_lib/guided-reading-v2.js';
const HEADERS=Object.freeze({'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'});
const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:HEADERS});
export async function onRequestPost(context){let body;try{body=await context.request.json();}catch{return json({ok:false,error:{code:'INVALID_JSON'}},400);}try{const input=normalizeMir8GuidedReadingRequest(body);const result=input.action==='ELIGIBILITY'?runMir8Eligibility(input):composeAcceptedGuidedReading({result:input.acceptedInterpretationResult,readingConsent:input.readingConsent,context:input.context,locale:input.locale});return json({ok:true,...result});}catch(error){const code=String(error?.code||error?.message||'MIR8_GUIDED_READING_FAILED');const status=/INVALID|FORBIDDEN|REQUIRED|NOT_ELIGIBLE|CONSENT|HANDOFF|AUTHORITY/.test(code)?400:500;return json({ok:false,error:{code},governance:{methodExecutionTriggered:false,methodRecalculation:false,newInterpretationDerived:false,unknownFilled:false,realityJourneyActivated:false}},status);}}
export async function onRequestGet(){return json({ok:false,error:{code:'MIR8_GUIDED_READING_POST_ONLY'}},405);}
