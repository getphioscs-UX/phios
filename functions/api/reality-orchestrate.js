import {buildCurrentRealityBundle,prepareRealityContinuation} from '../reality-orchestration/reality-orchestrator.js';
const H={'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff','referrer-policy':'no-referrer'};const json=(b,s=200)=>new Response(JSON.stringify(b),{status:s,headers:H});
export async function onRequestPost({request}={}){let body;try{body=await request.json()}catch{return json({ok:false,error:{code:'INVALID_JSON'}},400)}try{const bundle=await buildCurrentRealityBundle(body);const continuation=prepareRealityContinuation(bundle,{target:body.target||'REALITY_ENTRY',consent:body.consent||{}});return json({ok:true,bundle,continuation,governance:{persisted:false,canonicalRealityCreated:false,realityJourneyActivated:false}})}catch(e){return json({ok:false,error:{code:e.code||e.message||'STAGE15_ORCHESTRATION_FAILED'}},e.status||400)}}
export async function onRequestGet(){return json({ok:false,error:{code:'STAGE15_POST_ONLY'}},405)}
