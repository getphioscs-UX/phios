import {resolveBirthPlace} from '../location/place-resolver.js';
import {createConfirmedBirthLocationSnapshot} from '../location/confirmed-birth-location-snapshot.js';
const H={'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'};const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:H});
export async function onRequestPost(context){let body;try{body=await context.request.json()}catch{return json({ok:false,error:'INVALID_JSON'},400)};try{const birthDate=body?.birthDate||null,birthTime=body?.birthTime||null;const location=await resolveBirthPlace(body?.providerRef,{birthDate,birthTime,locale:body?.locale==='zh-Hans'?'zh-Hans':'en',env:context.env});const birthLocationSnapshot=createConfirmedBirthLocationSnapshot(location,{birthDate,birthTime});return json({ok:true,location,birthLocationSnapshot})}catch(error){const code=error?.code||'LOCATION_RESOLUTION_FAILED';return json({ok:false,error:code},code==='LOCATION_SELECTION_REQUIRED'?400:422)}}
export async function onRequestGet(){return json({ok:false,error:'METHOD_NOT_ALLOWED'},405)}
