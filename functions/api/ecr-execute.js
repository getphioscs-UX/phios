import {executeEcrCanonicalProjection} from '../embodied-configuration/ecr-canonical-projection-runtime.js';
const H={'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff'};const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:H});
export async function onRequestPost({request}){let body;try{body=await request.json()}catch{return json({ok:false,error:'INVALID_JSON'},400)}if(body?.consent!==true)return json({ok:false,error:'ECR_PROCESSING_CONSENT_REQUIRED'},403);try{const result=await executeEcrCanonicalProjection({canonicalInput:body.canonicalInput,requestId:body.requestId||`ECR-${crypto.randomUUID()}`});return json({ok:true,result});}catch(error){return json({ok:false,error:error?.code||'ECR_EXECUTION_FAILED'},422)}}
