import {normalizeAskContext} from '../../assets/customer-ui/js/ask-context-contract.js';
import {resolveSelectedArticle} from '../contextual-ask/contextual-ask-runtime.js';
import {knowledgeNavigationIntent} from '../../assets/customer-ui/js/navigation-intent.js';
import {onRequestPost as runAskOrchestrated} from './ask-phios-orchestrated.js';
import {retrieveCurrentFacts} from '../current-facts-gateway/current-facts-gateway.js';
import {projectKnowledgeAnswerForCustomer} from '../customer-projection/knowledge-customer-projection.js';
import {projectContextualAskForCustomer} from '../customer-projection/contextual-ask-customer-projection.js';
import {buildAskContextAvailability,resolveExplicitAskContexts,isPublicKnowledgeContextRef} from '../contextual-ask/contextual-ask-runtime.js';
import {normalizeAtlasRetrievalScope} from '../_lib/atlas-retrieval-scope.js';
import {resolveFormationEntry} from '../_lib/formation-retrieval-scope.js';
const H={'content-type':'application/json; charset=utf-8','cache-control':'no-store','x-content-type-options':'nosniff','referrer-policy':'no-referrer'};
const json=(body,status=200)=>new Response(JSON.stringify(body),{status,headers:H});
const clean=v=>String(v??'').trim();
function requestedContexts(body){const list=Array.isArray(body?.contexts)?body.contexts:[];if(list.length)return list;if(body?.questionOnly===true)return [];return [{contextType:'KNOWLEDGE'}]}
const safePublicText=(value,max=160)=>clean(value).slice(0,max);
async function publicationNavigation(base,entry,selected,env,locale){
 if(entry?.bookCode!=='BOOK-5')return base;
 const hrefs=[...new Set([...(base.basedOn?.sources||[]),...(base.relatedKnowledge||[])].map(r=>r.href?.split(/[?#]/)[0]).filter(h=>/^\/articles\/book5-[a-z0-9-]+$/.test(h||'')))].slice(0,3);
 const articles=selected?[selected]:(await Promise.all(hrefs.map(h=>resolveSelectedArticle(env,h.slice('/articles/'.length),locale)))).filter(Boolean);
 const navigationKey=href=>{const u=new URL(href,'https://phios.local');u.searchParams.delete('locale');u.searchParams.sort();return u.pathname+u.search+u.hash;};
 const publicHref=href=>{if(href.startsWith('/articles/'))return href.split('#')[0];const u=new URL(href,'https://phios.local');if(u.pathname==='/books/reality-differentiation/'&&u.searchParams.has('atlas')){u.searchParams.set('locale',locale);return u.pathname+u.search+u.hash;}return href;};
 const titles=new Map(articles.map(a=>[navigationKey(a.href),a.title]));
 for(const article of articles)for(const link of article.atlasLinks)titles.set(navigationKey(link.href),link.label);
 const related=[...articles.map(a=>({title:a.title,href:a.href})),...articles.flatMap(a=>a.atlasLinks.map(l=>({title:l.label,href:l.href}))),...(base.relatedKnowledge||[])];
 const byHref=new Map();for(const r of related.filter(r=>r.href)){const href=publicHref(r.href),key=navigationKey(href);if(!byHref.has(key))byHref.set(key,{...r,href});}const distinct=[...byHref.values()];
 const sources=[...new Map((base.basedOn?.sources||[]).map(s=>{const href=s.href?publicHref(s.href):s.href,key=href?navigationKey(href):s.label;return [key,{...s,label:titles.get(key)||(locale==='zh-Hans'?'第五册参考资料':'Book V reference'),href}];})).values()];
 return {...base,relatedKnowledge:distinct.slice(0,10),basedOn:{...base.basedOn,sources}};
}
function safeInternalRoute(value){const route=clean(value);return /^\/[a-zA-Z0-9/_?&=.%+-]*$/.test(route)&&!route.startsWith('//')?route.slice(0,240):null}
function explicitKnowledgeEntryContext(contexts,body){const selected=contexts.find(item=>item.contextType==='KNOWLEDGE'&&item.contextRef!=='PHIOS_GOVERNED_KNOWLEDGE');if(!selected||!isPublicKnowledgeContextRef(selected.contextRef))return null;const supplied=body?.knowledgeContext||{};const suppliedRef=clean(supplied.contextRef);const ref=suppliedRef===selected.contextRef?suppliedRef:selected.contextRef;const [kind,id]=ref.split(':');const entrySurface=kind==='ARTICLE'?'ARTICLE':kind==='BOOK'?'BOOK':kind==='FIGURE'?'FIGURE':'KNOWLEDGE_SEARCH';const entry={entrySurface,entryRoute:safeInternalRoute(supplied.contextRoute)||'/knowledge/',contextType:'KNOWLEDGE',contextId:ref,contextLabel:safePublicText(supplied.contextLabel)||selected.label,contextSummary:safePublicText(supplied.contextSummary,320)||null,readingPath:safePublicText(supplied.readingPath,240)||null,relatedKnowledgeRef:isPublicKnowledgeContextRef(supplied.relatedKnowledgeRef)?supplied.relatedKnowledgeRef:ref,retrievalScope:normalizeAtlasRetrievalScope(supplied.retrievalScope),mode:'CONTEXTUAL'};if(kind==='ARTICLE')entry.articleCode=id;if(kind==='BOOK')entry.bookCode=id;if(kind==='FIGURE')entry.figureCode=id;return entry}
function questionOnlyView(question,locale,currentFacts){const zh=locale==='zh-Hans',facts=(currentFacts?.evidence||[]).map(x=>({claim:x.claimText,source:x.publisher||x.title,sourceUrl:x.sourceUrl,retrievedAt:x.retrievedAt,freshness:x.freshnessState,authorityClass:x.authorityClass}));return {schemaVersion:'PHI-OS-CX-R9-R2-QUESTION-ONLY-v1.0.0',surface:'ASK',locale,state:facts.length?'CURRENT_FACTS_ONLY':'NO_GOVERNED_CONTEXT_SELECTED',question,answer:{text:facts.length?(zh?'我找到了与你的问题有关的当前公共信息；没有加入任何个人资料。':'I found current public information relevant to your question; no personal information was added.'):(zh?'这次没有选择可用于回答的来源，因此 PHI OS 不会用无关内容把缺口填满。':'No source was selected for this answer, so PHI OS will not fill the gap with unrelated content.'),supporting:[]},basedOn:{sources:facts.map(f=>({label:f.source,excerpt:f.claim,href:f.sourceUrl,retrievedAt:f.retrievedAt,freshness:f.freshness})),statement:zh?'没有自动加入账户或个人资料。':'No account or personal information was added automatically.'},limits:{items:[zh?'如果希望回答更贴近你的情况，可以选择一个来源，或加入当前处境。':'To make the answer more contextual, choose a source or add your current situation.']},relatedKnowledge:[],possibleNextStep:{kind:'SELECT_CONTEXT',label:zh?'选择来源后重新提问':'Choose a source and ask again'},currentFacts:{state:currentFacts?.state||'NOT_USED',retrievedAt:currentFacts?.retrievedAt||null,freshness:currentFacts?.freshness||null,limitations:currentFacts?.limitations||[],evidence:facts},handoff:{available:false},governance:{currentFactsAreCanonicalKnowledge:false,recommends:false}}}
function serverResolvedSeedAvailability(context,locale,seed){
 const type=clean(seed?.contextType).toUpperCase(),ref=clean(seed?.contextRef);if(!type||!ref||type==='KNOWLEDGE'||type==='CURRENT_REALITY')return null;
 const resolved=Array.isArray(context.data?.resolvedAskContexts)?context.data.resolvedAskContexts:[];
 try{const accepted=resolveExplicitAskContexts({requested:[{contextType:type,contextRef:ref}],resolvedContexts:resolved,locale});const item=accepted[0];if(!item)return null;return {contextType:item.contextType,label:item.label,sourceClass:item.sourceClass,participant:item.participant,caseScope:item.caseScope,availability:'AVAILABLE_FROM_SOURCE',reason:'SERVER_RESOLVED_SOURCE_CONTEXT',requestedContextRef:item.contextRef,generatedAt:item.generatedAt||null};}catch{return null}
}
export async function onRequestGet(context={}){const url=new URL(context.request?.url||'https://phios.test/api/customer-contextual-ask');const locale=url.searchParams.get('locale')==='zh-Hans'?'zh-Hans':'en';const seed=normalizeAskContext(url.searchParams);const availability=[...buildAskContextAvailability({locale,requestedContextSeed:seed})];const authorized=serverResolvedSeedAvailability(context,locale,seed);if(authorized){const index=availability.findIndex(x=>x.contextType===authorized.contextType);if(index>=0)availability[index]=authorized;else availability.push(authorized)}return json({ok:true,canonicalRoute:'/knowledge/ask/',availability})}
export async function onRequestPost(context){let body;try{body=await context.request.json()}catch{return json({ok:false,error:'INVALID_JSON'},400)};const question=clean(body?.question||body?.q);if(!question)return json({ok:false,error:'ASK_QUESTION_REQUIRED'},400);const locale=body?.locale==='zh-Hans'?'zh-Hans':'en';const navigation=knowledgeNavigationIntent(question,locale);if(navigation){const base={surface:'ASK',locale,state:'NAVIGATION',question,answer:{text:navigation.text,supporting:[]},basedOn:{sources:[],statement:''},limits:{items:[]},relatedKnowledge:[{title:navigation.title,href:navigation.href}],possibleNextStep:{kind:'RELATED_KNOWLEDGE',label:navigation.title},handoff:{available:false},currentFacts:{state:'NOT_USED',evidence:[]}};return json({ok:true,view:projectContextualAskForCustomer(base,{contexts:[],locale}),governance:{navigationOnly:true,rawAskRuntimeExposed:false}});}const currentFacts=await retrieveCurrentFacts({question,env:context.env||{},fetcher:fetch});
 let contexts;try{contexts=resolveExplicitAskContexts({requested:requestedContexts(body),guidedContext:body?.guidedContext||{},contextConsent:body?.contextConsent||{},resolvedContexts:Array.isArray(context.data?.resolvedAskContexts)?context.data.resolvedAskContexts:[],locale})}catch(error){return json({ok:false,error:error.code||error.message||'ASK_CONTEXT_REJECTED',contextNotUsed:true},error.status||422)}
 if(contexts.length===0){const base=questionOnlyView(question,locale,currentFacts);return json({ok:true,view:projectContextualAskForCustomer(base,{contexts,currentFacts,locale}),governance:{rawAskRuntimeExposed:false,silentAccountContextSweep:false}})}
 const currentRealitySelected=contexts.some(x=>x.contextType==='CURRENT_REALITY');const currentExternalEvidence=(currentFacts.evidence||[]).map(e=>({sourceId:e.sourceId,claimText:e.claimText,title:e.title,sourceUrl:e.sourceUrl,authorityClass:e.authorityClass,publisher:e.publisher,retrievedAt:e.retrievedAt,freshnessState:e.freshnessState}));const entryContext=explicitKnowledgeEntryContext(contexts,body);let selectedArticle=null;
 if(entryContext?.articleCode){const selected=await resolveSelectedArticle(context.env||{},entryContext.articleCode,locale);if(!selected)return json({ok:false,error:'SELECTED_SOURCE_UNAVAILABLE',contextNotUsed:true},422);selectedArticle=selected;entryContext.articleCode=selected.slug;entryContext.bookCode=selected.bookCode;entryContext.articleContext=selected.articleContext;entryContext.contextLabel=selected.title;entryContext.entryRoute=selected.href;entryContext.contextSummary=null;contexts=contexts.map(c=>c.contextRef===entryContext.contextId?{...c,label:selected.title}:c);}
 if(entryContext)Object.assign(entryContext,await resolveFormationEntry(entryContext.contextId,context.env||{}));
 const forwarded={q:question,locale,taxonomyHint:body?.taxonomyHint||null,guidedContext:currentRealitySelected?(body?.guidedContext||{}):{},publicRequest:true,currentExternalEvidence,externalCurrentRequired:currentFacts.state==='AVAILABLE'?true:null,contextualAsk:true,...(entryContext?{entryContext}:{})};
 const request=new Request(new URL('/api/ask-phios-orchestrated',context.request.url),{method:'POST',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify(forwarded)});const response=await runAskOrchestrated({...context,request});const payload=await response.json().catch(()=>({}));if(!response.ok||payload?.ok!==true)return json({ok:false,error:payload?.error?.code||payload?.error||'ASK_RUNTIME_FAILED'},response.status||422);
 let base=projectKnowledgeAnswerForCustomer(payload,{locale,currentFacts});if(selectedArticle){base={...base,answer:{...base.answer,text:selectedArticle.bookCode==='BOOK-5'?base.answer.text:(base.answer.text.match(/[^。！？.!?]+[。！？.!?]?/gu)||[base.answer.text]).slice(0,2).join(' ').trim(),supporting:[]},basedOn:{...base.basedOn,sources:base.basedOn.sources.map(s=>s.href===selectedArticle.href?{...s,label:selectedArticle.title}:s)},relatedKnowledge:[{title:selectedArticle.title,href:selectedArticle.href},...(base.relatedKnowledge||[]).filter(s=>s.href!==selectedArticle.href)]};}base=await publicationNavigation(base,entryContext,selectedArticle,context.env||{},locale);const view=projectContextualAskForCustomer(base,{contexts,currentFacts,answerPayload:payload,locale});return json({ok:true,view,governance:{rawAskRuntimeExposed:false,currentFactsCanonicalKnowledge:false,browserThirdPartyRetrieval:false,silentAccountContextSweep:false,clientCannotSelfAuthorizePaidOrRelationshipContext:true}})}
