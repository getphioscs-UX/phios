const root=document.querySelector('[data-px2-methods]');
const statusClass=s=>s.includes('READY')||s.includes('FROZEN')?'ready':s.includes('GATED')||s.includes('RESTRICTED')||s.includes('PENDING')?'gated':'holding';
const escapeHtml=value=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
fetch('/content/web-production/px2/registries/public-method-catalog-v2.json').then(r=>r.json()).then(d=>{root.innerHTML=d.methods.map(m=>{const symbolic=m.route.startsWith('/readings/symbolic/');const action=symbolic?`<div class="puxr-actions"><a class="puxr-btn" href="${escapeHtml(m.route)}">Explore method context</a></div>`:m.route.startsWith('/professional')?`<div class="puxr-actions"><a class="puxr-btn" href="${escapeHtml(m.route)}">Open professional surface</a></div>`:'';return `<article id="${escapeHtml(m.route.split('#')[1]||m.methodCode.toLowerCase())}" class="puxr-card px2-method"><div class="puxr-card__kicker">${escapeHtml(m.category.replaceAll('_',' '))}</div><h3>${escapeHtml(m.label)}<br><small>${escapeHtml(m.labelZh)}</small></h3><p>${m.runAllowed?'Available for governed execution.':symbolic?'Explore the method context, evidence and source boundaries. Public execution remains gated by production acceptance.':'Visible as a governed capability; execution remains gated by current production authority.'}</p><span class="px2-status px2-status--${statusClass(m.publicState)}">${escapeHtml(m.publicState.replaceAll('_',' '))}</span>${action}</article>`}).join('')});