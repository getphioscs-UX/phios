const form=document.querySelector('[data-px2-search-form]');const input=form?.querySelector('input[name="q"]');const target=document.querySelector('[data-px2-search-results]');
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
async function corpus(){const [pubs,books]=await Promise.all([fetch('/content/knowledge/public/retrieval/publications.json').then(r=>r.json()),fetch('/content/registry/books.json').then(r=>r.json())]);const locale=(localStorage.getItem('phiOSLocale')||'zh-Hans');const records=(pubs.records||[]).filter(x=>x.locale===locale&&x.status==='published').slice(0,200);const articles=await Promise.all(records.map(async r=>{try{const p=await fetch(`/content/knowledge/public/authority/articles/${locale}/${r.nodeCode}.json`).then(x=>x.ok?x.json():null);const a=p?.article;return a?{type:'Article',title:a.title,summary:a.summary,href:a.href||'/articles'}:null}catch{return null}}));const bookRows=(books.books||[]).map(b=>({type:'Book',title:b.title?.[locale]||b.title?.en,summary:b.subtitle?.[locale]||b.subtitle?.en,href:`/books/`}));return [...articles.filter(Boolean),...bookRows];}
let all=[];function render(q=''){const n=q.trim().toLowerCase();const rows=all.filter(x=>!n||`${x.title} ${x.summary} ${x.type}`.toLowerCase().includes(n)).slice(0,24);target.innerHTML=rows.length?rows.map(x=>`<a class="px2-result" href="${esc(x.href)}"><small>${esc(x.type)}</small><div><h3>${esc(x.title)}</h3><p>${esc(x.summary)}</p></div></a>`).join(''):`<div class="puxr-empty">No matching governed public knowledge.</div>`;}
form?.addEventListener('submit',e=>{e.preventDefault();render(input.value);history.replaceState(null,'',`?q=${encodeURIComponent(input.value)}`)});corpus().then(x=>{all=x;const q=new URLSearchParams(location.search).get('q')||'';if(input)input.value=q;render(q)});
