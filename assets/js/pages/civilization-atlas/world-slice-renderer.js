const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const loc=(v,lang)=>v?.[lang]||v?.en||v?.['zh-Hans']||'';
const formatYear=(y,lang)=>y<0?(lang==='zh-Hans'?`公元前${Math.abs(y)}年`:`${Math.abs(y)} BCE`):(lang==='zh-Hans'?`公元${y}年`:`${y} CE`);
export function renderWorldSlice(container,{registry,casesRegistry,state,locale='en',onSnapshotSelect=()=>{},onCaseSelect=()=>{}}={}){
  const lang=locale==='zh-Hans'?'zh-Hans':'en'; const snapshots=registry?.snapshots||[]; const caseMap=new Map((casesRegistry?.cases||[]).map(c=>[c.caseId,c]));
  const selected=snapshots.find(s=>s.snapshotId===state.snapshotId) || snapshots.reduce((best,s)=>state.time===null?best:(!best||Math.abs(s.year-state.time)<Math.abs(best.year-state.time)?s:best),null) || snapshots[0] || null;
  container.innerHTML=`<div class="civ-world" data-atlas-world>
    <div class="civ-world__selector" role="list" aria-label="${esc(lang==='zh-Hans'?'世界横切面时间':'World snapshot time')}">${snapshots.map(s=>`<button type="button" role="listitem" class="civ-world__time${selected?.snapshotId===s.snapshotId?' is-active':''}" data-snapshot-id="${esc(s.snapshotId)}" aria-pressed="${selected?.snapshotId===s.snapshotId?'true':'false'}"><strong>${esc(formatYear(s.year,lang))}</strong><small>${esc(s.snapshotId)}</small></button>`).join('')}</div>
    ${selected?`<article class="civ-world__slice"><p class="knowledge-eyebrow">${esc(selected.snapshotId)} · ${esc(formatYear(selected.year,lang))}</p><h4>${esc(loc(selected.title,lang))}</h4><p>${esc(loc(selected.summary,lang))}</p><div class="civ-world__map" aria-label="${esc(lang==='zh-Hans'?'结构化世界横切面':'Structured world slice')}"><div class="civ-world__globe" aria-hidden="true">◎</div><div class="civ-world__regions">${selected.regionalGroups.map(g=>`<section><h5>${esc(loc(g.label,lang))}</h5><p>${g.caseIds.map(id=>{const c=caseMap.get(id);return c?`<button class="civ-atlas-link" data-world-case="${esc(id)}">${esc(loc(c.title,lang))}</button>`:esc(id)}).join(' · ')||'—'}</p></section>`).join('')}</div></div><div class="civ-world__networks"><section><h5>${lang==='zh-Hans'?'主要贸易网络':'Trade networks'}</h5><ul>${selected.tradeNetworks.map(n=>`<li>${esc(loc(n.label,lang))}</li>`).join('')}</ul></section><section><h5>${lang==='zh-Hans'?'代表城市':'Representative cities'}</h5><ul>${selected.majorCities.map(c=>`<li>${esc(loc(c.name,lang))}</li>`).join('')}</ul></section><section><h5>${lang==='zh-Hans'?'技术与能源':'Technology & energy'}</h5><p>${esc(loc(selected.technologyContext?.label,lang))}</p><p>${esc(loc(selected.energyContext?.label,lang))}</p></section></div><p class="civ-atlas-note"><strong>${lang==='zh-Hans'?'知识状态：':'Knowledge state: '}</strong>${esc(selected.unknown.state)} · ${esc(loc(selected.unknown.note,lang))}</p>${selected.posterAssetRef?`<p class="civ-atlas-note">${lang==='zh-Hans'?'图谱海报引用':'Poster asset'}: <code>${esc(selected.posterAssetRef)}</code></p>`:''}</article>`:''}
  </div>`;
  container.querySelectorAll('[data-snapshot-id]').forEach(el=>el.addEventListener('click',()=>onSnapshotSelect(snapshots.find(s=>s.snapshotId===el.dataset.snapshotId))));
  container.querySelectorAll('[data-world-case]').forEach(el=>el.addEventListener('click',()=>onCaseSelect(el.dataset.worldCase)));
  return selected;
}
export function renderWorldInspector(container,{snapshot,locale='en'}={}){
  if(!snapshot) return false; const lang=locale==='zh-Hans'?'zh-Hans':'en';
  container.innerHTML=`<p class="knowledge-eyebrow">${esc(snapshot.snapshotId)}</p><h3>${esc(loc(snapshot.title,lang))}</h3><dl><div><dt>${lang==='zh-Hans'?'时间':'Time'}</dt><dd>${esc(formatYear(snapshot.year,lang))}</dd></div><div><dt>${lang==='zh-Hans'?'主要案例':'Major cases'}</dt><dd>${esc(snapshot.majorCaseIds.join(', ')||'—')}</dd></div><div><dt>${lang==='zh-Hans'?'知识状态':'Knowledge state'}</dt><dd>${esc(snapshot.unknown.state)}</dd></div><div><dt>${lang==='zh-Hans'?'开放边界':'Boundary'}</dt><dd>${esc(loc(snapshot.unknown.note,lang))}</dd></div></dl>`;
  return true;
}
