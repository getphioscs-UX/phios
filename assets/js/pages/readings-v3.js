const root=document.querySelector('[data-px2-methods]');
const escapeHtml=value=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
const statusClass=s=>/AVAILABLE|READY|FROZEN|LIMITED_PRODUCTION/i.test(s)?'ready':/GATED|RESTRICTED|PENDING|LIMITED/i.test(s)?'gated':'holding';
async function json(path){const r=await fetch(path,{cache:'no-store'});if(!r.ok)throw new Error(path);return r.json();}
async function render(){if(!root)return;const d=await json('/content/web-production/px2/successors/public-method-catalog-v3.json');let tarotStatus=null;try{tarotStatus=await json('/api/tarot-production-status');}catch{}
root.innerHTML=d.methods.map(m=>{const symbolic=m.route.startsWith('/readings/symbolic/');const live=m.methodCode==='TAROT'&&tarotStatus?.production?.runAllowed===true;const publicState=live?'LIMITED_PRODUCTION':m.publicState;const copy=live?'Available for governed Limited Production execution.':symbolic?'Explore the method context, evidence and source boundaries. Execution is controlled by live server authority.':'Visible as a governed capability; execution remains gated by current production authority.';const action=symbolic?`<div class="puxr-actions"><a class="puxr-btn" href="${escapeHtml(m.route)}">Explore method context</a></div>`:m.route.startsWith('/professional')?`<div class="puxr-actions"><a class="puxr-btn" href="${escapeHtml(m.route)}">Open professional surface</a></div>`:'';return `<article id="${escapeHtml(m.route.split('#')[1]||m.methodCode.toLowerCase())}" class="puxr-card px2-method"><div class="puxr-card__kicker">${escapeHtml(m.category.replaceAll('_',' '))}</div><h3>${escapeHtml(m.label)}<br><small>${escapeHtml(m.labelZh)}</small></h3><p>${escapeHtml(copy)}</p><span class="px2-status px2-status--${statusClass(publicState)}">${escapeHtml(publicState.replaceAll('_',' '))}</span>${action}</article>`}).join('');}
render().catch(()=>{if(root)root.innerHTML='<p>Method status is temporarily unavailable. This does not grant execution authority.</p>';});
