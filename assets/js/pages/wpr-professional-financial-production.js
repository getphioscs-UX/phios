const refs=Object.freeze({pr:'/content/runtime/professional-runtime/freeze/pr-v2-freeze-v1.json',financial:'/content/registry/financial-authority-boundary.json'});
const labels={en:{pr:'Professional Runtime',fin:'Financial authority',human:'Human professional attribution required',prod:'Product-neutral boundary',boundary:'WPR projects status only. It does not create professional judgment, financial calculations, recommendations or regulated advice.'},'zh-Hans':{pr:'专业运行',fin:'财务权限',human:'必须由人类专业人士承担判断归属',prod:'产品中立边界',boundary:'WPR 只投射治理状态，不产生专业判断、财务计算、建议或受监管意见。'}};
const locale=()=>document.documentElement.lang==='zh-Hans'?'zh-Hans':'en'; const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function get(url){const r=await fetch(url,{headers:{Accept:'application/json'}});if(!r.ok)throw new Error('authority_unavailable');return r.json();}
async function render(){const root=document.querySelector('[data-wpr-professional-financial-authority]');if(!root)return;try{const[pr,fin]=await Promise.all([get(refs.pr),get(refs.fin)]);const t=labels[locale()];root.innerHTML=`<article class="wpr-authority-card"><strong>${esc(t.pr)}</strong><p>${esc(pr.status)}</p><p class="wpr-authority-state">${esc(pr.productionState)}</p></article><article class="wpr-authority-card"><strong>${esc(t.fin)}</strong><p>${esc(fin.status)}</p><p class="wpr-authority-state">${esc(t.prod)}</p></article><article class="wpr-authority-card wpr-authority-boundary"><strong>${esc(t.human)}</strong><p>${esc(t.boundary)}</p></article>`}catch{root.innerHTML='<p class="wpr-authority-error">Governed authority status unavailable. No production authority is inferred.</p>'}}
render();window.addEventListener('phios:locale-change',render);
