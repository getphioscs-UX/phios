import {esc} from '../../surfaces/runtime-ui.js';
import {PHI_MANDALA_VIEWBOX_SIZE,PHI_MANDALA_CENTER,PHI_MANDALA_LAYER_GEOMETRY,ringSegmentGeometry,circularNodeGeometry,connectorGeometry,radialBarGeometry,textRotation,polarPoint} from './mandala-geometry.js';
import {customerLayerLabel,selectedCatalog} from './customer-language.js';

const SCHEMA='PHI-OS-ECR-CUSTOMER-MANDALA-PROJECTION-v1.0.0';
const zodiacGlyph=Object.freeze({ARIES:'♈',TAURUS:'♉',GEMINI:'♊',CANCER:'♋',LEO:'♌',VIRGO:'♍',LIBRA:'♎',SCORPIO:'♏',SAGITTARIUS:'♐',CAPRICORN:'♑',AQUARIUS:'♒',PISCES:'♓'});
const trigramGlyph=Object.freeze({KUN:'☷',ZHEN:'☳',KAN:'☵',XUN:'☴',LI:'☲',GEN:'☶',QIAN:'☰',DUI:'☱'});
const arr=value=>Array.isArray(value)?value:[];
const isZh=projection=>projection?.locale==='zh-Hans';
const local=(projection,en,zh)=>isZh(projection)?zh:en;
const by=(items,key,id)=>arr(items).find(item=>item?.[key]===id)||null;
const n=value=>Number.isFinite(Number(value))?Number(value):null;
const pct=value=>{const x=n(value);return x===null?'—':`${(x*100).toFixed(1)}%`;};
const deg=value=>{const x=n(value);return x===null?'—':`${x.toFixed(3)}°`;};
const data=value=>esc(String(value??'').replace(/\s+/g,' ').trim());

function nodeAttrs({layer,id,title,copy,meta='',selected=false,relation=''}){
  return `tabindex="0" role="button" data-ecr-mandala-node="true" data-layer="${data(layer)}" data-node-id="${data(id)}" data-detail-title="${data(title)}" data-detail-copy="${data(copy)}" data-detail-meta="${data(meta)}" data-selected="${selected?'true':'false'}"${relation?` data-relation="${data(relation)}"`:''} aria-label="${data(`${title}. ${copy}${meta?` ${meta}`:''}`)}"`;
}

function textAt(point,text,{className='cx-ecr-mandala__code',rotate=null,anchor='middle'}={}){
  return `<text class="${className}" x="${point.x.toFixed(2)}" y="${point.y.toFixed(2)}" text-anchor="${anchor}" dominant-baseline="middle"${rotate===null?'':` transform="rotate(${rotate.toFixed(2)} ${point.x.toFixed(2)} ${point.y.toFixed(2)})"`}>${esc(text)}</text>`;
}

function ring(projection,{catalog,key,idKey,layerId,selectedId,labelFor,detailFor,metaFor,glyphFor=null}){
  const xs=arr(catalog),layer=PHI_MANDALA_LAYER_GEOMETRY[layerId];
  return xs.map(item=>{const id=item[idKey],selected=id===selectedId,g=ringSegmentGeometry(item.ordinal,xs.length,layer),title=labelFor(item),copy=detailFor(item),meta=metaFor?.(item)||'',glyph=glyphFor?.(item)||id;
    return `<g class="cx-ecr-mandala__node cx-ecr-mandala__node--${layerId.toLowerCase()}${selected?' is-selected':''}" ${nodeAttrs({layer:layerId,id,title,copy,meta,selected})}><path d="${g.path}"/><title>${esc(`${id} · ${title}`)}</title>${textAt(g.label,glyph,{rotate:textRotation(g.mid),className:'cx-ecr-mandala__code'})}</g>`;}).join('');
}

function capabilityNetwork(projection){
  const xs=arr(projection.catalogs?.capabilities),selected=projection.selected||{},question=by(projection.catalogs?.questions,'questionId',selected.questionId),qGeometry=question?ringSegmentGeometry(question.ordinal,arr(projection.catalogs?.questions).length,PHI_MANDALA_LAYER_GEOMETRY.Q16):null,qPoint=qGeometry?polarPoint(PHI_MANDALA_LAYER_GEOMETRY.Q16.innerRadius-5,qGeometry.mid):null;
  const nodes=xs.map(item=>{const primary=item.capabilityId===selected.primaryCapabilityId,supporting=arr(selected.supportingCapabilityIds).includes(item.capabilityId),g=circularNodeGeometry(item.ordinal,xs.length),title=isZh(projection)?item.labelZhHans:item.label,copy=primary?local(projection,'Primary response capability for the selected question.','当前基础问题的主要回应能力。'):supporting?local(projection,'Supporting response capability for the selected question.','当前基础问题的辅助回应能力。'):local(projection,'One of the nine governed response capabilities.','九项受治理回应能力之一。'),meta=primary?local(projection,'Primary','主要'):supporting?local(projection,'Supporting','辅助'):item.capabilityId;
    return {item,g,primary,supporting,html:`<g class="cx-ecr-mandala__capability${primary?' is-primary':''}${supporting?' is-supporting':''}" ${nodeAttrs({layer:'R9',id:item.capabilityId,title,copy,meta,selected:primary||supporting,relation:primary?'PRIMARY':supporting?'SUPPORTING':''})}><circle cx="${g.x.toFixed(2)}" cy="${g.y.toFixed(2)}" r="${PHI_MANDALA_LAYER_GEOMETRY.R9.nodeSize}"/><text x="${g.x.toFixed(2)}" y="${g.y.toFixed(2)}" text-anchor="middle" dominant-baseline="middle">${esc(item.capabilityId)}</text></g>`};});
  const connectors=qPoint?nodes.filter(x=>x.primary||x.supporting).map(x=>{const c=connectorGeometry(qPoint,x.g);return `<line class="cx-ecr-mandala__relation${x.primary?' is-primary':' is-supporting'}" x1="${c.x1.toFixed(2)}" y1="${c.y1.toFixed(2)}" x2="${c.x2.toFixed(2)}" y2="${c.y2.toFixed(2)}"/>`;}).join(''):'';
  return connectors+nodes.map(x=>x.html).join('');
}

function driverProfile(projection){
  const catalog=arr(projection.catalogs?.drivers),stack=arr(projection.selected?.driverPriority),index=new Map(stack.map(item=>[item.driverId,item])),max=Math.max(...stack.map(item=>n(item.baselineAffinity)||0),0.000001);
  return catalog.map(item=>{const d=index.get(item.driverId)||{},g=radialBarGeometry(item.ordinal,catalog.length,d.baselineAffinity,max),selected=d.rank===1,title=isZh(projection)?item.labelZhHans:item.label,copy=local(projection,'Birth-baseline affinity. This is not a claim about current Reality priority.','出生基线亲和度；这不是对当前现实优先级的判断。'),meta=local(projection,`Rank ${d.rank??'—'} · ${pct(d.baselineAffinity)} · distance ${deg(d.angularDistanceDegrees)}`,`第 ${d.rank??'—'} 位 · ${pct(d.baselineAffinity)} · 距离 ${deg(d.angularDistanceDegrees)}`);
    return `<g class="cx-ecr-mandala__driver${selected?' is-selected':''}" ${nodeAttrs({layer:'D12',id:item.driverId,title,copy,meta,selected})}><line x1="${g.start.x.toFixed(2)}" y1="${g.start.y.toFixed(2)}" x2="${g.end.x.toFixed(2)}" y2="${g.end.y.toFixed(2)}"/><circle cx="${g.end.x.toFixed(2)}" cy="${g.end.y.toFixed(2)}" r="${selected?4:2.5}"/><title>${esc(`${item.driverId} · ${title} · ${meta}`)}</title></g>`;}).join('');
}

function configurationRing(projection){
  const xs=arr(projection.catalogs?.configurations),selectedId=projection.selected?.configurationId,layer=PHI_MANDALA_LAYER_GEOMETRY.H64;
  return xs.map(item=>{const selected=item.configurationId===selectedId,g=ringSegmentGeometry(item.ordinal,xs.length,layer),title=selected?(isZh(projection)?`${item.chineseNameZhHans} · ${item.configurationId}`:`${item.canonicalName} · ${item.configurationId}`):item.configurationId,copy=selected?local(projection,`Environment priority ${item.environmentPriorityMotionId}; embodied response ${item.embodiedResponseMotionId}.`,`环境优先运动 ${item.environmentPriorityMotionId}；载体回应运动 ${item.embodiedResponseMotionId}。`):local(projection,'One governed environment-response configuration position.','一个受治理的环境—回应构型位置。'),meta=selected?local(projection,`Upper ${item.upperTrigramRef} · Lower ${item.lowerTrigramRef}`,`上卦 ${item.upperTrigramRef} · 下卦 ${item.lowerTrigramRef}`):'';
    return `<g class="cx-ecr-mandala__node cx-ecr-mandala__node--h64${selected?' is-selected':''}" ${nodeAttrs({layer:'H64',id:item.configurationId,title,copy,meta,selected})}><path d="${g.path}"/><title>${esc(title)}</title></g>`;}).join('');
}

function selectedSummary(projection){
  const selected=selectedCatalog(projection),s=projection.selected||{},d=arr(s.driverPriority)[0];
  const c=customerLayerLabel(projection,'CC12',selected.context),g=customerLayerLabel(projection,'G16',selected.grammar),q=customerLayerLabel(projection,'Q16',selected.question),r=customerLayerLabel(projection,'R9',selected.primaryCapability,{role:'PRIMARY'}),m=customerLayerLabel(projection,'M8',selected.motion),h=customerLayerLabel(projection,'H64',selected.configuration),a=customerLayerLabel(projection,'A8',selected.activation),driver=customerLayerLabel(projection,'D12',d);
  const row=(title,x)=>`<div><dt>${esc(title)}</dt><dd><strong>${esc(x.primary)}</strong><small>${esc(x.secondary)}</small></dd></div>`;
  return `<div class="cx-ecr-mandala__summary" data-ecr-mandala-summary><p class="cx-eyebrow">${esc(local(projection,'SELECTED COORDINATE','本次构型坐标'))}</p><dl>${row(local(projection,'Context','背景'),c)}${row(local(projection,'Grammar','语法'),g)}${row(local(projection,'Baseline question','基础问题'),q)}${row(local(projection,'Primary capability','主要能力'),r)}${row(local(projection,'Top baseline driver','首位基线驱动'),driver)}${row(local(projection,'Change motion','变化运动'),m)}${row(local(projection,'Configuration','环境—回应构型'),h)}${row(local(projection,'Activation stage','激活阶段'),a)}</dl></div>`;
}

export function renderPhiMandalaVisual(visual){
  const projection=visual?.payload;if(projection?.schemaVersion!==SCHEMA)return '';
  const selected=projection.selected||{},contexts=arr(projection.catalogs?.contexts),grammars=arr(projection.catalogs?.grammars),questions=arr(projection.catalogs?.questions),motions=arr(projection.catalogs?.motions),activations=arr(projection.catalogs?.activations),context=by(contexts,'contextId',selected.contextId),question=by(questions,'questionId',selected.questionId),title=visual?.title||local(projection,'Your PHI Configuration','你的 PHI 构型'),defaultDetailTitle=context?(isZh(projection)?`${context.labelZhHans} · ${context.contextId}`:`${context.label} · ${context.contextId}`):title,defaultDetailCopy=question?(isZh(projection)?question.questionZhHans:question.question):local(projection,'Explore a layer to see how the selected coordinate is organized.','聚焦任一层，查看这次构型如何被组织。');
  const contextRing=ring(projection,{catalog:contexts,idKey:'contextId',layerId:'CC12',selectedId:selected.contextId,labelFor:item=>isZh(projection)?item.labelZhHans:item.label,detailFor:item=>local(projection,`Longitude interval ${item.startLongitudeInclusive}° to <${item.endLongitudeExclusive}°. Position label only; no zodiac personality meaning is imported.`,`黄经区间 ${item.startLongitudeInclusive}° 至 <${item.endLongitudeExclusive}°。这里只是位置名称，不导入星座人格意义。`),metaFor:item=>item.contextId,glyphFor:item=>`${zodiacGlyph[item.zodiacCode]||''} ${item.contextId}`.trim()});
  const grammarRing=ring(projection,{catalog:grammars,idKey:'grammarId',layerId:'G16',selectedId:selected.grammarId,labelFor:item=>isZh(projection)?item.labelZhHans:item.label,detailFor:item=>local(projection,'One of sixteen governed Reality Grammar positions.','十六个受治理现实语法位置之一。'),metaFor:item=>item.grammarId,glyphFor:item=>item.grammarId});
  const questionRing=ring(projection,{catalog:questions,idKey:'questionId',layerId:'Q16',selectedId:selected.questionId,labelFor:item=>isZh(projection)?item.questionZhHans:item.question,detailFor:item=>local(projection,'The baseline question paired to the same Grammar ordinal.','与同序号现实语法配对的基础问题。'),metaFor:item=>item.questionId,glyphFor:item=>item.questionId});
  const motionRing=ring(projection,{catalog:motions,idKey:'motionId',layerId:'M8',selectedId:selected.motionId,labelFor:item=>isZh(projection)?item.labelZhHans:item.label,detailFor:item=>local(projection,'A PHI OS organization-change motion. Trigram identity is reused without importing I Ching fortune meaning.','PHI OS 的组织变化运动；只复用卦象身份，不导入易经吉凶意义。'),metaFor:item=>`${item.motionId} · ${item.trigramRef}`,glyphFor:item=>`${trigramGlyph[item.trigramCode]||''}${item.motionId}`});
  const activationRing=ring(projection,{catalog:activations,idKey:'activationId',layerId:'A8',selectedId:selected.activationId,labelFor:item=>isZh(projection)?item.labelZhHans:item.label,detailFor:item=>local(projection,'Runtime activation stage. This is not a good/bad score or guaranteed event.','运行激活阶段；不是吉凶分数，也不是必然事件。'),metaFor:item=>item.activationId,glyphFor:item=>item.activationId});
  const center=PHI_MANDALA_CENTER;
  const style=`<style>
    .cx-ecr-mandala__bg{fill:color-mix(in srgb,var(--cx-surface,#fff) 92%,var(--cx-ink,#111));stroke:var(--cx-line,#d8d3c7);stroke-width:1}.cx-ecr-mandala__node{cursor:pointer;outline:none}.cx-ecr-mandala__node path{fill:color-mix(in srgb,var(--cx-surface,#fff) 88%,var(--cx-ink,#111));stroke:var(--cx-line,#d8d3c7);stroke-width:.8;transition:opacity .18s ease,filter .18s ease,fill .18s ease}.cx-ecr-mandala__node:hover path,.cx-ecr-mandala__node:focus path,.cx-ecr-mandala__node.is-active path{filter:brightness(.96);stroke:var(--cx-accent,#9b6a16);stroke-width:1.8}.cx-ecr-mandala__node.is-selected path,.cx-ecr-mandala__node[data-selected="true"] path{fill:color-mix(in srgb,var(--cx-accent,#9b6a16) 26%,var(--cx-surface,#fff));stroke:var(--cx-accent,#9b6a16);stroke-width:1.6}.cx-ecr-mandala__code{font:600 9px system-ui,sans-serif;fill:var(--cx-ink,#111);pointer-events:none}.cx-ecr-mandala__relation{stroke:var(--cx-line,#b7b0a0);stroke-width:1.5;stroke-dasharray:3 3}.cx-ecr-mandala__relation.is-primary{stroke:var(--cx-accent,#9b6a16);stroke-width:3;stroke-dasharray:none}.cx-ecr-mandala__capability circle{fill:var(--cx-surface,#fff);stroke:var(--cx-line,#b7b0a0);stroke-width:1.2}.cx-ecr-mandala__capability text{font:700 8px system-ui,sans-serif;fill:var(--cx-ink,#111);pointer-events:none}.cx-ecr-mandala__capability.is-primary circle{fill:var(--cx-accent,#9b6a16);stroke:var(--cx-accent,#9b6a16)}.cx-ecr-mandala__capability.is-primary text{fill:var(--cx-surface,#fff)}.cx-ecr-mandala__capability.is-supporting circle{stroke:var(--cx-accent,#9b6a16);stroke-width:2}.cx-ecr-mandala__driver line{stroke:color-mix(in srgb,var(--cx-ink,#111) 35%,transparent);stroke-width:8;stroke-linecap:round}.cx-ecr-mandala__driver circle{fill:var(--cx-ink,#111)}.cx-ecr-mandala__driver.is-selected line{stroke:var(--cx-accent,#9b6a16);stroke-width:10}.cx-ecr-mandala__driver.is-selected circle{fill:var(--cx-accent,#9b6a16)}.cx-ecr-mandala__core{fill:var(--cx-surface,#fff);stroke:var(--cx-accent,#9b6a16);stroke-width:2}.cx-ecr-mandala__core-title{font:700 15px system-ui,sans-serif;letter-spacing:.08em;fill:var(--cx-ink,#111)}.cx-ecr-mandala__core-meta{font:500 10px system-ui,sans-serif;fill:var(--cx-muted,#6b675f)}.cx-ecr-mandala__summary dd{display:grid;gap:.08rem}.cx-ecr-mandala__summary dd small{color:var(--cx-muted,#6b675f)}.cx-ecr-mandala__guide{fill:none;stroke:color-mix(in srgb,var(--cx-line,#d8d3c7) 65%,transparent);stroke-width:.8}@media(prefers-reduced-motion:reduce){.cx-ecr-mandala__node path{transition:none}}
  </style>`;
  const guides=[338,300,262,218,184,148,118,84].map(r=>`<circle class="cx-ecr-mandala__guide" cx="${center.x}" cy="${center.y}" r="${r}"/>`).join('');
  const svg=`<svg class="cx-ecr-mandala__svg" data-ecr-phi-mandala="true" viewBox="0 0 ${PHI_MANDALA_VIEWBOX_SIZE} ${PHI_MANDALA_VIEWBOX_SIZE}" role="img" aria-labelledby="ecr-mandala-title ecr-mandala-desc" preserveAspectRatio="xMidYMid meet">${style}<title id="ecr-mandala-title">${esc(title)}</title><desc id="ecr-mandala-desc">${esc(local(projection,'Interactive PHI Configuration map. Focus a segment to inspect its governed layer.','互动 PHI 构型图。聚焦任一分区，可查看该受治理层的说明。'))}</desc><circle class="cx-ecr-mandala__bg" cx="${center.x}" cy="${center.y}" r="382"/>${guides}${contextRing}${grammarRing}${questionRing}${capabilityNetwork(projection)}${driverProfile(projection)}${motionRing}${configurationRing(projection)}${activationRing}<circle class="cx-ecr-mandala__core" cx="${center.x}" cy="${center.y}" r="${PHI_MANDALA_LAYER_GEOMETRY.CORE.radius}"/><text class="cx-ecr-mandala__core-title" x="${center.x}" y="${center.y-12}" text-anchor="middle">${esc(local(projection,'PHI CONFIGURATION','PHI 构型'))}</text><text class="cx-ecr-mandala__core-meta" x="${center.x}" y="${center.y+9}" text-anchor="middle">${esc(`${local(projection,'Solar anchor','太阳锚点')} ${deg(projection.anchor?.longitude)}`)}</text><text class="cx-ecr-mandala__core-meta" x="${center.x}" y="${center.y+26}" text-anchor="middle">${esc(`${selected.contextId||''} · ${selected.grammarId||''} · ${selected.questionId||''}`)}</text><text class="cx-ecr-mandala__core-meta" x="${center.x}" y="${center.y+42}" text-anchor="middle">${esc(`${selected.motionId||''} · ${selected.configurationId||''} · ${selected.activationId||''}`)}</text></svg>`;
  return `<section id="ecr-section-01" class="cx-ecr-mandala" data-ecr-mandala-root="true"><header><p class="cx-eyebrow">${esc(local(projection,'PHI OS ORIGINAL · INTERACTIVE CONFIGURATION MAP','PHI OS 原生体系 · 互动构型图'))}</p><h3>${esc(title)}</h3><p>${esc(local(projection,'All highlighted positions come from the governed ECR projection. The diagram calculates layout only; it does not recalculate your ECR result.','所有高亮位置都来自受治理的 ECR 投影。本图只计算视觉几何，不会重新计算你的 ECR 结果。'))}</p></header>${svg}<aside class="cx-ecr-mandala__detail" data-ecr-mandala-detail aria-live="polite"><strong data-ecr-mandala-detail-title>${esc(defaultDetailTitle)}</strong><p data-ecr-mandala-detail-copy>${esc(defaultDetailCopy)}</p><small data-ecr-mandala-detail-meta>${esc(`${local(projection,'Solar anchor','太阳锚点')} ${deg(projection.anchor?.longitude)} · ${local(projection,'Technical indices remain visible for traceability.','保留技术编号以便追溯。')}`)}</small></aside>${selectedSummary(projection)}<p class="cx-meta">${esc(local(projection,'Calculated astronomical position; PHI OS first-party interpretive layer mappings. Not a scientific personality fact or guaranteed outcome.','天文位置属于计算结果；各层映射属于 PHI OS 原生解释约定，不是科学人格事实或必然结果。'))}</p></section>`;
}

function activate(root,node){
  if(!root||!node)return;root.querySelectorAll('[data-ecr-mandala-node].is-active').forEach(item=>item.classList.remove('is-active'));node.classList.add('is-active');const detail=root.querySelector('[data-ecr-mandala-detail]');if(!detail)return;const title=detail.querySelector('[data-ecr-mandala-detail-title]'),copy=detail.querySelector('[data-ecr-mandala-detail-copy]'),meta=detail.querySelector('[data-ecr-mandala-detail-meta]');if(title)title.textContent=node.dataset.detailTitle||node.dataset.nodeId||'';if(copy)copy.textContent=node.dataset.detailCopy||'';if(meta)meta.textContent=node.dataset.detailMeta||'';
}

export function installPhiMandalaInteractions(scope){
  const roots=[...scope?.querySelectorAll?.('[data-ecr-mandala-root]')||[]];for(const root of roots){if(root.dataset.ecrMandalaInteractionInstalled==='true')continue;const onTarget=event=>{const node=event.target.closest?.('[data-ecr-mandala-node]');if(node&&root.contains(node))activate(root,node)};root.addEventListener('pointerover',onTarget);root.addEventListener('focusin',onTarget);root.addEventListener('click',onTarget);root.dataset.ecrMandalaInteractionInstalled='true';const initial=root.querySelector('[data-ecr-mandala-node][data-selected="true"]');if(initial)activate(root,initial);}return roots.length;
}

export default Object.freeze({renderPhiMandalaVisual,installPhiMandalaInteractions});
