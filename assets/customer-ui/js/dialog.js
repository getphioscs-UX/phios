export function installCustomerDialogs(scope=document){scope.addEventListener('click',event=>{const open=event.target.closest('[data-cx-dialog-open]');if(open){const dialog=scope.querySelector(`#${CSS.escape(open.dataset.cxDialogOpen)}`);if(dialog instanceof HTMLDialogElement)dialog.showModal();return}const close=event.target.closest('[data-cx-dialog-close]');if(close){const dialog=close.closest('dialog');dialog?.close();}});scope.querySelectorAll('dialog.cx-dialog').forEach(dialog=>dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()}));}
