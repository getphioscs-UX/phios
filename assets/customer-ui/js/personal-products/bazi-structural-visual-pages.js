import {esc} from '../surfaces/runtime-ui.js';
// Shared report renderer's bounded M03–M05 composition implementation.
// Geometry encodes values already bound in Page IR; it creates no reading.
export function renderBaziStructuralBatch(report){
 if(report.reviewMode!==true||report.customerPublishable!==false||report.depth!=='PAID'||report.pages.length!==5||report.pages.some((p,i)=>p.pageNumber!==i+6))throw Error('BAZI_BATCH_REVIEW_SCOPE_REQUIRED');
 const locale=report.locale,t=(en,zh)=>locale==='bilingual'?`${zh} / ${en}`:locale==='zh-Hans'?zh:en;
 const text=(value)=>esc(value),panel=(title,body,role='')=>`<section class="vrpt-tile" ${role?`data-part="${role}"`:''}><h3>${text(title)}</h3>${body}</section>`;
 const context=v=>`<div data-part="context">${panel(t('Day Master','日主'),`<b data-glyph data-element="${v.context.dayMaster.element}">${text(v.context.dayMaster.glyph)}</b><p>${text(v.context.dayMaster.code)} · ${text(v.context.dayMaster.polarity)} ${text(v.context.dayMaster.label)}</p>`)}${panel(t('Season','季节'),`<b data-glyph data-element="${v.context.season.element}">${text(v.context.season.glyph)}</b><p>${text(v.context.season.name)} · ${text(v.context.season.label)}</p>`)}</div>`;
 const pillars=(v,detail=false)=>`<div data-part="pillars">${v.pillars.map(p=>`<article class="vrpt-tile" data-selected="${p.id==='DAY'}"><h4>${text(p.label)}</h4><small>${text(t('Heavenly stem','天干'))}</small><b data-glyph data-element="${p.stemElement}">${text(p.stem)}</b><span>${text(p.stemCode)} · ${text(p.polarity)}</span><strong data-element="${p.stemElement}">${text(p.stemLabel)}</strong><hr><small>${text(t('Earthly branch','地支'))}</small><b data-glyph data-element="${p.branchElement}">${text(p.branch)}</b><span>${text(p.branchCode)}</span><strong data-element="${p.branchElement}">${text(p.branchLabel)}</strong>${detail&&p.hiddenStems?`<hr><small>${text(t('Hidden stems','藏干'))}</small><p>${text(p.hiddenStems)}</p>`:''}</article>`).join('')}</div>`;
 const donut=v=>{const total=v.elements.reduce((s,n)=>s+n.value,0),circ=2*Math.PI*88;let offset=0;return `<svg data-part="donut" viewBox="0 0 260 260" role="img" aria-label="${text(v.elements.map(n=>`${n.label}: ${n.value}`).join('; '))}"><title>${text(v.unit)}</title>${v.elements.map(n=>{const start=offset;offset+=n.value/total*circ;return `<circle data-element="${n.id}" cx="130" cy="130" r="88" fill="none" stroke="currentColor" stroke-width="32" stroke-dasharray="${n.value/total*circ} ${circ}" stroke-dashoffset="${-start}" transform="rotate(-90 130 130)"/>`;}).join('')}<text x="130" y="125" text-anchor="middle" data-total>${total}</text><text x="130" y="149" text-anchor="middle">${text(t('COUNT','计数'))}</text></svg>`;};
 const bars=v=>`<div data-part="bars">${[...v.elements].sort((a,b)=>b.value-a.value).map(n=>`<div data-element="${n.id}"><span>${text(n.label)}</span><svg viewBox="0 0 160 12" role="img" aria-label="${text(`${n.label}: ${n.value} / ${v.context.total}`)}"><rect data-track width="160" height="12" rx="6"/><rect width="${160*n.value/v.context.total}" height="12" rx="6" fill="currentColor"/></svg><strong>${n.value}</strong></div>`).join('')}<small>${text(t('Count / total inventory','计数／清单总数'))}: ${v.context.total}</small></div>`;
 const distribution=v=>`<div data-part="distribution">${donut(v)}${bars(v)}</div>`;
 const seasonal=v=>{const c=v.context;return `<div data-part="season-structure"><svg viewBox="0 0 440 400" role="img" aria-label="${text(`${t('Day Master','日主')}: ${c.dayMaster.code}; ${t('Season','季节')}: ${c.season.name}; ${t('Root records','根气记录')}: ${c.roots.value}`)}"><circle class="vrpt-orbit" cx="220" cy="200" r="155"/><path class="vrpt-link" d="M220 85V315M75 200H365"/>${[[220,58,c.season.glyph,c.season.name],[75,200,String(c.roots.value),t('Roots','根气')],[365,200,'—',t('Open','开放')],[220,342,c.season.label,t('Month element','月令五行')]].map(([x,y,a,b])=>`<g class="vrpt-node"><circle cx="${x}" cy="${y}" r="55"/><text x="${x}" y="${y-4}" text-anchor="middle">${text(a)}</text><text data-caption x="${x}" y="${y+20}" text-anchor="middle">${text(b)}</text></g>`).join('')}<g class="vrpt-node" data-center><circle cx="220" cy="200" r="73"/><text data-total x="220" y="196" text-anchor="middle">${text(c.dayMaster.glyph)}</text><text x="220" y="226" text-anchor="middle">${text(c.dayMaster.label)}</text></g></svg>${context(v)}</div>`;};
 const relationships=v=>{const pts=v.nodes.map((n,i)=>({id:n.id,x:220+145*Math.cos(-Math.PI/2+i*2*Math.PI/5),y:205+145*Math.sin(-Math.PI/2+i*2*Math.PI/5)}));return `<svg data-part="relationships" viewBox="0 0 440 410" role="img" aria-label="${text(t('Solid arrows generate; dashed arrows control. Counts label the inventory only.','实线箭头相生，虚线箭头相克。数字仅表示清单计数。'))}"><defs><marker id="arrow-${locale}" markerWidth="7" markerHeight="7" refX="6" refY="3" orient="auto"><path d="M0 0L6 3L0 6" fill="currentColor"/></marker></defs><circle class="vrpt-orbit" cx="220" cy="205" r="145"/>${v.edges.map(e=>{const a=pts.find(p=>p.id===e.from),b=pts.find(p=>p.id===e.to),dx=b.x-a.x,dy=b.y-a.y,d=Math.hypot(dx,dy),x1=a.x+dx/d*43,y1=a.y+dy/d*43,x2=b.x-dx/d*48,y2=b.y-dy/d*48;return `<path class="vrpt-link" ${e.relation==='SUBJECT_CONTROLS_TARGET'?'stroke-dasharray="5 5"':''} marker-end="url(#arrow-${locale})" d="M${x1} ${y1}L${x2} ${y2}"/>`;}).join('')}${v.nodes.map((n,i)=>`<g class="vrpt-node" data-element="${n.id}"><circle cx="${pts[i].x}" cy="${pts[i].y}" r="43"/><text x="${pts[i].x}" y="${pts[i].y-2}" text-anchor="middle">${text(n.label)}</text><text x="${pts[i].x}" y="${pts[i].y+22}" text-anchor="middle">${n.value}</text></g>`).join('')}</svg>`;};
 const body=p=>{const v=p.visual;
  if(p.pageNumber===6)return panel(t('Four pillars','四柱命盘'),`<div data-part="snapshot">${pillars(v)}${context(v)}</div>`)+panel(t('Five elements · unweighted inventory','五行 · 未加权清单'),distribution(v));
  if(p.pageNumber===7)return panel(t('Day Master in context','日主的结构位置'),seasonal(v))+panel(t('Seasonal reference','季节定位'),`<div data-part="season-line">${[['SPRING','Spring','春'],['SUMMER','Summer','夏'],['AUTUMN','Autumn','秋'],['WINTER','Winter','冬']].map(([code,en,zh])=>`<span data-selected="${code===v.context.season.name.toUpperCase()||v.context.season.name.includes(zh)}">${text(t(en,zh))}</span>`).join('')}</div><div data-part="metrics"><span>${text(t('Month branch','月支'))}<b>${text(v.context.season.glyph)}</b></span><span>${text(t('Element','五行'))}<b>${text(v.context.season.label)}</b></span><span>${text(t('Root records','根气记录'))}<b>${v.context.roots.value}</b></span></div>`);
  if(p.pageNumber===8)return panel(t('Stems · branches · positions','天干 · 地支 · 柱位'),pillars(v,true))+panel(t('The day stem is the reference point','以日干为参照'),`<div data-part="metrics"><span>${text(t('Day Master','日主'))}<b>${text(v.context.dayMaster.glyph)} ${text(v.context.dayMaster.label)}</b></span><span>${text(t('Month command','月令'))}<b>${text(v.context.season.glyph)} ${text(v.context.season.label)}</b></span><span>${text(t('Pillars','四柱'))}<b>${v.pillars.length}</b></span></div>`);
  if(p.pageNumber===9)return panel(t('Element inventory','五行清单'),distribution(v))+panel(t('Count composition','计数构成'),`<table data-part="inventory"><thead><tr><th>${text(t('Element','五行'))}</th><th>${text(t('Stems','天干'))}</th><th>${text(t('Branches','地支'))}</th><th>${text(t('Hidden','藏干'))}</th><th>${text(t('Total','合计'))}</th></tr></thead><tbody>${v.elements.map(n=>`<tr><th data-element="${n.id}">${text(n.label)}</th><td>${n.breakdown.visibleStems}</td><td>${n.breakdown.visibleBranches}</td><td>${n.breakdown.hiddenStemsUnweighted}</td><td>${n.value}</td></tr>`).join('')}</tbody></table>`);
  return panel(t('Generating & controlling cycles','相生与相克循环'),`<div data-part="relationship-layout">${relationships(v)}<div data-part="relation-keys">${['SUBJECT_GENERATES_TARGET','SUBJECT_CONTROLS_TARGET'].map((kind,i)=>`<section><h4>${text(i?t('Dashed → controls','虚线 → 相克'):t('Solid → generates','实线 → 相生'))}</h4>${v.edges.filter(e=>e.relation===kind).map(e=>`<p>${text(v.nodes.find(n=>n.id===e.from).label)} → ${text(v.nodes.find(n=>n.id===e.to).label)}</p>`).join('')}</section>`).join('')}</div></div>`)+panel(t('Inventory stays distinct from relationship intensity','清单计数不等于关系强度'),bars(v));
 };
 return `<article class="vrpt-report" data-visual-batch="${report.visualBatch}" data-report-locale="${locale}" lang="${locale==='bilingual'?'zh-Hans':locale}">${report.pages.map(p=>`<section class="vrpt-page" id="${p.pageId}" data-page-number="${p.pageNumber}" data-master="${p.visualTemplateId}"><header data-part="chrome"><div><strong>P H I O S</strong><small>HUMAN POTENTIAL INTELLIGENCE</small></div><span>${text(t('BAZI · FULL REPORT','八字 · 完整报告'))}<br>${String(p.pageNumber).padStart(2,'0')}</span></header><div class="vrpt-heading"><span data-part="page-index">${String(p.pageNumber).padStart(2,'0')}<small>/ 26</small></span><div><small>${text(t('STRUCTURAL','结构层'))}</small><h2>${p.title.split(" / ").map(part=>`<span>${text(part)}</span>`).join("")}</h2><p>${text(p.question)}</p></div></div><div data-part="page-body"><figure class="vrpt-primary">${body(p)}<figcaption>${text(p.visual.unit)}</figcaption></figure><div class="vrpt-insights">${p.insights.map((x,i)=>`<article class="vrpt-tile" data-card-type="${['PRIMARY_INSIGHT','CONDITION','OPEN_QUESTION'][i]}" data-source-ref="${text(x.sourceRef)}"><small>${text([t('Structure','结构'),t('Condition','条件'),t('Observe','观察')][i])}</small><p>${text(x.text)}</p></article>`).join('')}</div></div><aside class="vrpt-boundary">${text(p.boundaryText)}</aside><details class="vrpt-evidence"><summary>${text(t('Data & source lineage','数据与来源追溯'))}</summary><ul>${p.evidenceRefs.map(ref=>`<li>${text(ref)}</li>`).join('')}</ul></details><footer><span>${text(t('Your life, in context.','在情境中理解你的人生。'))}</span><span>${String(p.pageNumber).padStart(2,'0')} / 26</span></footer></section>`).join('')}</article>`;
}
