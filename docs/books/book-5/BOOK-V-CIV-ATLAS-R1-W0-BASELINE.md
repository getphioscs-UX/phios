# BOOK-V-CIV-ATLAS-R1-W0｜Baseline Reconciliation & Freeze

Baseline: `e8d9c5c0b222fabfae9bae0ed9abb6e098705c1f` (`PHASE 9` latest main supplied for this wave).

## Result

`NO_DRIFT / NO_STALE_BASELINE / NO_BOOK_IV_A6_OVERWRITE`

`BOOK-IV-A6` is already `PRODUCTION_ADMITTED`, so `BOOK-V-CIV-ATLAS-R1` may enter its foundation wave. This commit is deliberately limited to W0–W2. It does not execute W3 state, W4 public shell, or any later customer layer.

## Book V authority

Book V remains `BOOK-5 / part-12 / P12` at `/books/reality-differentiation/`. The public page continues to use the shared seven-volume renderer `assets/js/pages/book-volume-seven.js`. W0–W2 do not modify the page or renderer.

Part 12 canonical node identities remain under the existing Knowledge Registry. Publication ownership is resolved by the active seven-volume publication context, where `P12 -> BOOK-5`. Older canonical-node records that still carry predecessor five-volume publication fields are historical provenance, not Atlas publication authority.

## Visual authority

Atlas does not create an image authority or resolver. Book identity/cover/branding remains under the current seven-volume publication pointer and R2 visual binding. Generic client assets continue through the existing current client visual registry. Phase 9 PVP remains projection-only.

## Contextual Ask authority

The current customer cutover freeze keeps the canonical Ask route at `/knowledge/ask/`. Atlas W0–W2 do not modify Ask, retrieval, composer, consent, or context-source ownership. A later Atlas bridge may state what the customer is viewing; it may not dictate the answer.

## Protected surfaces

The baseline audit records SHA-256 snapshots for the Book V page, generic renderer, public shell/PDS, seven-volume architecture and visual authorities, current Contextual Ask authorities, BOOK-IV-A6 admission, and the Phase 9 freeze. `package.json` is merge-sensitive and is modified only to add the W1/W2 checker aliases.

## Next gate

W1 freezes Atlas authority/layer boundaries. W2 establishes schema-valid empty/seed registries. Public UI remains unchanged until W4.
