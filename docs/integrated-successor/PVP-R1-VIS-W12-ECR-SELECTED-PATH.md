# PVP-R1-VIS-W12 — ECR Selected Configuration Path

Baseline: `ffc05bc07345011bc24e68884cc1d569c8d96418`.

Phase 8 reuses the existing ECR Mandala rather than creating a second renderer. W12 freezes the selected-configuration path as a projection-only visual relation over already governed selected nodes. Paid depth may connect all eight selected layers; Free Snapshot connects the five free-visible selected points and withholds M8/H64/A8 personalized depth from the path.

The path is decorative (`aria-hidden`, no pointer interaction), creates no ECR meaning, performs no semantic selection, and does not change calculation authority.

Status: `MACHINE_ACCEPTED_SELECTED_CONFIGURATION_PATH`.
