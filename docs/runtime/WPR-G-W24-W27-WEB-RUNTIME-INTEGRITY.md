# PHI OS — WPR-G Web Runtime Integrity

Baseline: `d150a741231abe608a0d994e9e5787e6c71cfc3d`.

WPR-W24 establishes a single hydration execution contract over existing Canonical Web Production records. It does not create persistence, professional data-read authority, Method execution, or report authority.

WPR-W25 establishes current production discovery on `https://phios-github.pages.dev` from WPR indexability plus the existing Published Articles public projection. `https://getphios.com` remains a future canonical origin until deployment verification; switching origin requires a versioned successor.

WPR-W26 reconciles response security: private/ephemeral routes are `private, no-store` and `noindex`; CSP admits only the already-used Google Fonts origins and governed R2/getphios image origin classes. Robots is discovery guidance, never authorization.

WPR-W27 maps all 38 limited-production records to frozen PDS/CPR responsive and accessibility authority. Existing WPR CSS raw-breakpoint drift is recorded, not silently redesigned. Production browser revalidation remains required at 360/768/1440 in en/zh-Hans.

WPR remains a projection/runtime-delivery layer. RDG, PWS, PR, RR, CPR and PDS authority are preserved.
