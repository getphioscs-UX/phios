# ECR V4.1 — Section 46 handoff

## 46.1 Baseline

Starting HEAD: `b5a2123a7d760941041f724715fa67115f3c8207`. Ending HEAD observed: `2de6cdea8f9680b5e80d8e43f05b1ccc2cf4842f` (`ECR-V4`). Branch: `main`. HEAD advanced externally during execution; this agent did not commit, push, reset or deploy. The working tree retains the final Carrier hierarchy correction, regenerated evidence and this handoff. The five pre-existing BaZi edits were preserved; S02 owner acceptance is recorded. S03 generation is deferred while this ECR request is handled.

## 46.2 Audit findings

- Predecessor H64: `floor(normalized longitude / 5.625)`, zero-degree origin; semantic environment order is not astronomical order.
- Predecessor M8: `floor(longitude / 45)`. Predecessor A8: eight divisions of that old H64 sector.
- Reused shared P64 Gate/Line helpers, AST production ephemeris, `TRUE_NODE.V1` and the existing 88-degree solar-arc solver through a neutral adapter. No fixed-day subtraction.
- Chiron: no governed shared capability found; UNKNOWN.
- Ten executable Figure contracts did not exist under ECR. Added contracts transcribed from the owner's V4/V4.1 text; no independent source-image inspection is claimed. Existing MIR-6 Carrier/Experience interpretation boundaries remain referenced and preserved.

## 46.3 Files

Exact inventories: [changed-files.json](changed-files.json). Runtime modules remain under existing ECR ownership; the existing customer route and renderer consume a versioned successor. Historical accepted R1/R1A semantics and the 48-card deck remain unchanged.

## 46.4 Mechanical proof

- 302° → Gate 41 / Line 1 → ECR-H48 → upper GEN / M6; lower DUI / M8.
- 358.250° → 3.875° spans Gate 25 / ECR-H50; 360° and 0° return the same coordinate.
- 302.937500° → Gate 41 / Line 2 exact boundary.
- 302.703125° → Gate 41 / Line 1 / A2 exact boundary. Line and A8 are independent.
- Tests traverse 64 sectors, 384 Line starts and 512 A8 starts, including values immediately before boundaries. Twelve synthetic births replay the governed solar-arc solver deterministically.

## 46.5 Runtime proof

[Runtime lineage proof](runtime-lineage-proof.json): synthetic birth `2000-01-01T04:00:00Z` → prior Design `1999-10-05T17:32:31.169Z` → shared P64 and H64 bridge → D1–D12 field → K2A → C1 → C2 → C3 → C4 → C5 → two distinct feedback architectures.

Current Reality is UNBOUND in this proof. No observation was invented. Personal taxonomy and unadmitted semantic mappings stay UNKNOWN. Current Reality reuses the existing canonical observation/comparison owner; self-reported recovery/drift context does not become a diagnosis or an inferred dynamic state.

## 46.6 Checks

Exact commands, statuses and reasons: [check-results.json](check-results.json). Full `npm.cmd run check` (including precheck and postcheck) and final `npm.cmd run check:pages-build` both completed with exit code 0. All 17 ECR successor checks passed. Local browser verification passed for en / zh-Hans at 1440 / 390, including keyboard selection, fullscreen Escape and print overflow. These results do not constitute deployed customer E2E.

Two integration failures were corrected: prohibited JSON import attributes (replaced by a reproducible generated authority bundle), and shared API digest drift (registered through the existing current-owner successor mechanism, retaining historical hashes). No predecessor assertion was removed.

## 46.7 Human-review status

- Machine accepted scope: implemented mechanical, lineage, fail-closed and local presentation checks PASS (W1–W17 and full repository checks). This does not supply missing personal mappings or constitute unconditional completion of every semantic requirement.
- Human review: 28 bilingual report-section candidates PENDING.
- Human accepted: no ECR V4.1 semantic or visual decision recorded. BaZi S02 acceptance is separate.
- Customer production admitted: NO. The customer experience remains on its admitted predecessor.

Review: [English](review-en.html) · [简体中文](review-zh-Hans.html). These are internal synthetic review artifacts, not released personal reports.

## 46.8 Remaining gaps

1. Chiron calculation is unavailable under shared astronomy.
2. Exact PHI-neutral personal resolvers for the source taxonomies are not admitted.
3. Gate/Line source identities have no admitted personal C1–C5 owner mapping. The 297 architecture addresses do not turn source coverage into customer meaning.
4. Exact mapping from accepted 48-card semantics to the new six runtime positions remains unresolved; the selector returns six explicit UNKNOWN positions rather than arbitrary cards.
5. No admitted rule converts Current Reality self-reports into current driver priority, bottleneck or dynamic loop state. Observations/comparisons are retained; dynamic conclusions stay UNKNOWN.
6. Bilingual structural parity passed; human prose/semantic parity and successor visual acceptance remain pending. Existing topic meaning cannot be carried into successor geometry without admitted mapping.
7. Local browser checks passed. Deployed customer E2E and Production deployment were not run.

This implementation must not be called FULL_PRODUCTION_ACCEPTED or a fully admitted personal interpretation while these gates remain.


