# I Ching / Tarot current reconciliation report

Baseline commit: `6eb24e4e55a77859b0068836da7fa89e946ae3b1`  
Input archive: `read(1).zip`  
Successors: `ICH-PROD-W10`, `TPA-W35`  
Result: `CURRENT_SOURCE_RUNTIMES_RECONCILED_PRODUCTION_ACTIVATION_NOT_GRANTED`

## Outcome

The I Ching product source Runtime remains complete and frozen. The historical
`ICH-PROD-W0–W9` checker reached its frozen `package.json` / shared-context
digest boundary only after its deterministic calculation, interpretation,
Reality, product view and fail-closed execution assertions passed. A current
successor now preserves that historical evidence while allowing governed,
additive package and shared-API evolution.

The shared symbolic context API now reports source readiness truthfully for both
`I_CHING` and `TAROT`. Method execution authority remains separate:

- I Ching continues to use its trusted server-side execution authority.
- Tarot remains `HUMAN_ACCEPTANCE_PENDING` and non-executable.
- Neither method gains public, Production or Professional authority.

Tarot Phase H source persistence remains D1-ready and identity-scoped. The
historical Tarot persistence acceptance is preserved; a current successor
reconciles the shared context API change introduced after its freeze.

## Validation

Passed on the aligned archive:

- I Ching structural Runtime `ICH-W0–W13`
- I Ching interpretation and Reality `ICHI-W0–W9`
- I Ching product current successor `ICH-PROD-W10`
- Tarot product phases A–G
- Tarot persistence current successor `TPA-W35`
- Symbolic public UX, sensitive-domain, static browser, human-review and
  production-candidate gates
- `PWS-I2 v1.0.0` acceptance and freeze, including the 71 registry objects /
  70 relationships reconciliation
- Current System Baseline successor
- Current Method Production Activation consolidation `MPA-W0–W30`

The aggregate `npm` aliases could not be invoked in the archive-analysis
environment because the command host classified the spawned package-manager
process as a network action. The exact Node checkers behind those aliases were
executed directly.

## Product activation inventory

| Runtime | Source / technical state | Current product authority | Remaining activation work |
| --- | --- | --- | --- |
| Astrology (`AST`) | Implemented | PCM v6 `AVAILABLE` | Public client execution is still separately gated |
| BaZi (`BZR`) | Implemented | PCM v6 `AVAILABLE` | Public client execution is still separately gated |
| Numerology (`NUM`) | Implemented | PCM v6 `AVAILABLE` | Public client execution is still separately gated |
| Zi Wei Dou Shu (`ZWR`) | Implemented | PCM v6 `AVAILABLE` | Public client execution is still separately gated |
| I Ching (`ICH`) | Structural, interpretation, Reality and product source Runtime complete | `COMING_LATER`; `runAllowed=false` | 24 accepted human sessions, verified persistence identity/provider, live browser acceptance and live Production SHA alignment |
| Tarot (`TAR`) | Structural, interpretation, product surface and D1 source persistence complete | `COMING_LATER`; `runAllowed=false` | 24 accepted human sessions, global verified account provider, live browser acceptance and live Production SHA alignment |
| Human Design (`HDR`) | Calculation/projection implemented for validation only | `BLOCKED` | Commercial rights/license, authorized mapping/algorithm authority, controlled public vocabulary and commercial approval |
| Psychology (`PSY`) | Registry holding only | Not in current PCM/public product catalog | Method definition, implementation, validation and independent activation |

PCM `AVAILABLE` and public self-service execution are separate authorities.
The current PX2 public method catalog remains fail-closed with
`runAllowed=false`, so no table row above should be read as an instruction to
open a public execution route without its own gate.

## Separate unresolved successor

`scripts/check-stage15-reality-orchestration.mjs` remains historical and fails
because it requires `reality-entry.html` to load
`reality-entry-stage15-handoff.js`. Current web authority intentionally treats
`reality-entry.html` as a compatibility redirect shell to the canonical
`/reality/` surface.

Reactivating the legacy page merely to satisfy the old regex would conflict with
the current route authority and would not produce a valid handoff: the redirect
drops the popup handshake before the old module can receive its bundle. This
must be resolved as a versioned current Reality-handoff successor that targets
the canonical Reality surface, with live browser verification. It is not an
I Ching calculation, interpretation or product-source defect.

## Source corpus note

The uploaded course PDFs are not required to complete either source Runtime.
They can help a later private research and source-mapping phase, but they must
not be promoted into a public canonical interpretation corpus without explicit
rights/provenance clearance and source-by-source admission.
