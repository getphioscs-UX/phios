# EXP-G1 Open Defects

EXP-G1 has **0 P0** and **0 P1** defects in scope. The following lower-priority issues remain explicit and do not reduce a required page below its gate threshold.

| ID | Page | Location | Current behavior | PDS effect | User impact | Priority | Required later owner |
|---|---|---|---|---|---|---|---|
| G1-D01 | Entry | Customer input and recovery state | Some Journey vocabulary remains necessary around recovery and saved state even though the primary task is plain-language description. | Customer language; hierarchy | A first-time customer may need one extra reading pass before distinguishing input from recovery. | P2 | Future Entry production polish; not PWS-I4 |
| G1-D02 | Reconstruction | Customer cards, supporting evidence layer | Source and confidence concepts remain available after the customer summary to preserve correction traceability. | Progressive disclosure | The second layer is denser than the first, but default comprehension and correction are not blocked. | P2 | Future Reconstruction production polish |
| G1-D03 | Navigation | Six-field direction comparison | Complete action criteria make each direction card information-dense. | Information hierarchy | Customers with several available directions may need more comparison time; selection remains explicit and bounded. | P2 | Future Navigation production polish |
| G1-D04 | Stateful Journey pages | Cross-device recovery boundary | Browser-held recovery is explicit; cross-device recovery remains unavailable without a supported account capability. | Product completion | A customer changing devices cannot assume the current browser Journey follows automatically. | P2 | Later authorised account/runtime capability; not a gate waiver |
| G1-D05 | About | Responsibility narrative | The page deliberately keeps its main action inside the document before offering external continuation. | Next-step clarity | Visitors looking only for a destination need to read the responsibility section first. | P3 | Future About content review |
| G1-D06 | Production acceptance script | `scripts/check-m3c-public-journey-production.mjs` | The live check retained the removed pre-EXP-W3 `stage-entry` marker. EXP-G1 replaces it with the current `journey-flow-title` marker. | Acceptance evidence integrity | Before repair the live script falsely failed an unchanged, correctly deployed Overview. No customer behavior was affected. | Closed P2 | Closed in EXP-G1 |
| G1-D07 | Knowledge Registry directory | `content/knowledge/registry/m3c-navigation-operationalization.json` | `d7b5a9f` placed the EXP-W6 Navigation renderer acceptance hash under Knowledge instead of updating its authority file at `content/registry/m3c-navigation-operationalization.json`. | One source of truth; Registry boundary | The misplaced file raised the frozen Knowledge count to 13 while Navigation’s real acceptance still rejected the W6 renderer. | Closed P1 | Misplaced copy removed; identical hash update applied to the correct authority file |

The closed G1-D07 regression is the required return-and-repair of the EXP-W6 packaging path; it is not carried as an open P1. No open defect expands Runtime, Commercial Runtime, Payment, Entitlement, Consent, Provider, Registry authority, Schema, Migration or D1.
