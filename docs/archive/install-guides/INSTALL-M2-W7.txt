PHI OS — M2-W7 Lineage and Revision

1. Copy this package over the repository root and preserve folder structure.
2. Do not modify 0001 or 0002 migrations.
3. Run:

   npm run check:runtime-lineage-revision
   npm run check

4. Commit and deploy only after both commands pass.

M2-W7 freezes:
Edit = same Runtime + new Revision
New Journey = independent Runtime
Branch = child Runtime + parent relationship

No new Runtime Stage or SQL migration is introduced.
