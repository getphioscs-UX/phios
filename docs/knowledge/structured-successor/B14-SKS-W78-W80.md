# W78–W80：快照、来源归档与待关闭的冻结条件

基线：`ee07ca18d71a4493ae5aad2920fc97a96cc7eb0c`。

## 正文不是没给，登记版本也不是同名文件的任意新版本

| 册 | repo 已登记 PDF 页数 | 本轮桌面 PDF 页数 | 结果 |
|---|---:|---:|---|
| I | 402 | 406 | 新二进制另行归档 |
| II | 386 | 394 | 新二进制另行归档 |
| III | 397 | 397 | 页数相同，字节摘要不同 |
| IV | 433 | 428 | 新二进制另行归档 |

来源二进制、章节正文、提炼出的 meaning、真人认可及官网上线是不同记录。旧版有权威登记，并不意味着同名桌面文件会自动覆盖旧版。四册逐页正文已加入 `functions/_source-material/books/`，并附 PDF SHA256、正文 SHA256 和空文本页清单。文本提取不验证原 PDF 的图像与视觉版式。原 PDF 保留桌面，未复制大文件到 Git。

Book I/II 的已登记 reviewed corpus 和 Book III 的已登记章节文本也加入 repo，40 条提案的核验不再依赖桌面或相邻目录。重新核对：40/40 来源身份、88/88 引文通过。

## 40 条解释核对

用户澄清：“仅指页面和验收方法能理解；请你核对正文并整理差异。”因此没有记录为逐条 meaning 审批。

- 30 条核心解释与已核验引文未发现直接冲突。
- 8 条已补充展开细节的正文句子、章节 ID 和原文 SHA256。
- 2 条提出措辞修订：责任负荷不宜只限定于“责任失去边界后”；人工载体的正确、合法、安全应表达为共同条件，避免“或”造成任选其一的误读。

具体原文、建议与证据见本地 `functions/_source-material/meaning-proposals/MEANING-40-SOURCE-COMPARISON.html`；metadata 见 `meaning-extraction/meaning-semantic-differences-v1.json`。这是 AI 初审，并非对所有 scopeAndLimits 完成独立逐句审定。原提案未覆盖，实时 registry 的 40 个空值未伪装成已批准定义。

Book IV 的阈值在正文中确实存在：新 PDF 第 37 页讨论“扩展阈值”，第 55 页讨论“尺度转换”。已生成 `book4-threshold-source-candidates-v1.json`。原文把阈值描述为结构性临界区域，并非一个统一数字。候选页码属于新 PDF，不能直接冒充旧版章节绑定；文章摘要也不能代替这一步。

## W78 / W79 / W80 交付

- W78：`content/knowledge/structured/freeze/b14-sks-r1-registry-freeze-v1.json`，绑定 schema、类型、各册 registry、跨册、index、authority contract 的当前摘要。
- W79：`b14-sks-r1-runtime-freeze-v1.json`，绑定现有页面、Ask、article/context resolver 入口及静态相对依赖图；不创建新的运行系统。动态构造导入和远端服务不在该静态图的证明范围内。
- W80：`b14-sks-r1-production-freeze-v1.json`，记录基线、全部摘要、检查器版本、客户反馈、生产 URL、部分回滚包及完整站点回滚基线。状态为 `PREPARED_NOT_PRODUCTION_FROZEN`。

## 验证与下一步

`npm run check:b14-sks:freeze` 验证快照和现有 W64–W77 相关合同，包含拒绝虚假冻结的负例。它通过并不代表上线冻结。

`npm run check:b14-sks:production-freeze` 是严格激活检查：当前应失败，直到完成语义审定与版本化导入、补齐 Pressure 与 Book IV 提取、收到逐任务客户验收证据、验证实际部署及回滚。不要删除 blocker 或把 false 改成 true 来让检查变绿。

构建检查复用 `node scripts/build-cloudflare-pages.mjs --check-only`，不部署。不声称本轮已执行全部 `npm run check`、线上全站走查或真人验收。

回滚时先在独立 checkout 检出上述 Git 基线，用正常依赖安装和 Pages 构建流程验证，再按正式部署流程恢复。ZIP 是受影响文件的辅助快照，不足以单独部署完整站点。尚未执行线上回滚演练。
