# M1–M4 内容候选与统一审核包

本批完成审核准备，人工决定统一留到 M8。没有正式导入 meaning、替换来源版本或激活 W80 生产冻结。

## 已准备内容

| 阶段 | 交付 |
|---|---|
| M1 | 四册 678 条章节记录的候选版本映射；71 个现有对象的来源章节均定位到桌面正文。保留旧来源及文本差异窗口。 |
| M2 | 40 条 meaning、84 条原提案主张、最终候选逐句上下文和 120 条边界；保留原文、修订理由及额外证据。 |
| M3 | 71 个对象的 237 个字段位置；其中 186 个有候选内容，51 个明确记录提取未确立及理由。新增 20 条来自正文的 Book IV 独立定义候选。 |
| M4 | 2 条阅读关系、5 组重复/冲突线索及 4 项链路、图示、Atlas 审核记录；保留独立节点，不把阅读顺序当因果关系。 |

统一入口：仓库 `functions/_source-material/m1-m4-review/M1-M4-UNIFIED-REVIEW.html`。共 128 项决定，支持阶段/册筛选、原文展开、意见填写和导出绑定来源摘要的 JSON 草稿。M8 前保持“暂不审核”即可。

## 尚未确立的内容

- Book I 通用 Pressure、Threshold 的独立定义仍未确立。已提供相关正文候选，但不能把物理压力或特定领域阈值直接提升为通用定义。
- 51 个字段未通过本次提取确立。这不代表原书没有相关内容；逐项对象、字段和原因列在 `m1-m4-review-status-v1.json`，供继续定向补证。
- Book IV 旧版完整正文不在本轮档案内；新版本正文已归档，不能仅凭标题对齐宣称新旧语义一致。
- 章节重名和前置页的未定位项保留在状态清单中；不影响上述 71 个对象的来源定位。
- 规则选出的字段片段、词面相似的上下文与 AI 改写均待 M8 判断适用性及逻辑支持。引文真实存在不等于定义或字段分类已获批准。

## 验证与重建

直接运行下列命令，不改变旧冻结绑定的 package.json：

```powershell
node scripts/build-sks-m1-m4-review.mjs
node scripts/check-sks-m1-m4-review.mjs
node scripts/check-sks-m1-m4-review-browser.mjs
node scripts/check-b14-sks-freeze.mjs
```

浏览器检查使用 Playwright；环境未提供模块时通过 `PHIOS_PLAYWRIGHT_MODULE` 指向已安装的入口。

本批验证通过：确定性重建、380 条桌面正文精确引文、原提案与最终候选上下文、草稿版本/完整性/决定守卫；390px 与 1440px 页面筛选、展开、导出，无横向溢出或脚本错误；现有 W78/W79/W80 准备状态检查通过。未声称运行完整 npm run check 或线上验收。

审核包及正文留在私有 source-material 范围。M5–M7 可继续准备客户流程、隔离预览与全站走查；M8 集中审核，M9 才按批准范围正式导入。来源或候选发生变化时，重建包并同步状态清单，旧草稿必须重新核对来源摘要。
