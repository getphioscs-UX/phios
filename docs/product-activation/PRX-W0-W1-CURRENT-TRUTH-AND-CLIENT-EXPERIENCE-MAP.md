# PRX-W0/W1 | Current Capability Truth + 42/186 Client Experience Map

Baseline: `6f2b4c6791c552c065ac699b12e08ebe4aa1e7fe`

## Purpose

This successor keeps historical PRX-W0 intact and adds the Master Execution Plan v3 Stage 0/1 truth model. It separates runtime truth, product truth and live truth, then maps the frozen 42 runtime families / 186 capabilities to seven client experiences or `NONE_BY_DESIGN`.

## Canonical current states

`REGISTERED`, `IMPLEMENTED`, `VALIDATED`, `PROJECTED`, `CLIENT_CONSUMED`, `PRODUCTION_ACCEPTED`, `DEPLOYED`, `LIVE_VERIFIED`, `PILOT_VERIFIED`, `BLOCKED`, `DEFERRED`.

No repository checker, production acceptance file or `AVAILABLE` label may imply `LIVE_VERIFIED`.

## Frozen inventory result

- 42/42 runtime families mapped.
- 186/186 capabilities mapped.
- Exactly one primary client experience is assigned to each capability; secondary experiences may be recorded.
- Internal governance/infrastructure uses `NONE_BY_DESIGN` rather than becoming a public menu item.
- `lensRole` is recorded separately from client experience.

## Lens-role classification for Stage 1

- `AST` → `FUNCTIONAL_DYNAMICS`
- `BZR` → `TEMPORAL_STRUCTURE`
- `HDR` → `OPERATING_STRATEGY`
- `NUM` → `CYCLE_RHYTHM`

This is a Stage-1 classification only. Formal method authority remains for ML-W0–W3 and is not silently frozen here.

## Post-inventory truth

The frozen MRM v1.3 inventory still contains exactly 42 families / 186 capabilities. Current main now also contains runtime work that is newer than that inventory:

- `ZWR` — bounded production acceptance exists; `liveDeploymentVerified=false`; intended role `REALITY_DOMAIN_TOPOLOGY`.
- `ICH` — machine + interpretation validation exists; production activation remains closed.
- `TAR` — machine + interpretation validation exists; production activation remains closed.
- `HRX` — Health Reality foundation validated; public surface and external clinical authority are not activated.

They are tracked but are **not** silently inserted into the 42/186 count. A future MRM inventory successor must explicitly admit them.

## Client experience domains

1. Understand
2. Know Myself
3. Understand My Reality
4. Understand Money
5. Navigate
6. Learn
7. Work with Professional

`NONE_BY_DESIGN` is reserved for internal infrastructure/governance.

## Next gate

Stage 2 is `ML-W0–W3 Multi-Lens Authority Reconciliation`. PRX-W1 does not create a public lens router and does not activate any blocked method.
