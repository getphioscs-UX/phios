# PPR-C1 W0–W3

Baseline: `3f6825a9b57dc9e62e34fb69bc55d2aac2c39768`.

This tranche freezes `/perspectives/personal/` as the only Personal customer authority, removes the duplicate single-method-reading JS authority, introduces the method-native customer product envelope, and wires BaZi Full Production into `/api/customer-personal-reality` without recalculating the natal chart.

If no explicit BaZi target date/time/timezone/offset is supplied, the adapter emits a governed no-target boundary projection: natal W9–W15 remain readable while the Timing section remains unavailable/partial. If an explicit target context is supplied, the existing BZR Temporal runtime consumes the **same** natal execution retained from the canonical BaZi calculation.
