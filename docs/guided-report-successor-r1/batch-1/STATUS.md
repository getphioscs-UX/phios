# BAZI-DYNAMIC-R1-BATCH-01

Baseline: `7aead829a4af9358fda73719bfbd54f5badca872`.

Scope: P06–P10 only. Implementation and review artifacts are available; human dynamic visual acceptance remains pending. P11–P26 have not been implemented by this batch. This review does not activate a paid production route or change the approval of the 120 static images.

## Review

Open [the review page](http://127.0.0.1:8788/docs/guided-report-successor-r1/batch-1/review.html). Select 中文, English or 双语. “参考图并排对照” compares each selected page with its frozen master. The A4 PDF link follows the selected language.

- [Chinese A4](bazi-p06-p10-zh-Hans.pdf)
- [English A4](bazi-p06-p10-en.pdf)
- [Bilingual A4](bazi-p06-p10-bilingual.pdf)
- [Browser evidence](browser-evidence.json): 1440px and 390px × three languages × five pages; layout density, horizontal overflow and print overlap checks.
- [PDF evidence](pdf-evidence.json): each file is A4, five pages, retaining logical numbering 06–10 / 26.
- `screenshots/`: 30 page captures and 15 reference comparisons.
- `pdf-review/`: all 15 rasterized PDF pages and three contact sheets.

## Runtime and ownership

`projectBaziVisualReport` now accepts an explicit review-only Batch 1 option. Its subordinate projection uses the existing admitted BaZi method-native reading and `createVisualPage`; the existing `renderVisualReportPages` dispatches to the M03–M05 compositions. There is no new calculation or report authority. Web and PDF consume the same Page IR.

The review case is a **governed deterministic synthetic benchmark executed through the existing runtime**, not a live customer's birth record. The benchmark's supplied solar-term fixtures remain identified in `scripts/smr-benchmark-support.mjs`. Runtime output is regenerated by `scripts/build-bazi-batch-1-review.mjs`; no values are taken from M03–M05 pictures.

Four pillars, hidden stems, Day Master, month command, root records and the element inventory carry source references. Missing data or inconsistent totals fail closed. Five-element arrows come from the existing `elementRelation` owner. Their presence does not assert personal intensity, auspiciousness or a strong/weak verdict. Zero-count elements remain visible. Counts include unweighted hidden stems and are explicitly not strength percentages.

## Reference comparison

| Page | Master | Preserved composition and bounded adaptation |
| --- | --- | --- |
| P06 | M03 | Four pillar columns, Day Master/season side panels, ring with ranked count bars, three insight/condition/observation cards. |
| P07 | M04 | Central Day Master, thin orbit and contextual nodes, seasonal panel. Roots and an open verdict replace unsupported example support/strength claims. Landscape reference reflows to portrait A4. |
| P08 | M04 | Structural panels preserve stem, branch and hidden-stem positions; Day Master and month command remain separate contextual anchors. |
| P09 | M05 | Shared element palette, ring and ranked bars. A source breakdown replaces the reference's unsupported “dominant/moderate/weak” categories. |
| P10 | M05 | Shared distribution panels and line treatment. Generating/controlling cycles are explicitly distinguished by solid/dashed arrows and readable relationship lists. |

All five pages share the frozen navy/ivory/gold tokens, serif/sans hierarchy, border/panel treatment, header and footer. Bilingual titles put Chinese above English. Mobile pillars become a 2 × 2 grid; figures and context stack. Print keeps each logical page on one A4 sheet.

**Visual difference requiring review:** these are live vector/data compositions on ivory panels, without the reference pictures' painted mountain textures or ornamental medallions. The reference image pixels and sample interpretations are not embedded in report pages. Machine checks do not certify equivalence to the static artwork or human visual acceptance. Bilingual prose retains the existing Page IR's conservative review flag; actual browser geometry is separately checked rather than weakening that flag.

## Validation and corrections

`scripts/check-bazi-batch-1.mjs` covers page/master mapping, cross-locale evidence identity, invalid/missing sources, changed source counts, frozen-token styling and review-only access. It is prepended to `npm run check`, preserving the required PTRC tail.

The full check exposed a pre-existing Batch 0 audit mismatch: `report-visual-reference-freeze-r1.json` was counted as an extra historical registry. The audit now explicitly admits and validates that named presentation successor; the historical 114-file baseline and 51-entry index are unchanged.

Visual QA corrected wrapped mobile page numbers, hidden-stem field binding, a sixth bilingual PDF page and print chart/card overlap. Print overlap now has an executable browser assertion. All 15 final PDF pages were rasterized and visually inspected.

Full `npm run check`: **PASS, exit 0**, including precheck, check and postcheck. See `validation.json` and `npm-check.log`. The Batch 1 targeted checker and browser checks were rerun after the final source-validation and print-scope changes.
