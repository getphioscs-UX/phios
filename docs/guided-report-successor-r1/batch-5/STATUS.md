# BAZI-DYNAMIC-R1-BATCH-05

Baseline: `d199fe6d87f61770014150015c0f8e53a9b6a997`. Scope: P23–P26, M08, following the simultaneously authorized Batch 4. Stop after P26; no Batch 6 whole-book acceptance is asserted.

[Open Batch 5](http://127.0.0.1:8788/docs/guided-report-successor-r1/batch-5/review.html). Chinese, English and bilingual share the same bound Page IR; Web and PDF use the same renderer. Use the page selector for M08 reference comparisons.

## Pages and source mapping

| Page | Composition | Data and meaning boundary |
| --- | --- | --- |
| P23 Observable Signals | WHEN → OBSERVE → COUNTER-SIGNAL connected composition | The admitted CAREER repeat/context and counterexample prompts are shown as questions. WHEN is the work-setting context of the source prompt. These are prompts for observations, not independently recorded signals. |
| P24 Opportunities & Risks | OPPORTUNITY → CONDITION → RISK composition | The PRESSURE counterexample prompt explores changing support; the CAPABILITY condition preserves the approved carrying-context text; the CAREER counterexample explores unsustainable cost. These questions do not assign an opportunity, personal risk rating or predicted outcome. |
| P25 Reality Navigation | NOTICE → COMPARE → TEST → REVIEW route | Implements the user's requested procedural sequence: record a concrete context, seek a contrasting example, choose a small reversible test, then review observations. It is guidance for observation, not a new method conclusion or automatic action. |
| P26 Evidence, Boundary & Closing | Chart calculation → method reading → interpretation → report lineage; source-count panel | Uses `evidence.sourceNatalProjectionId`, `summary.reportDigest`, `customerSafeGraph.lineage` and `customerSafeGraph.summary`. Method evidence counts remain separate from Current Reality. Full bound references remain in source-lineage disclosure. |

Arrows indicate presentation/observation order, not measured causal force or a prediction. The pages are connected compositions rather than checkbox lists. Source questions and conditions are retained in both languages; prose inside nodes is included in the existing Page IR text-budget calculation. Bilingual P23–P25 retain the conservative REVIEW_REQUIRED text-density status while browser geometry is checked separately. Machine success does not override that review status.

The fixture is the same governed deterministic synthetic benchmark used by earlier batches, not a live customer record. No current evidence or counter-evidence is invented. The M08 sample personal claims, mountain pixels and decorative icons are not copied into the dynamic report.

## Artifacts and verification

- Three A4 PDFs, each with logical P23–P26 / 26.
- 24 default page screenshots at 1440px and 390px, and 12 M08 reference comparisons.
- `browser-evidence.json`: responsive/print checks and 84 prior Batch 1–3 screenshot comparisons.
- `pdf-evidence.json` and `pdf-review/`: A4/page-number checks and all twelve rendered PDF pages.
- `validation.json`: final execution and review state.
- Full npm log: `../batch-4/npm-check.log`.

`check-bazi-batch-4-5.mjs` checks stage identity/order, prompt source presence, counter-evidence boundary, quantitative lineage propagation, all locales, review-only admission, CSS-token restrictions, and exact prior HTML/CSS/token hashes. It is prepended to the existing `npm run check`; the required PTRC tail remains intact.

The shared renderer and frozen report tokens retain ownership. Human dynamic visual acceptance remains PENDING; publication and checkout stay disabled. The user's earlier static-image approval is unchanged.

Full `npm run check`: **PASS, exit 0**, including precheck, check and postcheck. Final presentation changes passed the targeted check, browser checks and A4 verification. All default PDF pages were visually inspected. Prior screenshot regression: 83/84 historical captures are byte-identical; the one known raster variation is exactly reproduced by the unchanged prior stylesheet. HTML/CSS/tokens remain exact. Dynamic human acceptance remains pending.
