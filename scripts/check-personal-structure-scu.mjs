import assert from 'node:assert/strict';
import fs from 'node:fs';
import {SCU_ENDPOINTS,resolveActivatedChannels} from '../functions/method-runtime/personal-structure/scu-resolver.js';
const reg=JSON.parse(fs.readFileSync('content/method/personal-structure/scu-structural-endpoint-registry-v1.json','utf8'));const cmr=JSON.parse(fs.readFileSync('content/professional/canonical-meaning-runtime/identity/meaning-identity-registry-v1.2.json','utf8'));
assert.equal(SCU_ENDPOINTS.length,36);assert.equal(reg.entries.length,36);assert.equal(new Set(SCU_ENDPOINTS.map(x=>x.scuCode)).size,36);assert.equal(new Set(SCU_ENDPOINTS.map(x=>[x.gateA,x.gateB].sort((a,b)=>a-b).join('-'))).size,36);const centers=new Set(reg.centerCodes);for(const e of SCU_ENDPOINTS){assert.ok(centers.has(e.centerA));assert.ok(centers.has(e.centerB));}
const cmrCodes=new Set((cmr.records||[]).map(x=>x.meaningCode||x.code||x.canonicalCode).filter(x=>/^SCU-\d{2}$/.test(x)));if(cmrCodes.size!==36){const text=JSON.stringify(cmr);for(const e of SCU_ENDPOINTS)assert.ok(text.includes(e.scuCode),`CMR missing ${e.scuCode}`);}else for(const e of SCU_ENDPOINTS)assert.ok(cmrCodes.has(e.scuCode));
const acts=[{activationId:'A1',layer:'PERSONALITY',bodyCode:'X',gate:64,status:'CALCULATED'},{activationId:'A2',layer:'DESIGN',bodyCode:'Y',gate:47,status:'CALCULATED'},{activationId:'A3',layer:'DESIGN',bodyCode:'Z',gate:47,status:'CALCULATED'}];const r=resolveActivatedChannels(acts);assert.equal(r.activatedChannels.length,1);assert.equal(r.activatedChannels[0].scuCode,'SCU-01');assert.equal(new Set(r.activatedChannels.map(x=>x.scuCode)).size,r.activatedChannels.length);console.log('✓ Personal Structure SCU passed: 36/36 structural endpoints reconcile 1:1 with CMR identities; duplicate activations do not duplicate SCU identity.');
