import fs from 'node:fs';import assert from 'node:assert/strict';const r=JSON.parse(fs.readFileSync('content/customer-experience-rebuild/r12r4b/smr-r2/review/smr-r2-w19-human-review-results-v1.json','utf8'));assert.deepEqual(r.summary,{accepted:20,rejected:0,pending:0},'R2-W19 requires 20 accepted / 0 rejected / 0 pending');for(const x of r.results){assert.equal(x.decision,'HUMAN_ACCEPTED');for(const state of Object.values(x.criteria))assert.equal(state,'HUMAN_ACCEPTED')}assert.equal(r.productionCutoverAllowed,true);console.log('✓ R2-W19 product-integration human review passed 20/20.');
