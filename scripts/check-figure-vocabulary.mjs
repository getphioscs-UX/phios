import {assert,all} from './figure/figure-check-lib.mjs';const m=new Map();for(const {id,spec} of all())for(const n of spec.nodes){if(m.has(n.semanticKey))assert.equal(m.get(n.semanticKey),n.displayLabel,`${id} terminology drift for ${n.semanticKey}`);else m.set(n.semanticKey,n.displayLabel);}console.log(`✓ FIG vocabulary checker: ${m.size} reusable semantic keys have zero label drift across 57 figures.`);
