import assert from 'node:assert/strict'; import {readJson,BASELINE} from './lib/web-production/wpr-foundation-v1.mjs';
const a=readJson('content/web-production/contracts/wpr-authority-boundary-v1.json'); assert.equal(a.baselineCommit,BASELINE); for(const x of ['web_production_resolution','route_resolution','surface_projection','runtime_consumption_resolution','composition_execution','asset_delivery_resolution','client_hydration','web_render_delivery']) assert.ok(a.owns.includes(x)); for(const x of ['knowledge_creation','meaning_selection','reality_truth','readout_authority','method_production_eligibility','professional_judgment','academy_entitlement','asset_publication','consent_authority']) assert.ok(a.doesNotOwn.includes(x)); assert.equal(a.invariants.htmlIsAuthority,false); assert.equal(a.invariants.freezeCreatesProductionEligibility,false); assert.equal(a.invariants.dependencyNeverGrantsAuthority,true); console.log('✓ WPR-W1 Authority Boundary passed.');
