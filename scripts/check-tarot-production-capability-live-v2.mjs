import assert from 'node:assert/strict';import fs from 'node:fs';
import {LIVE_SHA_EVIDENCE,LIVE_CAPABILITY_EVIDENCE,readJson,git,getJson,PRODUCTION_URL,runLiveSmoke} from './lib/tarot/production-capability-promotion-v2.mjs';
assert.ok(fs.existsSync(LIVE_SHA_EVIDENCE),'Phase-L v2 live evidence missing. Run verify:tarot-production-sha.');assert.ok(fs.existsSync(LIVE_CAPABILITY_EVIDENCE),'Phase-M live evidence missing. Run promote:tarot-limited-production.');const head=git('rev-parse','HEAD');const l=readJson(LIVE_SHA_EVIDENCE),m=readJson(LIVE_CAPABILITY_EVIDENCE);assert.equal(l.alignment.verified,true);assert.equal(l.alignment.deployedCommit,head);assert.equal(m.status,'LIMITED_PRODUCTION_LIVE_VERIFIED');assert.equal(m.commit,head);assert.equal(m.authority.runAllowed,true);assert.equal(m.authority.productionCapabilityPromoted,true);assert.equal(m.authority.approvedCommitSha,head);assert.equal(m.runtimeDb.newMigrationCreated,false);const status=await getJson(new URL('/api/tarot-production-status',PRODUCTION_URL));assert.equal(status.status,200);assert.equal(status.payload?.production?.runAllowed,true);assert.equal(status.payload?.production?.approvedCommitSha,head);const smoke=await runLiveSmoke({sha:head});assert.equal(smoke.guestSave.error,'ACCOUNT_REQUIRED');console.log(`✓ TPA-MR live capability gate passed: Tarot LIMITED_PRODUCTION is active for exact SHA ${head}.`);console.log('  Guest hidden persistence remains false; decision authority remains USER.');
