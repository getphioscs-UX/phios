import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';import {spawnSync} from 'node:child_process';
const path='content/production/symbolic-method/freeze/iching-limited-production-implementation-freeze-v1.json';const freeze=JSON.parse(fs.readFileSync(path,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
assert.equal(freeze.baselineCommit,'fc2c3dd6ab4910581fd9c859dc303e8c89697ec0');assert.equal(freeze.status,'IMPLEMENTATION_FROZEN_DEPLOYMENT_CONFIGURATION_AND_LIVE_LIMITED_ACCEPTANCE_PENDING');
for(const item of freeze.artifacts){assert.ok(fs.existsSync(item.path),`ICHING_LIMITED_ARTIFACT_MISSING:${item.path}`);assert.equal(sha(item.path),item.sha256,`ICHING_LIMITED_ARTIFACT_DRIFT:${item.path}`);}
assert.equal(freeze.acceptance.humanBase,'24/24');assert.equal(freeze.acceptance.humanDepth,'448/448');assert.equal(freeze.acceptance.admittedDepth,'448/448');assert.equal(freeze.acceptance.bilingualRuntimeCases,'896/896');assert.equal(freeze.acceptance.guestRunAllowed,false);assert.equal(freeze.acceptance.retentionImplicitlyGranted,false);assert.equal(freeze.acceptance.fullProductionAllowed,false);assert.equal(freeze.deploymentBoundary.liveLimitedProductionClaimed,false);
const run=spawnSync(process.execPath,['scripts/check-iching-limited-production-static.mjs'],{cwd:process.cwd(),encoding:'utf8'});assert.equal(run.status,0,`${run.stdout}\n${run.stderr}`);process.stdout.write(run.stdout);
console.log(`✓ ICH-PROD-W28 Limited Production implementation freeze passed: ${freeze.artifacts.length}/${freeze.artifacts.length} additive successor artifacts match exact SHA256.`);console.log('  Deployment/configuration/live beta evidence remain separate; FULL_PRODUCTION remains false.');
