import assert from 'node:assert/strict';
import fs from 'node:fs';
import crypto from 'node:crypto';
import {execFileSync} from 'node:child_process';
const works=['W71','W72','W73'];const sha=s=>crypto.createHash('sha256').update(s).digest('hex');
execFileSync(process.execPath,['scripts/build-ppr-r2-w71-w73-human-review.mjs'],{stdio:'inherit'});
for(const work of works){const k=work.toLowerCase(),casesText=fs.readFileSync(`content/personal-reading/human-acceptance/review/${k}-human-review-cases-v1.json`,'utf8'),cases=JSON.parse(casesText),results=JSON.parse(fs.readFileSync(`content/personal-reading/human-acceptance/review/${k}-human-review-results-v1.json`,'utf8')),html=fs.readFileSync(`content/personal-reading/human-acceptance/review/${k}-human-review.html`,'utf8');assert.equal(cases.requiredCases,24);assert.equal(cases.cases.length,24);assert.equal(cases.status,'READY_FOR_NEW_HUMAN_REVIEW');assert.equal(results.requiredCases,24);assert.equal(results.results.length,24);assert.equal(results.caseBundleDigest,sha(casesText));assert.equal(results.accepted+results.revised+results.rejected+results.pending,24);assert.ok(['PENDING_24_OF_24','REVIEW_IN_PROGRESS','REVIEW_COMPLETE_WITH_CHANGES','HUMAN_ACCEPTED_24_OF_24'].includes(results.status));if(results.status==='HUMAN_ACCEPTED_24_OF_24'){assert.deepEqual({accepted:results.accepted,revised:results.revised,rejected:results.rejected,pending:results.pending},{accepted:24,revised:0,rejected:0,pending:0});}assert.ok(html.includes('Machine acceptance is already complete'));assert.ok(html.includes('Export '+work+' results JSON'));}
const w72=JSON.parse(fs.readFileSync('content/personal-reading/human-acceptance/review/w72-human-review-cases-v1.json','utf8'));assert.equal(w72.cases.filter(x=>x.group==='PERSONAL').length,8);assert.equal(w72.cases.filter(x=>x.group==='PROFILE_ASSESSMENT').length,8);assert.equal(w72.cases.filter(x=>x.group==='RELATIONSHIP').length,8);assert.equal(w72.cases.every(x=>x.verificationSummary?.passed===true),true);assert.equal(w72.cases.every(x=>x.reopen?.regenerationAllowed===false),true);
console.log('✓ W71-W73 Human review readiness passed: 3 × 24 review cases are current; Human decisions remain authoritative and are not inferred from machine PASS.');
