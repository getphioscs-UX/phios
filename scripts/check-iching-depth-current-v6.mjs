import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const prev=j('content/interpretation/iching/reconciliation/iching-depth-current-successor-v5.json'),cur=j('content/interpretation/iching/reconciliation/iching-depth-current-successor-v6.json');assert.equal(cur.successorOf,'content/interpretation/iching/reconciliation/iching-depth-current-successor-v5.json');assert.equal(cur.historicalPredecessorMutated,false);assert.equal(prev.admittedCoverage.humanApproved,'448/448');assert.equal(cur.admittedCoverage.humanApproved,'448/448');assert.equal(cur.humanAcceptance.accepted,'448/448');assert.equal(cur.runtimeAcceptance.bilingualProductCases,'896/896');assert.equal(cur.reconciliationReason.humanReviewDriftObserved,false);assert.equal(cur.reconciliationReason.depthRuntimeDriftObserved,false);assert.equal(cur.reconciliationReason.historicalW31LiveEvidenceRewritten,false);assert.equal(cur.checkerGovernance.successorShaObservationRebindSeparateFromDepthFreeze,true);assert.equal(cur.productionBoundary.w32R1SuccessorShaObservationRebindReady,true);assert.equal(cur.productionBoundary.fullProductionAllowed,false);
for(const item of cur.historicalPredecessorWitnesses)assert.equal(sha(item.path),item.sha256,`historical predecessor drift: ${item.path}`);for(const item of cur.domainArtifacts)assert.equal(sha(item.path),item.sha256,`ICHI-DEPTH v6 preserved domain artifact drift: ${item.path}`);const pkg=j('package.json');for(const [k,v] of Object.entries(cur.orchestrationBindings.requiredScripts||{}))assert.equal(pkg.scripts[k],v,`package orchestration drift: ${k}`);
console.log('✓ ICHI-DEPTH current v6 successor passed: 448/448 + 896/896 remain unchanged; historical v5 bytes are preserved while W32R1 successor-SHA observation authority stays external to the depth freeze.');
