import assert from 'node:assert/strict';
import fs from 'node:fs';
import crypto from 'node:crypto';
import {composeMir4ClientResultSurface,MIR4_CLIENT_TABS} from '../assets/js/method-client-delivery/mir4-client-result-surface.js';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8')); const read=p=>fs.readFileSync(p,'utf8'); const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const registry=j('content/professional/method-client-delivery/registries/mcd-7-mir-4-client-result-surface-registry-v2.json');
const readiness=j('content/method/personal-structure/personal-runtime-capability-readiness-v1.json');
const fixture=j('content/professional/method-client-delivery/fixtures/personal-structure-projection.renderer-fixture.v1.json');
const freeze=j('content/professional/method-client-delivery/freeze/mcd-7-personal-runtime-result-surface-freeze-v1.json');
assert.equal(registry.status,'SUCCESSOR_COMPOSITION_READY_ROUTE_PROMOTION_DEFERRED'); assert.equal(registry.routePromotion,'DEFERRED_TO_MIR11_UNIFIED_ACCEPTANCE');
assert.deepEqual(registry.tabs.map(x=>x.tabCode),MIR4_CLIENT_TABS); assert.equal(registry.tabs.find(x=>x.tabCode==='reading').interpretationAllowed,false); assert.equal(registry.tabs.find(x=>x.tabCode==='reading').calculationAllowed,false); assert.equal(registry.tabs.find(x=>x.tabCode==='reading').meaningAllowed,false);
assert.equal(registry.currentPersonalStructureReadiness.rendererMayBypass,false); assert.equal(readiness.currentFullCoreProductionReady,false);
for(const item of freeze.frozenOutputs) assert.equal(sha(item.path),item.sha256,`MCD-7 frozen drift: ${item.path}`);
let surface=composeMir4ClientResultSurface({personalStructureProjection:fixture.bundle,personalStructureReadiness:readiness,locale:'en'}); assert.equal(surface.panels['personal-structure'].status,'UNAVAILABLE'); assert.equal(surface.panels['personal-structure'].code,'PERSONAL_STRUCTURE_UPSTREAM_CAPABILITY_NOT_READY'); assert.equal(surface.panels.comparison.status,'UNAVAILABLE'); assert.equal(surface.panels.reading.status,'HANDOFF_ONLY'); assert.equal(surface.panels.reading.interpretationAllowed,false); assert.equal(surface.readingIsInterpretationRuntime,false); assert.equal(surface.productionAuthorityCreated,false); assert.equal(surface.calculationAuthorityCreated,false); assert.equal(surface.projectionAuthorityCreated,false);
surface=composeMir4ClientResultSurface({personalStructureProjection:fixture.bundle,personalStructureReadiness:readiness,locale:'en',validationMode:true}); assert.equal(surface.panels['personal-structure'].status,'RENDERED'); assert.equal(surface.panels.reading.status,'HANDOFF_ONLY');
const composition={schemaVersion:'PHI-OS-MCD-6-CROSS-METHOD-COMPOSITION-v1.0.0',compositionId:'CMC-MIR4-SURFACE-01',sourceProjectionRefs:[{projectionId:'CMP-ABCDEF0123456789ABCDEF01',publicMethodCode:'ASTROLOGY_PROJECTION'}],signals:[{signalId:'S1',classification:'CONVERGENCE',label:'Upstream-classified signal',projectionRefs:['CMP-ABCDEF0123456789ABCDEF01'],evidenceRefs:['E1']}],unknown:[],evidence:[{ref:'E1',label:'Evidence'}],lineage:[{ref:'L1',label:'Lineage'}]};
surface=composeMir4ClientResultSurface({crossMethodComposition:composition,personalStructureReadiness:readiness}); assert.equal(surface.panels.comparison.status,'RENDERED'); assert.match(surface.panels.comparison.html,/UPSTREAM|upstream/i); assert.equal(surface.panels.reading.status,'HANDOFF_ONLY');
const src=read('assets/js/method-client-delivery/mir4-client-result-surface.js'); assert.doesNotMatch(src,/core-method-runtime|functions\/method-runtime|canonical-meaning-runtime|reading-runtime|openai|workersAI|fetch\s*\(/i); assert.doesNotMatch(src,/localStorage|sessionStorage|indexedDB/);
console.log('✓ MIR-4 client surface successor passed: Method Result Surface remains separate from Reading; comparison and Personal Structure are input/readiness-gated, never renderer-created authority.');
