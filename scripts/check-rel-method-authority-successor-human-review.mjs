import assert from 'node:assert/strict';import fs from 'node:fs';const p=JSON.parse(fs.readFileSync('content/personal-reading/relationship/review/rel-method-authority-successor-human-review-cases-v1.json','utf8'));const r=JSON.parse(fs.readFileSync('content/personal-reading/relationship/review/rel-method-authority-successor-human-review-results-v1.json','utf8'));assert.equal(p.cases.length,72);for(const m of ['BZR','ZWR','ECR'])assert.equal(p.cases.filter(x=>x.methodId===m).length,24);assert.equal(r.pending,72);assert.equal(p.cases.every(x=>x.claims.every(c=>!/compatibility percentage|soulmate|will last|secretly|hidden feeling/i.test(c.summary))),true);console.log('✓ REL method authority successor Human review readiness passed: 72 cases (24 BZR + 24 ZWR + 24 ECR), all pending Human decision.');