import assert from 'node:assert/strict'; import fs from 'node:fs'; const r=p=>JSON.parse(fs.readFileSync(p,'utf8')); const x=r('content/interpretation/source/book-1-interpretation-source-inventory-v1.json'); assert.equal(x.status,'HUMAN_ACCEPTED_MANUSCRIPT_SOURCE_RECONCILED'); assert.equal(x.recordCount,65); assert.equal(x.humanAcceptedReconciliation.uncoveredCanonicalNodes,0); assert.equal(x.authorityBoundary.figureOnlyFoundation,false); for(const c of x.requiredConcepts) assert.ok(x.conceptCoverage[c],`missing concept ${c}`); for(const e of x.records){assert.equal(e.humanAcceptance,'ACCEPTED');assert.ok(e.sourceSectionRefs.length>0);assert.equal(e.status,'SOURCE_RECONCILED_HUMAN_ACCEPTED');} console.log('✓ IR-W3 Book I manuscript interpretation source reconciliation passed (65/65 human-accepted canonical-node bindings).');
