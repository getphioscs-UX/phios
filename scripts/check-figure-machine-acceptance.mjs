import {assert,fs,P,read,ids} from './figure/figure-check-lib.mjs';assert.ok(fs.existsSync(P.acceptance),'acceptance registry missing; run figure:production:all');const r=read(P.acceptance);assert.equal(r.expectedCount,57);assert.equal(r.acceptedCount,57);assert.equal(r.allAccepted,true);assert.deepEqual(r.entries.map(x=>x.figureId),ids());for(const e of r.entries){assert.equal(e.status,'MACHINE_ACCEPTED');assert.equal(e.stale,false);assert.equal(e.publicationAuthorityOwnedHere,false);}console.log('✓ FIG machine acceptance registry: 57/57 MACHINE_ACCEPTED; publication authority remains external.');
