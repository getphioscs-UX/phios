import assert from 'node:assert/strict';import fs from 'node:fs';import {deriveCapabilityState,rankDrivers,buildCoordinate,selectMotionCandidate,buildRealityState} from '../functions/interpretation-runtime/domain-derivers-v1.js';const r=p=>JSON.parse(fs.readFileSync(p,'utf8'));assert.equal(r('content/interpretation/runtime/coordinate-contract-v1.json').dimensions.length,5);const cap=deriveCapabilityState({exists:true,available:true,relevant:true,activated:false,effective:false});assert.equal(cap.Activated,false);const rank=rankDrivers([{driverId:'D1',scores:{currentState:1,grammar:1}},{driverId:'D2',scores:{currentState:3}}]);assert.equal(rank.ranked[0].driverId,'D2');const coord=buildCoordinate({Temporal:{unknown:false},Conditional:{unknown:false},Directional:{unknown:false},Carrier:{unknown:false},Relational:{unknown:true}});assert.equal(coord.dimensions.Relational.unknown,true);const motion=selectMotionCandidate([{motionCode:'MOTION-08',status:'HYPOTHESIS_SOURCE_REVIEW_REQUIRED',priority:99},{motionCode:'MOTION-01',status:'SOURCE_GROUNDED',priority:1}],{});assert.equal(motion.selected.motionCode,'MOTION-01');const state=buildRealityState({coordinate:coord,question:{},capability:cap,driverPriority:rank,motion,activation:{}});assert.equal(state.llmUsed,false);assert.equal(r('content/interpretation/carrier/human-carrier-interpretation-contract-v1.json').observedCarrierConditionEqualsMedicalDiagnosis,false);console.log('✓ IR-W35–W47 coordinate/motion/state/carrier passed: five-dimensional coordinate, contextual driver priority, unresolved motion blocked, Carrier ≠ diagnosis.');
