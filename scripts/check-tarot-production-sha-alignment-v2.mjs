import assert from 'node:assert/strict';import fs from 'node:fs';import {execFileSync} from 'node:child_process';
import {CONTRACT_V2_PATH,MANIFEST_V2_PATH,LIVE_EVIDENCE_V2_PATH,readJson,buildSourceManifestV2,isFullGitSha} from './lib/tarot/production-sha-alignment-v2.mjs';
const root=process.cwd();const c=readJson(root,CONTRACT_V2_PATH),m=readJson(root,MANIFEST_V2_PATH);assert.deepEqual(buildSourceManifestV2(root,c.baselineCommit),m);assert.ok(fs.existsSync(LIVE_EVIDENCE_V2_PATH),'TPA-L v2 live evidence pending. Run npm run verify:tarot-production-sha after the exact Phase-M commit deploys.');const e=readJson(root,LIVE_EVIDENCE_V2_PATH);assert.equal(e.status,'VERIFIED_CURRENT_MAIN_PHASE_M_RELEASE_EXACT_SHA_AND_RUNTIME_ASSETS_ALIGNED');assert.equal(e.alignment.verified,true);assert.ok(isFullGitSha(e.alignment.deployedCommit));const head=execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();assert.equal(e.alignment.deployedCommit,head,'Phase-L v2 evidence is not for current HEAD');assert.equal(e.repository.originMain,head);for(const [k,v] of Object.entries(e.checks))assert.equal(v,true,`TPA-L v2 live check failed: ${k}`);assert.equal(e.runtimeWitness.deployedSha,head);assert.equal(e.remoteAssets.every(x=>x.match),true);assert.equal(e.productionBoundary.productionCapabilityPromoted,false);assert.equal(e.productionBoundary.publicRunAllowed,false);console.log(`✓ TPA-L v2 exact production SHA alignment passed for ${head}.`);console.log('  D1 promotion remains a separate Phase-M action.');
