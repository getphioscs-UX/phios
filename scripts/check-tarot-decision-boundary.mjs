import assert from 'node:assert/strict';import fs from 'node:fs';
import {adaptTarotProjections} from '../functions/interpretation-runtime/adapters/tarot-interpretation-adapter-v1.js';
import {composeTarotRealityComparison,assertTarotGeneratedBoundaries} from '../functions/interpretation-runtime/tarot-reality-comparison-v1.js';
import {tariAuthorities,projectOne} from './lib/tarot/tari-fixtures-v1.mjs';
const c=JSON.parse(fs.readFileSync('content/interpretation/tarot/contracts/tarot-decision-boundary-v1.json','utf8'));assert.equal(c.work,'TARI-W6');assert.deepEqual(c.allowedOutputClasses,['REFLECTION','QUESTION','PERSPECTIVE']);assert.equal(c.rules.tarotMayDirectUserDecision,false);assert.equal(c.rules.userDecisionAuthorityRequired,true);
for(const phrase of ['Quit your job','You should invest now','Invest your money now','Leave your partner','End your relationship','Stop treatment','Stop your medication']) assert.throws(()=>assertTarotGeneratedBoundaries({systemClaim:phrase}),/TAROT_DECISION_BOUNDARY/);
const b=adaptTarotProjections(await projectOne('RWS-MAJOR-00','TARI-DEC'),{cardRegistry:tariAuthorities.cardRegistry,sourceRegistry:tariAuthorities.sourceRegistry,perspectiveRegistry:tariAuthorities.perspectiveRegistry,symbolDimensionRegistry:tariAuthorities.symbolDimensionRegistry,corpus:tariAuthorities.corpus});const out=composeTarotRealityComparison(b);assert.deepEqual(out.allowedOutputClasses,c.allowedOutputClasses);assert.equal(out.authority.compositionOwnsDecision,false);assert.equal(out.authority.professionalInstructionAllowed,false);assert.equal(out.rules.userDecisionAuthority,true);assert.ok(out.cards[0].lenses.every(x=>x.sourceClaim.decisionDirectivePromotedToSystemClaim===false));
console.log('✓ TARI-W6 decision boundary passed: Tarot can return reflection/questions/perspectives but cannot direct employment, investment, relationship, or treatment decisions.');
