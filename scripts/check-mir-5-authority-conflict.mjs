import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';const read=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const x=read('content/interpretation/governance/interpretation-authority-conflict-policy-v2.json');assert.equal(sha(x.predecessor),x.predecessorSha256);const req=['OLD_MANUSCRIPT > CURRENT_MANUSCRIPT','HISTORICAL_FIGURE > CANONICAL_MFIG_OR_CURRENT_FIGURE','AI_OUTPUT > CANONICAL_RULE','METHOD_ADAPTER > SHARED_AUTHORITY','INTERPRETATION > CALCULATION','INTERPRETATION > CANONICAL_MEANING_IDENTITY'];for(const q of req)assert.ok(x.blockedPrecedence.includes(q));const b=read('content/interpretation/governance/interpretation-authority-boundary-v1.json');assert.equal(b.futureKernel.state,'RESERVED_SINGLETON_UNINSTANTIATED');assert.equal(b.existingCompositionRuntime.classification,'CANDIDATE_ONLY_AI_COMPOSITION');console.log('✓ MIR-5 authority conflict policy passed; MIR-1 frozen singleton/composition separation remains intact.');
