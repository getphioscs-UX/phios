import {assert,read,text} from './_mir9-check-common.mjs';import {projectRjxClientStage} from '../functions/reality-journey-runtime/state-projection-v1.js';
const role=read('content/runtime/journey-runtime/contracts/reality-journey-role-contract-v1.json');assert.equal(role.boundaries.allQuestionsDefaultToJourney,false);const canonical=read('content/runtime/journey-runtime/registries/canonical-journey-stage-registry-v2.json');const sm=read('content/runtime/journey-runtime/mir9/mir-9-rjx-state-machine-v1.json');assert.deepEqual(sm.canonicalBackendOrder,canonical.canonicalOrder);assert.deepEqual(sm.clientStages,['Understand','Choose','Review']);for(const s of canonical.canonicalOrder){const p=projectRjxClientStage(s);assert.equal(p.runtimeState,s);assert.ok(['Understand','Choose','Review'].includes(p.clientStage));assert.equal(p.authority.runtimeStateMutated,false);assert.equal(p.authority.urlOwnsState,false);}assert.equal(projectRjxClientStage('reading').clientStage,'Understand');assert.equal(projectRjxClientStage('reconstruction').clientStage,'Understand');assert.equal(projectRjxClientStage('navigation').clientStage,'Choose');assert.equal(projectRjxClientStage('review').clientStage,'Review');assert.throws(()=>projectRjxClientStage('screen-2'),/MIR9_RUNTIME_STATE_UNKNOWN/);
const authority=read('content/runtime/journey-runtime/mir9/mir-9-rjx-authority-boundary-v1.json');assert.equal(authority.preservedAuthorities.inputAndCanonicalCase,'ICR');assert.equal(authority.preservedAuthorities.realityModel,'RMO');assert.equal(authority.preservedAuthorities.dataGovernance,'RDG');assert.equal(authority.interpretationBoundary.recommendation,false);assert.equal(authority.interpretationBoundary.action,false);
const workspace=read('content/runtime/journey-runtime/mir9/mir-9-unified-workspace-contract-v1.json');assert.equal(workspace.canonicalWorkspace,'/reality/');assert.equal(workspace.phase19BytesMayMutate,false);assert.equal(workspace.runtimeStateEncodedInUrl,false);assert.equal(workspace.rules.mir9CreatesSecondWorkspaceAuthority,false);
const nav=read('content/runtime/journey-runtime/mir9/mir-9-navigation-boundary-v1.json');assert.equal(nav.rules.understandMayChoose,false);assert.equal(nav.rules.interpretationMayRecommend,false);assert.equal(nav.rules.userChoiceRequired,true);const pro=read('content/runtime/journey-runtime/mir9/mir-9-professional-handoff-contract-v1.json');assert.equal(pro.rules.canonicalDerivationMutationAllowed,false);
for(const f of ['functions/reality-journey-runtime/reality-escalation-v1.js','functions/reality-journey-runtime/state-projection-v1.js']){const s=text(f);for(const forbidden of ['shared-calculation-runtime','core-method-runtime','derivation-engine-v2','kernel-v1.js','method-execute'])assert.equal(s.includes(forbidden),false,`${f} must not import upstream calculation/interpretation authority ${forbidden}`);}console.log('✓ RJX MIR-9 successor passed: RJX stays non-default, 8 backend states project read-only to Understand/Choose/Review, Navigation and Professional authority remain separate, and /reality/ remains the single canonical workspace target.');
