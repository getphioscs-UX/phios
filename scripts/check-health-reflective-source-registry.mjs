import assert from 'node:assert/strict';import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('content/health/health-reflective/sources/health-reflective-source-registry-v1.json','utf8'));assert.ok(x.sources.length>=1);assert.ok(x.sources.every(s=>s.medicalAuthority===false&&s.causalityAllowed===false));console.log(`✓ Health reflective source registry passed: ${x.sources.length} source(s).`);
