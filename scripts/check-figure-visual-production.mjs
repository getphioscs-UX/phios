import {assert,fs,all,read,sha,manifestPath,svgPath,pngPath,webpPath} from './figure/figure-check-lib.mjs';for(const {id,spec} of all()){const m=read(manifestPath(id));for(const p of [svgPath(id),pngPath(id),webpPath(id)])assert.ok(fs.existsSync(p),`${id} missing ${p}`);assert.equal(sha(svgPath(id)),m.svgDigest);assert.equal(sha(pngPath(id)),m.pngDigest);assert.equal(sha(webpPath(id)),m.webpDigest);const svg=fs.readFileSync(svgPath(id),'utf8');assert.ok(svg.includes(`data-figure-id="${id}"`));for(const n of spec.nodes){assert.ok(svg.includes(`id="node-${n.nodeId}"`));assert.ok(svg.includes(`data-semantic-key="${n.semanticKey}"`));}for(const e of spec.edges){assert.ok(svg.includes(`id="edge-${e.edgeId}"`));assert.ok(svg.includes(`data-from="${e.from}"`));assert.ok(svg.includes(`data-to="${e.to}"`));}for(const b of spec.boundaries)assert.ok(svg.includes(`id="boundary-${b.boundaryId}"`));}console.log('✓ FIG visual production checker: canonical SVG DOM + PNG/WebP derivative digests verified for 57/57.');
