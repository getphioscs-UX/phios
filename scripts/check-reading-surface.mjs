import assert from 'node:assert/strict';import fs from 'node:fs';import {buildGuidedReadingSurfaceModel} from '../assets/js/knowledge/guided-reading-surface-model.js';
const html=fs.readFileSync('guided-reading.html','utf8');const controller=fs.readFileSync('assets/js/pages/guided-reading.js','utf8');const client=fs.readFileSync('assets/js/knowledge/guided-reading-v2-client.js','utf8');const css=fs.readFileSync('assets/css/guided-reading.css','utf8');
for(const marker of ['data-gr-observed','data-gr-calculated','data-gr-derived','data-gr-meaning','data-gr-human','data-gr-why','data-gr-unknown','data-gr-outcome'])assert.match(html,new RegExp(marker));assert.match(html,/No browser storage is used/);assert.match(controller,/PHI_OS_ACCEPTED_INTERPRETATION_RESULT/);assert.match(client,/\/api\/guided-reading-v2/);for(const text of [controller,client])for(const forbidden of ['localStorage','sessionStorage','/api/method-execute','core-method-runtime','canonical-projection-runtime'])assert.equal(text.includes(forbidden),false,`Reading surface forbidden: ${forbidden}`);assert.match(css,/@media\(max-width:800px\)/);
const model=buildGuidedReadingSurfaceModel({interpretationResultId:'CIR-AAAAAAAAAAAAAAAAAAAAAAAA',locale:'en',sections:{whatWasObserved:[{sourceClass:'OBSERVED',value:{statement:'Observed'}}],whatWasCalculated:[{sourceClass:'CALCULATED',value:{summary:'Calculated'}}],whatWasDerived:[{sourceClass:'DERIVED',value:{target:'State'}}],whatItMayMean:[{sourceClass:'INTERPRETED',value:{authorityStatus:'CONDITIONAL'}}],humanProfessionalInputs:[{sourceClass:'HUMAN_PROFESSIONAL_INPUT',value:{source:'HUMAN_PROFESSIONAL',value:'accepted'}}],alternativeInterpretations:[],whatRemainsUncertain:[{sourceClass:'UNKNOWN',value:{code:'UNKNOWN_X'}}]},why:{ruleRefs:['RULE-1'],knowledgeRefs:['KN-1'],figureRefs:['MFIG-001'],evidenceRefs:['E-1'],sourceReference:{},evidenceExpansion:{}},outcome:{status:'ANSWER_SUFFICIENT',nextQuestions:[],handoff:{realityJourneyEligible:false}},governance:{}});assert.equal(model.observed[0].sourceClass,'OBSERVED');assert.equal(model.calculated[0].sourceClass,'CALCULATED');assert.equal(model.derived[0].sourceClass,'DERIVED');assert.equal(model.interpreted[0].sourceClass,'INTERPRETED');assert.equal(model.humanProfessionalInputs[0].sourceClass,'HUMAN_PROFESSIONAL_INPUT');assert.equal(model.unknowns[0].sourceClass,'UNKNOWN');assert.equal(model.outcome,'ANSWER_SUFFICIENT');
const registry=JSON.parse(fs.readFileSync('content/knowledge/answer-projection/registries/mir-8-reading-surface-registry-v1.json','utf8'));assert.equal(registry.status,'SUCCESSOR_SURFACE_READY_ROUTE_PROMOTION_DEFERRED_TO_MIR11');assert.equal(registry.existingFrozenSurfacesMutated,false);assert.equal(registry.readingIsSecondInterpretationRuntime,false);
console.log('✓ MIR-8 Reading Surface passed: explainable six-section surface is responsive, no-storage, accepted-result driven, and does not call method/calculation runtimes.');
