import assert from 'node:assert/strict';import fs from 'node:fs';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const p=j('content/production/symbolic-method/reconciliation/iching-production-activation-readiness-v4.json'),c=j('content/production/symbolic-method/reconciliation/iching-production-activation-readiness-v5.json');
assert.equal(c.successorOf,'content/production/symbolic-method/reconciliation/iching-production-activation-readiness-v4.json');assert.equal(c.historicalPredecessorMutated,false);assert.equal(p.currentAuthority.state,'LIMITED_PRODUCTION');assert.equal(c.currentAuthority.state,'LIMITED_PRODUCTION');assert.equal(c.currentAuthority.governedBetaRunAllowed,true);assert.equal(c.currentAuthority.globalPublicRunAllowed,false);assert.equal(c.currentAuthority.fullProductionAllowed,false);assert.equal(c.successorObservationAuthority.historicalFirstLiveAdmissionSha,'025588253432e411b130a61bf4b38fe540cfcf54');assert.equal(c.successorObservationAuthority.candidateMustBeDescendant,true);assert.equal(c.successorObservationAuthority.candidateMustMatchRuntimeFingerprint,true);assert.equal(c.fullProductionGates.w32R1SuccessorObservationAccepted,false);assert.equal(c.fullProductionGates.w33FinalProductionAcceptance,false);
console.log('✓ ICH-PROD-W32R1 activation readiness v5 passed: LIMITED_PRODUCTION remains active while successor-SHA observation requires fresh lineage, fingerprint and live-session evidence; FULL_PRODUCTION remains closed.');
