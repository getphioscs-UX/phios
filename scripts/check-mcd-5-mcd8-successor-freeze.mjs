import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const freeze=j('content/professional/method-client-delivery/freeze/mcd-5-canonical-projection-freeze-v1.json');const acceptance=j('content/professional/method-client-delivery/acceptance/mcd-5-canonical-projection-acceptance-v1.json');
assert.equal(freeze.status,'FROZEN_CORE_ISOLATED_CANONICAL_RESULT_INTERPRETATION_EXCLUDED_HDR_VALIDATION_ONLY');for(const record of [...freeze.frozenOutputs,...freeze.predecessorEvidence])assert.equal(sha(record.path),record.sha256,`MCD-5 predecessor drift: ${record.path}`);assert.equal(freeze.authorityFreeze.productionDispatchOwner,'MPA');assert.equal(freeze.authorityFreeze.clientContract,'CANONICAL_METHOD_PROJECTION_ONLY');assert.equal(freeze.hdrFreeze.productionCustomerResultAllowed,false);assert.equal(acceptance.nextWork,'MCD-6');
const mcd7=j('content/professional/method-client-delivery/contracts/mcd-7-personal-runtime-result-surface-contract-v1.json');assert.equal(mcd7.authority.frontendConsumesCanonicalProjectionOnly,true);assert.equal(mcd7.authority.interpretationIncluded,false);
console.log('✓ MCD-5 → MCD-8 Canonical Projection freeze successor passed.');
