import assert from 'node:assert/strict';import fs from 'node:fs';
import {composeAstPlanetSignMeaning} from '../functions/ast-full-production/ast-planet-sign-composition-runtime.js';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8')), ontology=j('content/professional/ast-production/meaning/ast-meaning-ontology-v1.json'),baseRule=j('content/professional/ast-production/contracts/ast-composite-meaning-rules-v1.json'),rule=j('content/professional/ast-full-production/recovery/ast-mfp-r-planet-sign-composition-rule-v1.json'),campaign=j('content/professional/ast-full-production/recovery/ast-mfp-r-planet-sign-machine-campaign-v1.json'),review=j('content/professional/ast-full-production/recovery/ast-mfp-r-planet-sign-human-review-v1.json');
assert.equal(ontology.entries.filter(x=>x.meaningFamily==='AST_FUNCTIONAL_DRIVER').length,10);assert.equal(ontology.entries.filter(x=>x.meaningFamily==='AST_DIRECTION_MODE').length,12);assert.ok(baseRule.components.body.includes('AST_FUNCTIONAL_DRIVER'));assert.ok(baseRule.components.body.includes('AST_DIRECTION_MODE'));
assert.equal(rule.ownerProgram,'AST_FULL_PRODUCTION');assert.equal(rule.rules.humanAdmissionRequiredBeforeCustomerRuntime,true);assert.equal(rule.productionAdmission,null);
assert.deepEqual(campaign.coverage,{planetCount:10,signCount:12,localeCount:2,totalCases:240,expectedCases:240});assert.equal(campaign.actual.passed,240);assert.equal(campaign.actual.failed,0);assert.equal(campaign.actual.deterministicReplayCases,240);assert.equal(campaign.governance.humanAccepted,false);assert.equal(campaign.governance.productionAdmitted,false);assert.equal(campaign.governance.customerRuntimeUseAllowed,false);
for(const x of campaign.cases){assert.equal(x.status,'PASS');assert.equal(x.boundaries.rendererCreatedMeaning,false);assert.equal(x.boundaries.customerRuntimeUseAllowed,false);assert.equal(x.sourceRefs.length,5)}
for(const locale of ['en','zh-Hans']){const out=composeAstPlanetSignMeaning({planetCode:'SUN',signCode:'ARIES',locale,meaningOntology:ontology,compositionRule:rule});assert.equal(out.state,'MACHINE_CANDIDATE_HUMAN_REVIEW_REQUIRED');assert.equal(out.boundaries.humanAdmissionRequired,true);assert.equal(out.boundaries.customerRuntimeUseAllowed,false);assert.doesNotMatch(out.customerText,/guaranteed outcome|fixed personality|命中注定|必然会发生/i)}
assert.equal(review.requiredCases,24);assert.equal(review.pending,24);assert.equal(review.accepted,0);assert.equal(review.rejected,0);assert.ok(review.cases.every(x=>x.decision==='PENDING'));
console.log('✓ MFP-R-AST-001 machine successor passed: 10 planets × 12 signs × 2 locales = 240/240 deterministic compositions; 24 human cases remain pending and customer runtime use stays blocked.');
