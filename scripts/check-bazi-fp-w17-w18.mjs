import assert from 'node:assert/strict';
import fs from 'node:fs';
import {runMachineCampaign,coverageSummary,humanSelectedSpecs} from './lib/bazi-fp-w17-campaign.mjs';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const t=p=>fs.readFileSync(p,'utf8');
const baseline='f52b6a3c4f1d94e6bf707af47f34e8c7dfca8837';
for(const p of ['content/professional/bzr-full-production/contracts/bazi-machine-campaign-contract-v1.json','content/professional/bzr-full-production/contracts/bazi-human-acceptance-contract-v1.json']){const x=j(p);assert.equal(x.status,'FROZEN_ENGINEERING_CONTRACT');assert.equal(x.baselineCommit,baseline)}
const reg=j('content/professional/bzr-full-production/machine/bazi-fp-w17-machine-case-registry-v1.json');const stored=j('content/professional/bzr-full-production/machine/bazi-fp-w17-machine-results-v1.json');assert.equal(reg.caseCount,96);assert.equal(reg.cases.length,96);assert.equal(stored.status,'MACHINE_CAMPAIGN_PASS');assert.deepEqual(stored.counts,{total:96,passed:96,failed:0});assert.equal(stored.baselineCommit,baseline);
const rerun=await runMachineCampaign(),coverage=coverageSummary(rerun.specs,rerun.results);assert.equal(coverage.caseCount,96);assert.equal(coverage.passCount,96);assert.equal(coverage.failCount,0);assert.equal(coverage.scenarioCount,12);assert.equal(coverage.monthBranches.length,12);assert.equal(coverage.dayMasters.length,10);assert.equal(coverage.threePillarCases,24);assert.equal(coverage.fourPillarCases,72);assert.equal(coverage.transitionCases,12);assert.equal(coverage.currentUnavailableCases,12);assert.equal(coverage.annualUnavailableCases,12);assert.deepEqual(coverage.locales,['en','zh-Hans']);
for(const rel of ['BRANCH_CLASH','BRANCH_SIX_COMBINATION','BRANCH_HARM','BRANCH_BREAK','BRANCH_PUNISHMENT_GROUP','BRANCH_SELF_PUNISHMENT','BRANCH_THREE_HARMONY','BRANCH_THREE_MEETING'])assert(coverage.relationTypes.includes(rel),rel);
const storedById=new Map(stored.cases.map(x=>[x.caseId,x]));for(const x of rerun.results){const s=storedById.get(x.caseId);assert(s,`stored result missing ${x.caseId}`);assert.equal(s.pass,true);assert.equal(x.readingDigest,s.readingDigest,`${x.caseId} reading drift`);assert.equal(x.reportDigest,s.reportDigest,`${x.caseId} report drift`);assert.deepEqual(x.checks,s.checks,`${x.caseId} machine criteria drift`)}
const w17=j('content/professional/bzr-full-production/acceptance/bazi-fp-w17-machine-acceptance-v1.json');assert.equal(w17.status,'MACHINE_ACCEPTED_96_OF_96');assert.equal(w17.counts.passed,96);assert.equal(w17.humanAcceptanceCreated,false);
const hc=j('content/professional/bzr-full-production/review/bazi-fp-w18-human-review-cases-v1.json');const hr=j('content/professional/bzr-full-production/review/bazi-fp-w18-human-review-results-v1.json');assert.equal(hc.status,'READY_FOR_24_CASE_HUMAN_ACCEPTANCE');assert.equal(hc.cases.length,24);assert.equal(hc.selectionCoverage.scenarioCount,12);assert.equal(hc.selectionCoverage.variantCount,8);assert.equal(hc.selectionCoverage.monthBranches.length,12);assert.equal(hc.selectionCoverage.dayMasters.length,10);assert.equal(hr.status,'PENDING_HUMAN_REVIEW');assert.equal(hr.requiredCaseCount,24);assert.equal(hr.pending,24);assert.equal(hr.accepted,0);assert.equal(hr.decisions.length,24);assert(hr.decisions.every(x=>x.decision==='PENDING'));
const ready=j('content/professional/bzr-full-production/acceptance/bazi-fp-w18-review-readiness-v1.json');assert.equal(ready.status,'REVIEW_READY_24_PENDING_HUMAN_ACCEPTANCE');assert.equal(ready.humanAccepted,0);assert.equal(ready.modelMayApprove,false);
const ui=t('content/professional/bzr-full-production/review/bazi-fp-w18-human-review.html');for(const token of ['24-Case Human Acceptance','全部标记 ACCEPT','导出审核 JSON','NEEDS_REVISION','WHOLE_CHART_READABILITY'])assert(ui.includes(token),token);
const client=t('assets/js/pages/bazi-full-reading.js');assert(client.includes('function schoolCopy'));for(const token of ['子平从月令与格局成败进入','体用视角观察整盘','调候单独观察寒暖与燥湿'])assert(client.includes(token));
const roadmap=j('content/professional/bzr-full-production/roadmap/bazi-full-production-master-work-v1.7.json');assert.equal(roadmap.currentBaselineCommit,baseline);assert.match(roadmap.status,/W17_MACHINE_96_OF_96_PASS_W18_24_CASE_HUMAN_REVIEW_READY/);assert.equal(roadmap.works.find(x=>x.work==='BAZI-FP-W17').status,'MACHINE_ACCEPTED_96_OF_96');assert.equal(roadmap.works.find(x=>x.work==='BAZI-FP-W18').status,'REVIEW_READY_24_PENDING_HUMAN_ACCEPTANCE');assert.equal(roadmap.works.find(x=>x.work==='BAZI-FP-W19').status,'BLOCKED_PENDING_W18_HUMAN_ACCEPTANCE');
console.log('✓ BAZI-FP-W17/W18 machine campaign + human review readiness passed.');
console.log(`  W17: ${coverage.passCount}/${coverage.caseCount} machine cases passed; 12 month branches, 10 Day Masters, ${coverage.relationTypes.length} natal relation types, ${coverage.temporalRelationTypes.length} temporal relation types.`);
console.log(`  Diversity: ${coverage.threePillarCases} three-pillar, ${coverage.fourPillarCases} four-pillar, ${coverage.transitionCases} transition-day, ${coverage.currentUnavailableCases} current-Luck-Cycle unavailable, ${coverage.annualUnavailableCases} annual unavailable.`);
console.log(`  W18: ${hc.cases.length} diversity-selected cases are review-ready; human acceptance remains ${hr.accepted}/${hr.requiredCaseCount} with ${hr.pending} pending.`);
