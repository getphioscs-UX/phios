import assert from 'node:assert/strict';
import {BASELINE,readJson,readText} from './lib/web-production/wpr-integrity-v1.mjs';
const c=readJson('content/web-production/contracts/wpr-privacy-security-production-v1.json');assert.equal(c.baselineCommit,BASELINE);assert.equal(c.work,'WPR-W26');assert.equal(c.status,'ACTIVE_FAIL_CLOSED_RESPONSE_BOUNDARY');assert.equal(c.rules.robotsIsNotAuthorization,true);assert.equal(c.rules.headersDoNotGrantDataAccess,true);assert.equal(c.rules.wprDoesNotCreateConsent,true);assert.equal(c.rules.wprDoesNotChangeRetention,true);
const rdg=readJson('content/governance/reality-data-governance/freeze/rdg-w16-w20-access-privacy-rights-freeze-v1.json');assert.equal(rdg.status,'frozen');assert.equal(rdg.permissionFailsClosed,true);assert.equal(rdg.professionalIsolationPreserved,true);
const security=readJson('content/registry/runtime-security-privacy.json');assert.equal(security.default_classification,'private');assert.equal(security.logging.full_sensitive_text_allowed,false);assert.equal(security.professional_boundary.automatic_account_access,false);assert.equal(security.professional_boundary.explicit_runtime_ids_required,true);
const reg=readJson('content/web-production/registries/wpr-response-security-registry-v1.json');assert.equal(reg.baselineCommit,BASELINE);assert.ok(reg.privateNoStoreRoutes.includes('/professional-workspace*'));assert.ok(reg.privateNoStoreRoutes.includes('/professional-reports*'));assert.deepEqual(reg.publicAssetOrigins,['self','data:','https://*.r2.dev','https://*.getphios.com']);
const h=readText('_headers');const rootBlock=h.split('\n\n')[0];for(const x of ["X-Content-Type-Options: nosniff","Referrer-Policy: strict-origin-when-cross-origin","camera=(), microphone=(), geolocation=()","style-src 'self' 'unsafe-inline' https://fonts.googleapis.com","font-src 'self' https://fonts.gstatic.com","img-src 'self' data: https://*.r2.dev https://*.getphios.com","object-src 'none'","form-action 'self'","frame-ancestors 'none'"])assert.ok(rootBlock.includes(x),x);assert.equal(/img-src[^;]*\shttps:(?:\s|;)/.test(rootBlock),false,'ARBITRARY_HTTPS_IMG_SOURCE');
for(const route of ['/reality-dashboard*','/personal-runtime*','/professional-workspace*','/professional-reports*','/account*','/checkout*']){const start=h.indexOf(`\n${route}\n`);assert.ok(start>=0,route);const block=h.slice(start,start+180);assert.ok(block.includes('Cache-Control: private, no-store'),route);assert.ok(block.includes('X-Robots-Tag: noindex, nofollow, noarchive'),route);}
const asset=readText('assets/js/runtime/web-production/asset-resolver.js');assert.ok(asset.includes("url.protocol !== 'https:'"));assert.ok(asset.includes('url.username || url.password || url.search || url.hash'));
const audit=readJson('content/web-production/audits/wpr-w26-security-header-reconciliation-v1.json');assert.equal(audit.resolution.arbitraryHttpsImageWildcardAdded,false);assert.equal(audit.authorityExpansionGranted,false);
const a=readJson('content/web-production/acceptance/wpr-w26-privacy-security-acceptance-v1.json');assert.equal(a.baselineCommit,BASELINE);for(const v of Object.values(a.nonActivation))assert.equal(v,false);
const pkg=readJson('package.json');assert.equal(pkg.scripts['check:wpr-w26'],'node scripts/check-wpr-w26-privacy-security-production.mjs');assert.equal(pkg.scripts['check:wpr-security'],'npm run check:wpr-w26');assert.ok(pkg.scripts['check:wpr'].includes('npm run check:wpr-security'));assert.equal(pkg.scripts.postcheck.includes('check:wpr'),false);
console.log('✓ WPR-W26 Privacy / Security Production Boundary passed.');console.log('  Private routes are no-store/noindex and CSP narrowly reconciles Google Fonts plus governed R2/getphios image origins.');
