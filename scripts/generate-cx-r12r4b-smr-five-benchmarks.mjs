import fs from 'node:fs';
import path from 'node:path';
import {METHODS,BASELINE_COMMIT,buildBenchmark,evidenceFromBenchmark} from './smr-benchmark-support.mjs';
const out='content/customer-experience-rebuild/r12r4b/smr/benchmark';fs.mkdirSync(out,{recursive:true});
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const label={AST:'Astrology',BZR:'BaZi',ZWR:'Zi Wei Dou Shu',NUM:'Numerology',ECR:'Embodied Configuration'};
const slug={AST:'ast',BZR:'bzr',ZWR:'zwr',NUM:'num',ECR:'ecr'};
const stateClass=s=>s==='PASS'?'pass':s==='FAIL'?'fail':s==='UPSTREAM_GAP'?'gap':s==='PARTIAL'?'partial':'na';
function techTable(vm){const nodes=(vm?.nodes||[]).slice(0,24),edges=(vm?.edges||[]).slice(0,24);return `<div class="tech-grid"><section><h4>Calculated structure</h4><table><thead><tr><th>Node</th><th>Role</th><th>Value</th><th>Priority</th></tr></thead><tbody>${nodes.map(n=>`<tr><td>${esc(n.localizedLabel||n.label||n.nodeId)}</td><td>${esc(n.role)}</td><td>${esc(n.value)}</td><td>${esc(n.priority)}</td></tr>`).join('')}</tbody></table>${(vm?.nodes||[]).length>24?`<p class="muted">Showing 24 of ${vm.nodes.length} nodes.</p>`:''}</section><section><h4>Calculated relations</h4><table><thead><tr><th>Relation</th><th>From</th><th>To</th></tr></thead><tbody>${edges.map(e=>`<tr><td>${esc(e.relationType)}</td><td>${esc(e.sourceNodeId)}</td><td>${esc(e.targetNodeId)}</td></tr>`).join('')}</tbody></table>${(vm?.edges||[]).length>24?`<p class="muted">Showing 24 of ${vm.edges.length} relations.</p>`:''}</section></div>`}
function render(methodId,b,e){const sections=b.ia.sections.filter(s=>s.eligibility==='SECTION_ELIGIBLE');const firstRefs=new Set(b.layout.firstScreen.blocks.flatMap(x=>x.sourceRefs||[]));return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${label[methodId]} · SMR-R2 Benchmark</title><style>
:root{--ink:#181817;--muted:#6d6b64;--line:#ddd9cf;--paper:#fbfaf7;--card:#fff;--soft:#f2efe8;--accent:#3c4a43;--good:#215d3d;--warn:#8a5b11;--bad:#8b2e2e}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:16px/1.58 ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.shell{max-width:1120px;margin:auto;padding:34px 24px 64px}.eyebrow{font-size:12px;letter-spacing:.14em;text-transform:uppercase;color:var(--muted)}h1{font-family:Georgia,serif;font-weight:500;font-size:clamp(34px,5vw,62px);line-height:1.04;margin:.25em 0}.lead{max-width:760px;font-size:20px;color:#403f3a}.badge{display:inline-block;padding:5px 9px;border:1px solid var(--line);border-radius:999px;font-size:12px;background:#fff}.hero{padding:34px 0 28px;border-bottom:1px solid var(--line)}.meta{display:flex;gap:10px;flex-wrap:wrap;margin-top:18px}.first{display:grid;grid-template-columns:minmax(0,1fr) 290px;gap:26px;padding:30px 0}.panel,.section,.review{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:24px}.panel h3,.section h2{margin-top:0}.first ul{padding-left:18px}.section{margin:18px 0}.section small{color:var(--muted)}.item{padding:15px 0;border-top:1px solid #eeeae1}.item:first-of-type{border-top:0}.item p{margin:.3em 0 0}.source{font-size:11px;color:var(--muted);word-break:break-all}.review{margin:28px 0}.criteria{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:10px}.criterion{border:1px solid var(--line);border-radius:12px;padding:13px}.criterion b{display:block}.criterion .state{font-size:11px;letter-spacing:.08em}.pass .state{color:var(--good)}.partial .state,.gap .state{color:var(--warn)}.fail .state{color:var(--bad)}details{background:#fff;border:1px solid var(--line);border-radius:16px;margin:18px 0}summary{cursor:pointer;padding:18px 22px;font-weight:650}.detail-body{padding:0 22px 22px}.tech-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px}table{border-collapse:collapse;width:100%;font-size:12px}th,td{text-align:left;border-bottom:1px solid #ece8df;padding:7px;vertical-align:top}.muted{color:var(--muted);font-size:13px}.boundary{padding:18px;border-left:3px solid #8b887f;background:var(--soft);margin-top:28px}@media(max-width:760px){.first,.tech-grid{grid-template-columns:1fr}.shell{padding:20px 14px 48px}.panel,.section,.review{padding:18px}h1{font-size:38px}}@media print{body{background:#fff}.shell{max-width:none;padding:0}.panel,.section,.review,details{break-inside:avoid;border-color:#bbb}summary{list-style:none}details>.detail-body{display:block}.review{page-break-before:always}.meta .badge{border:1px solid #999}}
</style></head><body><main class="shell"><header class="hero"><div class="eyebrow">PHI OS · Single-Method Reading R2 · W${12+METHODS.indexOf(methodId)} benchmark</div><h1>${label[methodId]} reading</h1><p class="lead">${esc(b.methodResult.summary)}</p><div class="meta"><span class="badge">${esc(e.caseId)}</span><span class="badge">${esc(e.benchmarkState)}</span><span class="badge">Different authority digest: ${e.structurallyDifferentFromLegacyCampaign?'YES':'NO'}</span><span class="badge">Technical detail collapsed by default</span></div></header>
<section class="first"><div class="panel"><div class="eyebrow">First screen</div><h3>What stands out first</h3>${b.layout.firstScreen.blocks.map(block=>{const items=sections.flatMap(s=>s.items).filter(i=>(block.sourceRefs||[]).includes(i.sourceRef));if(block.slot==='READING_TITLE')return `<p><strong>${label[methodId]} · governed benchmark reading</strong></p>`;if(block.slot==='EXPLORE_FULL_READING_CTA')return `<p class="muted">Continue into the full reading ↓</p>`;return items.map(i=>`<p>${esc(i.text||i.itemType)}</p>`).join('')}).join('')}</div><aside class="panel"><div class="eyebrow">Question</div><h3>${esc(e.questionIntent.prompt)}</h3><p class="muted">The question can change priority and eligible sections. It cannot create method meaning.</p><hr><p><strong>${e.counts.claims}</strong> claims · <strong>${e.counts.themes}</strong> themes<br><strong>${e.counts.eligibleSections}</strong> eligible sections<br><strong>${e.counts.renderedDuplicateTexts}</strong> duplicate rendered texts</p></aside></section>
${sections.map((s,idx)=>`<section class="section" id="${esc(s.sectionId)}"><small>${String(idx+1).padStart(2,'0')} · ${esc(s.sectionId.replaceAll('_',' '))}</small><h2>${esc(s.customerLabel)}</h2>${s.items.map(i=>`<article class="item"><p>${i.text?esc(i.text):i.itemType==='OBSERVATION_QUESTION'?`<em>Observation question ref:</em> ${esc(i.questionRef)}`:`<em>Evidence lineage:</em> ${esc(i.claimRef||i.sourceRef)}`}</p><div class="source">${esc(i.sourceRef)}</div></article>`).join('')}</section>`).join('')}
<details><summary>Why this reading / evidence lineage</summary><div class="detail-body"><p><strong>Reading authority:</strong> <code>${esc(e.readingAuthorityRef)}</code></p><p><strong>Production admission:</strong> <code>${esc(e.productionAdmissionRef)}</code></p><p><strong>Interpretation units:</strong> ${e.lineage.interpretationUnitRefs.map(x=>`<code>${esc(x)}</code>`).join(' · ')}</p><p class="muted">These references explain where admitted meaning came from. They are not additional customer claims.</p></div></details>
<details><summary>Method detail · calculated structure</summary><div class="detail-body"><p class="muted">Technical structure is shown for traceability only. Raw projection does not create a customer conclusion.</p>${techTable(b.methodResult.visualModel)}</div></details>
<section class="review"><div class="eyebrow">Machine benchmark evidence · not human acceptance</div><h2>Coverage checklist</h2><div class="criteria">${e.criteria.map(c=>`<div class="criterion ${stateClass(c.state)}"><span class="state">${esc(c.state)}</span><b>${esc(c.label)}</b><p class="muted">${esc(c.note)}</p>${c.gapCode?`<code>${esc(c.gapCode)}</code>`:''}</div>`).join('')}</div></section>
<div class="boundary"><strong>Boundary.</strong> This is a governed benchmark artifact, not a live customer reading and not a prediction. The body consumes admitted Customer Claim / Narrative IR only. Machine benchmark status does not substitute for Founder W17 human acceptance.</div></main></body></html>`}
const index=[];
for(const m of METHODS){const b=await buildBenchmark(m),e=evidenceFromBenchmark(m,b);if(!e.structurallyDifferentFromLegacyCampaign)throw new Error(`${m}_BENCHMARK_NOT_STRUCTURALLY_DIFFERENT_FROM_LEGACY`);const base=`smr-${slug[m]}-benchmark-v1`;fs.writeFileSync(path.join(out,`${base}-evidence.json`),JSON.stringify(e,null,2)+'\n');fs.writeFileSync(path.join(out,`${base}.html`),render(m,b,e));index.push({methodId:m,report:`${base}.html`,evidence:`${base}-evidence.json`,benchmarkState:e.benchmarkState,summary:e.summary,upstreamGapTickets:e.upstreamGapTickets.map(g=>g.gapCode)})}
const acceptance={schemaVersion:'PHI-OS-SMR-R2-W12-W16-ACCEPTANCE-v1.0.0',baselineCommit:BASELINE_COMMIT,commitUnit:'R2-5',work:['R2-W12','R2-W13','R2-W14','R2-W15','R2-W16'],status:index.some(x=>x.benchmarkState==='BENCHMARK_FAILED')?'ENGINEERING_BLOCKED':index.some(x=>x.upstreamGapTickets.length)?'BENCHMARKS_GENERATED_UPSTREAM_GAPS_RECORDED_READY_FOR_FOUNDER_REVIEW':'BENCHMARKS_GENERATED_READY_FOR_FOUNDER_REVIEW',benchmarks:index,humanAcceptance:{accepted:0,rejected:0,pending:5,productionAdmissionAffected:false},next:'R2-W17 FOUNDER BENCHMARK REVIEW'};fs.writeFileSync('content/customer-experience-rebuild/r12r4b/smr/smr-w12-w16-acceptance-v1.json',JSON.stringify(acceptance,null,2)+'\n');
console.log(JSON.stringify(acceptance,null,2));
