import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';const json=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha256=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const freeze=json('content/professional/method-client-delivery/freeze/mcd-5-canonical-projection-freeze-v1.json');const acceptance=json('content/professional/method-client-delivery/acceptance/mcd-5-canonical-projection-acceptance-v1.json');const pkg=json('package.json');assert.equal(freeze.status,'FROZEN_CORE_ISOLATED_CANONICAL_RESULT_INTERPRETATION_EXCLUDED_HDR_VALIDATION_ONLY');for(const record of [...freeze.frozenOutputs,...freeze.predecessorEvidence])assert.equal(sha256(record.path),record.sha256,`MCD-5 freeze drift: ${record.path}`);assert.equal(freeze.authorityFreeze.productionDispatchOwner,'MPA');assert.equal(freeze.authorityFreeze.clientContract,'CANONICAL_METHOD_PROJECTION_ONLY');assert.equal(freeze.authorityFreeze.interpretation,'EXCLUDED');assert.equal(freeze.hdrFreeze.productionCustomerResultAllowed,false);assert.equal(acceptance.nextWork,'MCD-6');assert.equal(pkg.scripts['check:mcd-5'],'node scripts/check-mcd-5-canonical-method-projection.mjs && node scripts/check-mcd-5-canonical-projection-freeze.mjs');assert.equal(pkg.scripts['check:mcd'],'npm run check:mcd-production-authority && npm run check:mcd-2 && npm run check:mcd-3 && npm run check:mcd-4 && npm run check:mcd-5');console.log('✓ MCD-5 Canonical Projection acceptance and freeze passed.');console.log('  Core isolation, explicit Unknown/Evidence/Version, Interpretation exclusion and HDR validation-only projection are frozen; MCD-6 is renderer-only successor work.');
