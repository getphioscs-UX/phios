import assert from 'node:assert/strict';import fs from 'node:fs';import {requireLiveSourceAdmission} from '../functions/health-knowledge/health-source-admission-v2.js';const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const s=j('content/health/health-knowledge/authority/health-k1-source-registry-v1.json');assert.ok(s.sources.length>=8);assert.ok(s.sources.every(x=>x.sourceAdmissionState==='LIVE_RETRIEVAL_REQUIRED'));const r=requireLiveSourceAdmission({sourceIds:[s.sources[0].sourceId]},[]);assert.equal(r.ready,false);console.log('✓ Health source admission passed: live admission remains fail-closed.');
