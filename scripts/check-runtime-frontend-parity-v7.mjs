import assert from 'node:assert/strict';import fs from 'node:fs';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const t=p=>fs.readFileSync(p,'utf8');const ex=p=>assert.ok(fs.existsSync(p),`missing ${p}`);
const ptr='content/governance/production-capability-matrix/reconciliation/production-capability-current-successor-v7.json';ex(ptr);const current=j(ptr);assert.equal(current.status,'CURRENT_REGISTRY_POINTER');assert.equal(current.predecessor,'content/governance/production-capability-matrix/reconciliation/production-capability-current-successor-v6.json');assert.equal(current.predecessorMutated,false);ex(current.predecessor);
const reg=j(current.currentRegistry),proj=j(current.currentStatusProjection);assert.equal(reg.registryVersion,'7.0.0');assert.equal(proj.source,current.currentRegistry);
for(const code of ['AST','BZR','NUM','ZWR']){const c=reg.capabilities.find(x=>x.methodRuntime?.pluginCode===code);assert(c,code);assert.equal(c.capabilityAvailability,'AVAILABLE',code);assert.equal(c.userExecutable,true,code);assert.equal(c.productionAccepted,true,code);}
const tar=reg.capabilities.find(x=>x.methodRuntime?.pluginCode==='TAR');assert(tar);for(const gate of ['implemented','validated','projected','meaningReady','readingReady','userExecutable','productionAccepted'])assert.equal(tar[gate],true,`TAR:${gate}`);assert.equal(tar.capabilityAvailability,'LIMITED');assert.equal(tar.classification,'LIMITED');assert.equal(tar.statusProjection,'Limited');assert.equal(tar.persistent,false);assert.equal(tar.frontendRoute,'/readings/symbolic/?method=TAROT');assert.equal(tar.executionAuthority.mode,'SERVER_RUNTIME_DYNAMIC');assert.equal(tar.executionAuthority.statusEndpoint,'/api/tarot-production-status');assert.equal(tar.executionAuthority.staticRegistryMayGrantRunAllowed,false);assert.equal(tar.executionAuthority.commitPinned,true);
for(const p of [tar.authority.registration,tar.authority.productionDispatch,tar.authority.clientDelivery,'functions/api/tarot-production-status.js','functions/api/symbolic-method-execute.js','functions/tarot-product-runtime/tarot-production-authority-v2.js','functions/tarot-product-runtime/tarot-product-runtime-v1.js','assets/js/pages/readings-v3.js','assets/js/pages/account-method-status-v2.js','content/web-production/px2/successors/public-method-catalog-v3.json','content/governance/full-method-availability/registries/account-method-status-projection-v2.json'])ex(p);
const projected=(proj.items||[]).find(x=>x.pluginCode==='TAR');assert(projected);assert.equal(projected.status,'Limited');assert.equal(projected.capabilityAvailability,'LIMITED');assert.equal(projected.runtimeAuthorityEndpoint,'/api/tarot-production-status');assert.equal(projected.runAllowedIsDynamic,true);
const method=j(tar.authority.registration).methods.find(x=>x.pluginCode==='TAR');assert(method);assert.equal(method.state,'PRODUCTION_ELIGIBLE');assert.equal(method.productionEligible,true);assert.equal(method.activationAuthority,'SERVER_D1_COMMIT_PINNED_LIMITED_PRODUCTION');
const catalog=j('content/web-production/px2/successors/public-method-catalog-v3.json').methods.find(x=>x.methodCode==='TAROT');assert(catalog);assert.equal(catalog.runAllowed,false);assert.equal(catalog.staticCatalogMayGrantRunAllowed,false);assert.equal(catalog.runtimeAuthorityEndpoint,'/api/tarot-production-status');
const account=t('assets/js/pages/account-method-status-v2.js');assert(account.includes('account-method-status-projection-v2.json'));assert(account.includes('/api/tarot-production-status'));assert(!/localStorage|sessionStorage|indexedDB/.test(account));assert(t('account.html').includes('/assets/js/pages/account-method-status-v2.js'));
const readings=t('assets/js/pages/readings-v3.js');assert(readings.includes('public-method-catalog-v3.json'));assert(readings.includes('/api/tarot-production-status'));assert(!/localStorage|sessionStorage|indexedDB/.test(readings));assert(t('readings/index.html').includes('/assets/js/pages/readings-v3.js'));
console.log('✓ PCM v7 Tarot runtime/frontend parity passed.');console.log('  Tarot is Limited in static capability authority, but runAllowed remains a dynamic server D1 + exact-SHA decision; AST/BZR/NUM/ZWR availability is preserved.');
