import fs from 'node:fs';import assert from 'node:assert/strict';const s=JSON.parse(fs.readFileSync('content/civilization-atlas/reconfiguration/snapshots/world-reconfiguration-snapshots-v1.json','utf8')).snapshots;assert.equal(s.length,12);assert.equal(new Set(s.map(x=>x.visualAssetId)).size,12);for(const x of s){assert.equal(x.visualAssetType,'STATIC_ATLAS_BASE_VISUAL');assert.ok(['NONE','MINIMAL'].includes(x.visualTextPolicy));assert.equal(x.r2.expectedPath.endsWith('.webp'),true);}console.log('✓ Book VI snapshot visual contract');