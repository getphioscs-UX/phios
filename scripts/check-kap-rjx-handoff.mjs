import {assert,read,stable,fixture} from './_mir9-check-common.mjs';import {interpretCanonicalProjection} from '../functions/interpretation-runtime/canonical-api-v1.js';import {composeAcceptedGuidedReading} from '../functions/_lib/guided-reading-v2.js';import {evaluateRealityComplexityGate} from '../functions/_lib/knowledge-reality-complexity.js';import {evaluateRealityEntryEligibility,buildReadingRealityHandoff,MIR9_ESCALATION_CONSTANTS} from '../functions/reality-journey-runtime/reality-escalation-v1.js';
const result=await interpretCanonicalProjection(fixture('astrology-projection.valid.json'),{context:{runtimeState:{state:'observed'}},locale:'zh-Hans'});const readingConsent={optedIn:true,readingPermitted:true,interpretationResultId:result.interpretationResultId};const context={currentConcern:'一个持续多年、涉及家庭与工作、多重目标冲突且原因不清的问题',currentCondition:'多个关系和约束持续互相影响',complexitySignals:{persistent:true,multiFactor:true,caseSpecific:true,realityDependent:true}};const reading=composeAcceptedGuidedReading({result,readingConsent,context,locale:'zh-Hans'});assert.equal(reading.outcome.status,'REALITY_MODEL_REQUIRED');
const structuredContext={peopleCount:2,relationshipCount:2,goalCount:2,constraintCount:3,goalConflict:true,longTimeline:true,repeatPattern:true,unclearCausalStructure:true,persistentUnresolvedState:true,highConsequenceDecision:true};const complexity=evaluateRealityComplexityGate({question:context.currentConcern,guidedContext:{originalQuestion:context.currentConcern,escalationSignals:{persistent:true,multiFactor:true,caseSpecific:true}},structuredContext,w22StopCondition:reading.outcome});assert.equal(complexity.realityModelRequirement.requirement,'YES');
const before=stable(result);let eligibility=evaluateRealityEntryEligibility({acceptedInterpretationResult:result,guidedReadingResponse:reading,complexityEvaluation:complexity});assert.equal(eligibility.status,'AWAITING_EXPLICIT_ESCALATION_CONSENT');assert.equal(eligibility.eligible,false);assert.throws(()=>buildReadingRealityHandoff({acceptedInterpretationResult:result,guidedReadingResponse:reading,complexityEvaluation:complexity}),/MIR9_RJX_ENTRY_NOT_ELIGIBLE/);
const escalationConsent={explicit:true,accepted:true,scope:MIR9_ESCALATION_CONSTANTS.consentScope,interpretationResultId:result.interpretationResultId,consentTextVersion:'1.0.0',recordedAt:'2026-08-17T12:00:00.000Z'};eligibility=evaluateRealityEntryEligibility({acceptedInterpretationResult:result,guidedReadingResponse:reading,complexityEvaluation:complexity,escalationConsent});assert.equal(eligibility.status,'ELIGIBLE_FOR_RJX_ENTRY');const handoff=buildReadingRealityHandoff({acceptedInterpretationResult:result,guidedReadingResponse:reading,complexityEvaluation:complexity,escalationConsent});assert.equal(handoff.schemaVersion,'PHI-OS-MIR-9-READING-REALITY-HANDOFF-v1.0.0');assert.equal(handoff.backendState,'entry');assert.equal(handoff.clientStage,'Understand');assert.equal(handoff.governance.canonicalCaseCreated,false);assert.equal(handoff.governance.realityModelCreated,false);assert.equal(handoff.governance.navigationDecisionCreated,false);assert.equal(handoff.governance.actionCreated,false);assert.equal(handoff.icrHandoff.owner,'ICR');assert.equal(handoff.rmoHandoff.owner,'RMO');assert.equal(handoff.rdgHandoff.owner,'RDG');assert.equal(stable(result),before,'MIR-9 handoff must not mutate accepted interpretation result');
const easyReading={...reading,outcome:{status:'ANSWER_SUFFICIENT',stop:true,nextQuestions:[],handoff:{target:null,realityJourneyEligible:false,automaticRealityJourney:false,requiresExplicitEscalationConsent:false}}};assert.equal(evaluateRealityEntryEligibility({acceptedInterpretationResult:result,guidedReadingResponse:easyReading,complexityEvaluation:complexity,escalationConsent}).eligible,false);
console.log('✓ KAP → RJX MIR-9 handoff passed: REALITY_MODEL_REQUIRED + KAP W24 YES + exact explicit escalation consent are all required; no case/reality/navigation/action authority is created by the handoff.');
