import assert from 'node:assert/strict';import fs from 'node:fs';const r=p=>JSON.parse(fs.readFileSync(p,'utf8'));const s=r('content/interpretation/research/structural-scale-research-v1.json');assert.equal(s.transitions.length,7);assert.ok(s.transitions.every(x=>x.status==='HYPOTHESIS'));assert.equal(s.invariants.allDoublingIsCanonical,false);const m=r('content/interpretation/research/motion-interaction-matrix-v1.json');assert.equal(m.cells.length,64);assert.ok(m.cells.every(x=>x.derived64SduIdentity===null));const e=r('content/interpretation/structure/eight-motion-registry-v1.json');assert.equal(e.motions.length,8);assert.equal(e.motions.filter(x=>x.status==='HYPOTHESIS_SOURCE_REVIEW_REQUIRED').length,1);console.log('✓ IR-W10–W18 structural research passed: scale hypotheses contained; 8×8 matrix is not SDU identity; unresolved eighth motion fails closed.');
