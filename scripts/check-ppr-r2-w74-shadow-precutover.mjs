import assert from 'node:assert/strict';import fs from 'node:fs';const e=JSON.parse(fs.readFileSync('content/personal-reading/production-cutover/evidence/w74-shadow-precutover-evidence-v1.json','utf8'));assert.equal(e.status,'MACHINE_SHADOW_EVIDENCE_PASS_HUMAN_ACCEPTED');assert.equal(e.caseCount,24);assert.equal(e.metrics.methodCompletionRate,1);assert.equal(e.metrics.claimCompletionRate,1);assert.equal(e.metrics.narrativeBriefCompletionRate,1);assert.equal(e.metrics.verificationPassRate,1);assert.equal(e.metrics.generationErrors,0);assert.equal(e.metrics.cacheHitProxyRate,1);assert.equal(e.metrics.lineagePassRate,1);assert.equal(e.metrics.surfaceSemanticIrPassRate,1);assert.equal(e.customerRendering.dualRenderCompetingReports,false);assert.equal(e.humanGate.hardCutoverAllowed,true);console.log('✓ W74 shadow pre-cutover evidence current: 24/24 machine shadow observations; Human gate is accepted; W76 is eligible.');
