import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const freeze=j('content/professional/method-client-delivery/freeze/mcd-2-canonical-runtime-adapter-freeze-v1.json');const registry=j('content/professional/method-client-delivery/registries/mcd-2-canonical-runtime-adapter-registry-v1.json');const mcd4=j('content/professional/method-production-activation/successors/mpa-w30-mcd4-execution-successor-v1.json');const mcd5=j('content/professional/method-production-activation/successors/mpa-w30-mcd5-projection-successor-v1.json');const current=j('content/professional/method-client-delivery/successors/mcd-ast-bzr-production-adapter-successor-v1.json');const pkg=j('package.json');
assert.equal(freeze.status,'FROZEN_ADAPTER_BINDING_MCD3_MCD4_NOT_ACTIVATED');for(const code of ['AST','BZR','NUM']){const a=registry.entries.find(x=>x.pluginCode===code);assert.equal(a.productionAdapterBindingActive,true);assert.equal(a.mpaDispatchAllowed,true)}assert.equal(registry.entries.find(x=>x.pluginCode==='HDR').productionInvocation,'FORBIDDEN');
const frozenApi=[...freeze.frozenOutputs,...freeze.predecessorEvidence].find(x=>x.path==='functions/api/method-execute.js');assert.equal(frozenApi.sha256,mcd4.authorizedDrift[0].predecessorSha256);assert.equal(mcd4.authorizedDrift[0].successorSha256,mcd5.authorizedDrift[0].predecessorSha256);assert.equal(mcd5.authorizedDrift[0].successorSha256,current.predecessors.api.predecessorSha256);assert.equal(sha('functions/api/method-execute.js'),current.predecessors.api.currentSha256);for(const record of [...freeze.frozenOutputs,...freeze.predecessorEvidence])if(record.path!=='functions/api/method-execute.js')assert.equal(sha(record.path),record.sha256,record.path);
assert.equal(current.authorityBoundary.mpaRemainsOnlyProductionDispatchAuthority,true);assert.equal(pkg.scripts['check:mcd-2'],'node scripts/check-mcd-2-ast-bzr-production-successor.mjs');assert.equal(pkg.scripts['check:mcd-2-historical'],'node scripts/check-mcd-2-canonical-runtime-adapter.mjs');assert.equal(current.authorityBoundary.secondMethodRuntimeCreated,false);assert.equal(current.authorityBoundary.hdrProductionActivationCreated,false);
console.log('✓ MCD-2 AST+BZR physical adapter successor reconciliation passed.');console.log('  Historical adapter freeze is preserved; API lineage advances MCD-4 → MCD-5 → current AST/BZR Production adapter successor without second authority.');
