import {assert,P,read} from './figure/figure-check-lib.mjs';const g=read(P.grammar),s=read(P.style),rt=read(P.runtime);assert.equal(g.rules.deterministic,true);assert.equal(g.rules.rendererMayInferNodes,false);assert.equal(g.rules.rendererMayInferEdges,false);assert.equal(rt.productionRules.generativeProductionStylingAllowed,false);assert.equal(s.canvas.width,2000);assert.equal(s.canvas.height,1200);console.log('✓ FIG layout checker: deterministic multi-role grammar + fixed style tokens frozen; generative styling disabled.');
