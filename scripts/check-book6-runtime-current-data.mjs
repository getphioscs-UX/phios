import fs from 'node:fs';import assert from 'node:assert/strict';const d=JSON.parse(fs.readFileSync('content/civilization-atlas/reconfiguration/dossiers/contemporary-runtime-dossiers-v1.json','utf8')).dossiers;for(const x of d){assert.ok(x.asOfDate);assert.ok(x.lastReviewedAt);assert.ok(['CANONICAL_HISTORY','CURRENT_DATA','DERIVED_RUNTIME_READOUT','CONDITIONAL_PROJECTION','UNKNOWN'].includes(x.dataClass));if(x.dataClass==='CURRENT_DATA')assert.ok(x.sourceDate&&x.sourceFreshness!=='UNVERIFIED');}console.log('✓ Book VI current-data boundary');