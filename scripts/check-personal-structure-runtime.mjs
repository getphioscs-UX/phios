import assert from 'node:assert/strict';
import {GATE_SEQUENCE,gateWheelAngleToEclipticLongitude} from '../functions/method-runtime/personal-structure/gate-wheel.js';
import {calculatePersonalStructure} from '../functions/method-runtime/personal-structure/personal-structure-runtime.js';
import {projectPersonalStructure} from '../functions/method-runtime/personal-structure/personal-structure-projection-runtime.js';
function lonForGate(gate,line=1,pos=.1){const i=GATE_SEQUENCE.indexOf(gate);assert.ok(i>=0);return gateWheelAngleToEclipticLongitude(i*5.625+(line-1)*.9375+pos);}
function astro(sun, north, others, instantUTC){const longitudes={SUN:sun,EARTH:(sun+180)%360,NORTH_NODE:north,SOUTH_NODE:(north+180)%360,...others};return {instantUTC,longitudes,astronomyRef:'TEST_UPSTREAM_CANONICAL_ASTRONOMY'};}
const personality=astro(232.75,lonForGate(2),{MOON:lonForGate(1),MERCURY:lonForGate(8),VENUS:lonForGate(14),MARS:lonForGate(3),JUPITER:lonForGate(60),SATURN:lonForGate(21),URANUS:lonForGate(45),NEPTUNE:lonForGate(17),PLUTO:lonForGate(62)},'1989-11-15T14:50:00Z');
const design=astro(144.75,lonForGate(41),{MOON:lonForGate(46),MERCURY:lonForGate(29),VENUS:lonForGate(35),MARS:lonForGate(36),JUPITER:lonForGate(18),SATURN:lonForGate(58),URANUS:lonForGate(25),NEPTUNE:lonForGate(51),PLUTO:lonForGate(40)},'1989-08-18T04:38:04Z');
const canonicalInput={birthDate:'1989-11-15',birthTime:'22:50:00',birthPlace:{displayName:'Taiping',countryCode:'MY',latitude:4.85,longitude:100.74},timezone:{iana:'Asia/Kuala_Lumpur',utcOffsetAtBirth:'+08:00',source:'PINNED_IANA_TZDB',confidence:'HIGH'},timeAccuracy:'EXACT',locale:'zh-Hans',consent:{},inputVersion:'MCD-3-CANONICAL-BIRTH-INPUT-v1.0.0'};
const record=(recordId,recordType,payload)=>({authority:'SHARED_DATA_AUTHORITY',status:'verified',methodOwner:null,pluginOwner:null,recordId,recordType,recordVersion:'MIR-3-TEST-v1',payload});
const records=[record('IN','CANONICAL_BIRTH_INPUT',canonicalInput),record('PA','PERSONALITY_ASTRONOMY',personality),record('DA','DESIGN_ASTRONOMY',design),record('DM','DESIGN_MOMENT',{designMomentRef:'DM',designInstantUTC:'1989-08-18T04:38:04Z',personalitySunLongitude:232.75,designSunLongitude:144.75,solarArcDeg:88,solverTolerance:1e-7,iterationCount:17,fixedDaySubtractionUsed:false,lineage:{solver:'HDR_DESIGN_MOMENT_RUNTIME'}}),record('CONSENT','CONSENT',{valid:true})];
const full=await calculatePersonalStructure({calculationId:'MIR3-PS-FULL',inputRecords:records,nodeConvention:'TRUE_NODE.V1'});assert.equal(full.runtimeCode,'SHARED_CALCULATION_RUNTIME');assert.equal(full.output.activations.length,26);assert.equal(full.output.activations.every(a=>a.status==='CALCULATED'),true);assert.equal(full.output.profile.profileCode,'5/1');assert.equal(full.output.incarnation.configurationCode,'43/23|29/30');assert.equal(full.output.capabilityReadiness.eligible,true);assert.equal(full.output.publicBrandedMethodCreated,false);const projections=await projectPersonalStructure(full);assert.equal(projections.projections.length,5);assert.equal(projections.publicVocabulary,'PHI_OS_ONLY');assert.equal(projections.legacyBrandedSurface,false);
const partial=await calculatePersonalStructure({calculationId:'MIR3-PS-PARTIAL',inputRecords:records,nodeConvention:'PHI_OS_AST_NODE_NONE_V1'});assert.equal(partial.output.activations.filter(a=>a.status==='UNKNOWN').length,4);assert.equal(partial.output.activations.filter(a=>a.status==='CALCULATED').length,22);assert.equal(partial.output.capabilityReadiness.states.ASTRONOMY_READY,false);assert.equal(partial.output.capabilityReadiness.eligible,false);assert.equal(partial.output.capabilityReadiness.blockedBySourceOrTrademark,false);
console.log('✓ Personal Structure Runtime passed: Shared Calculation → capability-gated structural result → Shared Projection; 26/full when node authority exists, 22+4 UNKNOWN under current AST node-none policy.');
