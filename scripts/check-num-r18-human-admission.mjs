import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');const p=path.join(ROOT,'content/professional/num-production/expansion-r9-r18/review/num-r18-human-review-results-v1.json');const r=JSON.parse(fs.readFileSync(p,'utf8'));const criteria=['schoolSeparation','formulaFidelity','sourceEvidenceTraceable','unresolvedFormulaFailClosed','noTraitOverreach','noFortuneTelling','customerClarity','dedupCorrect'];
assert.equal(r.caseCount,18);const ok=r.status==='HUMAN_ADMITTED'&&r.accepted===18&&r.revised===0&&r.rejected===0&&r.pending===0&&r.results.every(x=>x.decision==='ACCEPT'&&criteria.every(k=>x.criteria?.[k]===true));if(!ok){const e=new Error(`NUM_R18_HUMAN_ADMISSION_PENDING accepted=${r.accepted} pending=${r.pending}`);e.code='NUM_R18_HUMAN_ADMISSION_PENDING';throw e}console.log('✓ NUM-R18 human admission verified: 18/18 accepted with all criteria true.');
