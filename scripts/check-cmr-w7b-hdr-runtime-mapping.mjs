import assert from "node:assert/strict";
import crypto from "node:crypto";
import fs from "node:fs/promises";
const readJson=async p=>JSON.parse(await fs.readFile(p,"utf8")); const sha=async p=>crypto.createHash("sha256").update(await fs.readFile(p)).digest("hex");
const r="content/professional/canonical-meaning-runtime/";
const paths={contract:r+"contracts/hdr-runtime-mapping-contract-v1.json",registry:r+"registries/hdr-runtime-mapping-registry-v1.json",readiness:r+"readiness/hdr-runtime-mapping-readiness-v1.json",freeze:r+"freeze/cmr-w7b-hdr-runtime-mapping-freeze-v1.json",codes:r+"registries/canonical-meaning-code-registry-v1.json",global:r+"registries/versioned-method-meaning-mapping-registry-v1.json",w6d:r+"freeze/cmr-w6d-mapping-foundation-freeze-v1.json",projection:"content/professional/core-method-runtime/hdr-projection-runtime-v1.json"};
const [c,g,q,f,codes,global,w6d,p]=await Promise.all(Object.values(paths).map(readJson));
assert.equal(c.work,"CM-W7B"); assert.deepEqual(c.runtimeScopes.map(x=>x.scopeCode),["AUTHORITY","RUNTIME_TYPE","CONNECTIVITY","PROFILE_NUMBER_STRUCTURE"]);
for(const x of ["AUTHORITY_INTERPRETATION","TYPE_STRATEGY_TEXT","PROFILE_DESCRIPTION","ORIGINAL_METHOD_INTERPRETATION_TEXT"]) assert.equal(c.forbiddenContent.includes(x),true);
assert.equal(g.mappingCount,0); assert.deepEqual(g.mappings,[]); assert.equal(g.runtimeScopes.length,4);
assert.equal(q.status,"blocked"); assert.equal(q.readyForMappingPopulation,false); assert.equal(q.exampleShapeOnly.isRegistryRecord,false); assert.equal(q.actualMappingsCreated,false);
assert.deepEqual(codes.meaningCodes,[]); assert.equal(global.mappingCount,0); assert.deepEqual(global.mappings,[]); assert.equal(w6d.status,"frozen");
assert.deepEqual(p.projectionTypes,["GATE","CHANNEL","CENTER","AUTHORITY","PROFILE"]);
for(const output of f.outputs) assert.equal(await sha(output),f.digests[output]);
assert.equal(await sha(paths.w6d),f.protectedDigests.cmrW6D); assert.equal(await sha(paths.codes),f.protectedDigests.meaningCodeRegistry); assert.equal(await sha(paths.global),f.protectedDigests.versionedMappingRegistry);
assert.equal(f.invariants.actualMappingsCreated,false); assert.equal(f.invariants.interpretationTextStored,false);
console.log("✓ CM-W7B HDR Runtime Mapping readiness foundation passed.");
console.log("✓ Authority, Runtime Type, Connectivity and Profile number structure consume Canonical Projection only.");
console.log("✓ No HDR interpretation text or fabricated Meaning Mapping was stored.");
