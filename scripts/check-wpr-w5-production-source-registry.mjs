import assert from 'node:assert/strict'; import {readJson,BASELINE,exists} from './lib/web-production/wpr-foundation-v1.mjs';
const r=readJson('content/web-production/registries/wpr-production-source-registry-v1.json'); assert.equal(r.baselineCommit,BASELINE); for(const c of ['CANONICAL','PUBLISHED','PRODUCTION','SESSION','CUSTOMER_PRIVATE','PROFESSIONAL_PRIVATE']) assert.ok(r.sourceClasses.includes(c)); for(const e of r.entries) { assert.equal(e.sourceAuthorityPreserved,true); assert.ok(exists(e.reference),e.reference); } for(const x of ['DRAFT','CANDIDATE','REVIEW_ONLY','FIXTURE_AS_PRODUCTION']) assert.ok(r.forbiddenDirectSourceStates.includes(x)); assert.equal(r.rules.sourceRegistrationDoesNotPromoteSourceState,true); console.log('✓ WPR-W5 Production Source Registry passed.');
