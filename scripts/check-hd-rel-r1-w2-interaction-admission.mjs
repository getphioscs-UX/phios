import assert from 'node:assert/strict';import fs from 'node:fs';const j=JSON.parse(fs.readFileSync('content/personal-reading/relationship/hd-r1/registries/HD-REL-R1-W2-interaction-class-registry-v1.json','utf8'));assert.equal(j.work,'HD-REL-R1-W2');assert.equal(j.interactionClasses.length,4);assert.deepEqual(j.interactionClasses.map(x=>x.interactionClass),['ELECTROMAGNETIC','DOMINANCE','COMPROMISE','COMPANIONSHIP']);for(const x of j.interactionClasses){assert.equal(x.semanticAdmissionStatus,'SEMANTIC_ADMITTED_SHADOW');assert.equal(x.humanAccepted,false);assert.equal(x.customerPublishable,false);assert.ok(x.structuralCondition);assert.ok(x.customerFrame.zhHans&&x.customerFrame.en);}assert.ok(j.forbiddenTranslations.DOMINANCE.includes('superiority'));assert.ok(j.forbiddenTranslations.COMPANIONSHIP.includes('compatibility proof'));console.log('✓ HD-REL-R1 W2 Electromagnetic / Dominance / Compromise / Companionship semantic admission passed in shadow.');
