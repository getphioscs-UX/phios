import assert from 'node:assert/strict'; import fs from 'node:fs'; import {P,BASE,read,sha,specPath,sourcePath,manifestPath,svgPath,pngPath,webpPath,carPath,ids,renderSvg,geometryFingerprint,stable} from './mfig/mfig-lib.mjs';
const style=read(P.style);assert.equal(style.rules.generativeStylingAllowed,false);assert.equal(style.rules.randomnessAllowed,false);assert.equal(style.canvas.width,1600);assert.equal(style.canvas.height,1000);for(const id of ids()){const sp=read(specPath(id));assert.ok(sp.productionRules.deterministicGeometryRequired);assert.equal(sp.productionRules.generativeProductionStylingAllowed,false);}console.log('✓ MFIG layout checker: deterministic grammar/style tokens frozen; generative production styling disabled.');