import assert from 'node:assert/strict';import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('content/health/health-knowledge/care-navigation/health-care-navigation-registry-v1.json','utf8'));const ids=new Set(x.states);for(const id of ['ROUTINE_SELF_OBSERVATION','ROUTINE_CLINIC_REVIEW','PROMPT_CLINIC_REVIEW','URGENT_EVALUATION','EMERGENCY'])assert.ok(ids.has(id));assert.ok(x.rules.includes('CARE_STATE_IS_NOT_DIAGNOSIS'));console.log('✓ Health care navigation passed.');
