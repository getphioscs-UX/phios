import assert from 'node:assert/strict'; import {readJson,BASELINE,W11,SLOTS} from './lib/web-production/wpr-composition-v1.mjs'; import {resolveWebComposition,WebCompositionResolutionError} from '../assets/js/runtime/web-production/composition-resolver.js';
const c=readJson(W11),slots=readJson(SLOTS);assert.equal(c.baselineCommit,BASELINE);assert.equal(c.work,'WPR-W11');assert.equal(c.authority.wprMayCreateKnowledge,false);assert.equal(c.authority.wprMaySelectMeaning,false);assert.equal(c.bindingContract.componentToSlotInferenceForbidden,true);
const codes=slots.slots.map(x=>x.slotCode);for(const code of ['HERO','PRIMARY_CONTENT','PRIMARY_VISUAL','RELATED','CONTINUITY','CTA'])assert.ok(codes.includes(code),code);
const cpr=readJson('content/professional/canonical-presentation-runtime/registries/canonical-presentation-registry-v1.json');assert.equal(cpr.productionRecords.length,0);
const fixture=readJson('content/web-production/fixtures/wpr-composition.valid.json');const args={surfaceRegistry:readJson('content/web-production/registries/wpr-surface-registry-v1.json'),routeRegistry:readJson('content/web-production/registries/wpr-route-registry-v1.json')};
const resolved=resolveWebComposition({webCompositionRecord:fixture.webCompositionRecord,...args});assert.equal(resolved.renderState,'VALIDATION_RESOLVED');assert.deepEqual(resolved.slots.map(x=>x.slotCode),['HERO','PRIMARY_CONTENT']);
assert.throws(()=>resolveWebComposition({webCompositionRecord:{...fixture.webCompositionRecord,productionMode:'PRODUCTION'},...args}),e=>e instanceof WebCompositionResolutionError&&e.code==='WPR_CPR_PRODUCTION_RECORD_REQUIRED');
console.log('✓ WPR-W11 Canonical Composition Resolver passed.');console.log('✓ WPR executes explicit CPR composition bindings only; production resolution remains blocked while CPR productionRecords are empty.');
