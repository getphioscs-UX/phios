import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';import {spawnSync} from 'node:child_process';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const freeze=j('content/professional/method-client-delivery/freeze/mcd-4-execution-freeze-v1.json');const prior=j('content/professional/method-production-activation/successors/mpa-w30-mcd5-projection-successor-v1.json');const current=j('content/professional/method-client-delivery/successors/mcd-ast-bzr-production-adapter-successor-v1.json');const pkg=j('package.json');assert.equal(freeze.status,'FROZEN_MPA_FIRST_PARTIAL_EXECUTION_HDR_FAIL_CLOSED');for(const record of [...freeze.frozenOutputs,...freeze.predecessorEvidence]){if(record.path==='functions/api/method-execute.js'){assert.equal(record.sha256,prior.authorizedDrift[0].predecessorSha256);assert.equal(prior.authorizedDrift[0].successorSha256,current.predecessors.api.predecessorSha256);assert.equal(sha(record.path),current.predecessors.api.currentSha256)}else assert.equal(sha(record.path),record.sha256,record.path)}assert.equal(sha(current.predecessors.mcd4HistoricalRuntime.path),current.predecessors.mcd4HistoricalRuntime.sha256);assert.equal(pkg.scripts['check:mcd-4'],'node scripts/check-mcd-4-ast-bzr-production-successor.mjs');assert.equal(pkg.scripts['check:mcd-4-historical'],'node scripts/check-mcd-4-execution.mjs && node scripts/check-mcd-4-execution-freeze.mjs');const run=spawnSync(process.execPath,['scripts/check-mcd-ast-bzr-production-successor.mjs'],{cwd:process.cwd(),encoding:'utf8'});assert.equal(run.status,0,`${run.stdout}\n${run.stderr}`);process.stdout.write(run.stdout);console.log('✓ MCD-4 current AST+BZR execution successor passed; historical MCD-4 runtime remains immutable.');
