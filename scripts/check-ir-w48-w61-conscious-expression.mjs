import assert from 'node:assert/strict';import fs from 'node:fs';import {deriveExperienceContext,deriveExpressionContext} from '../functions/interpretation-runtime/domain-derivers-v1.js';const r=p=>JSON.parse(fs.readFileSync(p,'utf8'));assert.equal(r('content/interpretation/conscious/experience-derivation-contract-v1.json').experienceEqualsTotalReality,false);assert.equal(r('content/interpretation/expression/expression-derivation-contract-v1.json').expressionEqualsReality,false);const ex=deriveExperienceContext({carrierRuntime:{},selection:{},stabilization:{},perspective:{},motivation:{}});assert.equal(ex.experienceEqualsTotalReality,false);const out=deriveExpressionContext({experience:ex});assert.equal(out.expressionEqualsReality,false);assert.equal(out.llmUsed,false);console.log('✓ IR-W48–W61 conscious/expression passed: Experience ≠ total Reality and Expression < Reality with no-LLM structured derivation.');
