import assert from 'node:assert/strict';import fs from 'node:fs';import {execFileSync} from 'node:child_process';
execFileSync(process.execPath,['scripts/check-cross-w25-human.mjs'],{stdio:'inherit'});const a=JSON.parse(fs.readFileSync('content/customer-experience-rebuild/r12r4b/cross/acceptance/cross-w26-final-production-admission-v1.json','utf8'));assert.equal(a.status,'PRODUCTION_ACCEPTED');assert.equal(a.productionAllowed,true);assert.equal(a.customerCrossCutoverAllowed,true);assert.equal(a.currentApiMutationApplied,true);console.log('✓ R2-W26 Cross final production admission passed.');
