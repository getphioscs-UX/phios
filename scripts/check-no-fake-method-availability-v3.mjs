import assert from 'node:assert/strict';import fs from 'node:fs';const c=JSON.parse(fs.readFileSync('content/web-production/px2/successors/public-method-catalog-v4.json','utf8'));const t=c.methods.find(x=>x.methodCode==='TAROT');assert.equal(t.capabilityAvailability,'LIMITED');assert.equal(t.runAllowed,false);assert.equal(t.staticCatalogMayGrantRunAllowed,false);assert.equal(t.runtimeAuthorityRequired,true);assert.equal(t.route,'/perspectives/tarot/');console.log('✓ No-fake Method availability v3 passed: canonical route is visible, but only server D1 + exact SHA can grant execution.');
