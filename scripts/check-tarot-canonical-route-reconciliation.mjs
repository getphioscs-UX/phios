import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const routes=fs.readFileSync('_redirects','utf8');assert.match(routes,/\/readings\/symbolic\/? \/perspectives\/ 308/);const c=j('content/web-production/px2/successors/public-method-catalog-v4.json'),t=c.methods.find(x=>x.methodCode==='TAROT');assert.equal(t.route,'/perspectives/tarot/');assert.equal(t.runAllowed,false);assert.equal(t.runtimeAuthorityRequired,true);const h=fs.readFileSync('perspectives/tarot/index.html','utf8'),x=fs.readFileSync('assets/customer-ui/js/surfaces/tarot.js','utf8');assert.match(h,/rel="canonical" href="\/perspectives\/tarot\/"/);assert.match(h,/data-result-layer="YOUR_INPUT"/);assert.match(h,/data-result-layer="POSSIBLE_NEXT_QUESTIONS_ACTIONS"/);assert.match(x,/const method='TAROT'/);assert.match(x,/serverAuthorityOk/);assert.match(x,/tarot-production-status/);assert.doesNotMatch(x,/localStorage|sessionStorage|indexedDB/);console.log('✓ TPA-MR canonical Tarot route reconciliation passed: /perspectives/tarot/ owns product execution; legacy shared symbolic route remains compatibility-only.');
