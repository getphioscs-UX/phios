import assert from 'node:assert/strict';import fs from 'node:fs';const r=JSON.parse(fs.readFileSync('content/personal-reading/relationship/review/rel-w4-human-review-results-v1.json','utf8'));const a=JSON.parse(fs.readFileSync('content/personal-reading/relationship/acceptance/rel-w4-human-admission-v1.json','utf8'));assert.equal(r.results.length,48);assert.equal(r.accepted,48);assert.equal(r.pending,0);assert.equal(r.results.every(x=>x.decision==='ACCEPT'&&Object.values(x.criteria).every(Boolean)),true);assert.equal(a.status,'HUMAN_ADMITTED_AST_NUM');assert.equal(a.authorityEffect.relationshipProductCustomerPublicationAllowed,false);console.log('✓ REL-W4 Human admission passed: 48/48 ACCEPT; AST + NUM method-level W4 wording admitted; full relationship product remains closed.');