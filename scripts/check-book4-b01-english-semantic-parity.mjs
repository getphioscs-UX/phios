import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';
const read=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const a3=read('content/knowledge/production-planning/plans/book4-a3-final-article-map-v1.json');const human=read('content/knowledge/production-planning/acceptance/book4-pja-wave1-human-acceptance-v1.json');const parity=read('content/knowledge/production-planning/acceptance/book4-b01-english-semantic-parity-v1.json');const plans=new Map(a3.articles.filter(x=>x.batchCode==='B4-B01').map(x=>[x.articlePlanId,x]));assert.equal(human.acceptedArticleCount,8);assert.equal(parity.status,'MACHINE_SEMANTIC_PARITY_ACCEPTED_8_OF_8');assert.equal(parity.records.length,8);for(const r of parity.records){const p=plans.get(r.articlePlanId);assert.ok(p);assert.equal(sha(r.zhHansPath),r.zhHansSha256);assert.equal(sha(r.enPath),r.enSha256);const zh=read(r.zhHansPath),en=read(r.enPath);assert.equal(en.article.title,p.workingTitleEn);assert.deepEqual(en.sourceBindings,zh.sourceBindings);assert.deepEqual(en.canonicalNodeBinding,zh.canonicalNodeBinding);assert.deepEqual(en.blocks.map(x=>x.type),zh.blocks.map(x=>x.type));assert.ok(en.blocks.filter(x=>x.type==='paragraph').length>=5)}console.log('✓ BOOK IV B01 historical English parity reconciled: 8/8 machine semantic parity accepted.');console.log('✓ Original Wave 1 human-acceptance evidence and English prose remain unchanged; publication remains closed.');
