import assert from 'node:assert/strict';import {BASELINE,readJson,readText,exists} from './lib/web-production/wpr-professional-report-v1.mjs';
const c=readJson('content/web-production/contracts/wpr-professional-financial-production-v1.json');assert.equal(c.baselineCommit,BASELINE);assert.equal(c.work,'WPR-W22');assert.equal(c.status,'ACTIVE_LIMITED_PRODUCTION_PR_HUMAN_AUTHORITY_GATED_FINANCIAL_PRODUCT_NEUTRAL');for(const v of Object.values(c.nonAuthority))assert.equal(v,false);
const pr=readJson('content/runtime/professional-runtime/freeze/pr-v2-freeze-v1.json');assert.equal(pr.status,'PR-v2.0.0-FROZEN');assert.equal(pr.productionState,'GOVERNED_PROFESSIONAL_RUNTIME_READY_HUMAN_AUTHORITY_GATED');assert.equal(pr.authorityClosure.professionalJudgmentAuthority,'PR_ONLY_HUMAN_ATTRIBUTABLE');assert.equal(pr.nonAuthority.prCreatesReport,false);
const pab=readJson('content/runtime/professional-runtime/contracts/professional-authority-boundary-v2.json');assert.equal(pab.rules.onlyPrW4MayCreateProfessionalJudgment,true);assert.equal(pab.rules.professionalJudgmentRequiresHumanAttribution,true);assert.equal(pab.rules.rrRemainsReportAssemblyAuthority,true);
const fin=readJson('content/registry/financial-authority-boundary.json');assert.equal(fin.rules.productSpecificAdviceEnabled,false);assert.equal(fin.rules.outOfScopeSignOffAllowed,false);const pub=readJson('content/registry/m4a-w6-financial-public-pages.json');assert.equal(pub.route,'/professional/financial');assert.equal(pub.boundaries.checkout_enabled,false);assert.equal(pub.boundaries.future_financial_outcome_guaranteed,false);
const projection=readJson('content/web-production/registries/wpr-professional-financial-projection-registry-v1.json');assert.equal(projection.baselineCommit,BASELINE);assert.equal(projection.entries.length,2);assert.ok(projection.entries.every(x=>x.projectionState==='LIMITED_PRODUCTION'));assert.equal(projection.rules.projectionIsNotProfessionalJudgment,true);
const surfaces=readJson('content/web-production/registries/wpr-surface-registry-v1.json');assert.equal(surfaces.entries.find(x=>x.surfaceCode==='FINANCIAL').implementationState,'LIMITED_PRODUCTION_DISCOVERY');const routes=readJson('content/web-production/registries/wpr-route-registry-v1.json');assert.equal(routes.entries.find(x=>x.routeCode==='FINANCIAL').implementationState,'EXISTING');
const sources=readJson('content/web-production/registries/wpr-production-source-registry-v1.json');for(const code of ['PR','FINANCIAL_RUNTIME'])assert.ok(sources.entries.some(x=>x.sourceCode===code),code);assert.equal(sources.entries.find(x=>x.sourceCode==='PR').professionalJudgmentAuthorityMayBeInferredByWpr,false);assert.equal(sources.entries.find(x=>x.sourceCode==='FINANCIAL_RUNTIME').recommendationAuthorityMayBeInferredByWpr,false);
const consumption=readJson('content/web-production/registries/wpr-runtime-consumption-registry-v1.json');assert.equal(consumption.entries.find(x=>x.runtimeCode==='PR').professionalJudgmentCreatedByWpr,false);assert.equal(consumption.entries.find(x=>x.runtimeCode==='FINANCIAL_RUNTIME').financialCalculationExecutedByWpr,false);
const web=readJson('content/web-production/registries/canonical-web-production-registry-v1.json');const records=web.productionRecords.filter(x=>x.lineage?.wprWork==='WPR-W22');assert.equal(records.length,4);assert.deepEqual([...new Set(records.map(x=>x.locale))].sort(),['en','zh-Hans']);assert.ok(records.every(x=>x.productionState==='LIMITED_PRODUCTION'));assert.ok(records.filter(x=>x.surfaceCode==='FINANCIAL').every(x=>x.renderPolicy.financialCalculationExecutionAllowed===false));
const html=readText('professional/financial/index.html');for(const m of ['data-wpr-production-surface="FINANCIAL"','/assets/css/wpr-professional-production.css','data-wpr-professional-financial-authority','/assets/js/pages/wpr-professional-financial-production.js'])assert.ok(html.includes(m),m);const controller=readText('assets/js/pages/wpr-professional-financial-production.js');assert.ok(controller.includes('pr-v2-freeze-v1.json'));assert.ok(controller.includes('financial-authority-boundary.json'));for(const bad of ['localStorage','sessionStorage','method: \'POST\'','/api/'])assert.equal(controller.includes(bad),false,bad);
const services=readText('services.html');assert.ok(services.includes('/professional/financial'));const audit=readJson('content/web-production/audits/wpr-w22-professional-financial-authority-audit-v1.json');for(const v of Object.values(audit.nonActivation))assert.equal(v,false);const a=readJson('content/web-production/acceptance/wpr-w22-professional-financial-acceptance-v1.json');assert.equal(a.baselineCommit,BASELINE);for(const v of Object.values(a.nonActivation))assert.equal(v,false);
for(const f of ['content/web-production/composition/professional/professional-financial-composition-v1.json','docs/runtime/WPR-F-W22-W23-PROFESSIONAL-FINANCIAL-REPORT-SURFACES.md'])assert.ok(exists(f),f);
const pkg=readJson('package.json');assert.equal(pkg.scripts['check:wpr-w22'],'node scripts/check-wpr-w22-professional-financial-production.mjs');assert.equal(pkg.scripts['check:wpr-professional-financial'],'npm run check:wpr-w22');assert.ok(pkg.scripts['check:wpr'].includes('npm run check:wpr-professional-financial'));assert.equal(pkg.scripts.postcheck.includes('check:wpr'),false);
console.log('✓ WPR-W22 Professional / Financial Production Surface passed.');console.log('  PR human judgment authority and financial product-neutral boundaries remain upstream; WPR projects discovery only.');
