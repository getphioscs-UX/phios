import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const dirs=['audit','governance','source','figure','grammar','research','structure','projection','meaning','runtime','carrier','conscious','expression','agency','identity','continuity','integration','map','review','release'];for(const d of dirs)assert.ok(fs.statSync(path.join('content/interpretation',d)).isDirectory(),`missing ${d}`);const manifest=JSON.parse(fs.readFileSync('content/interpretation/governance/interpretation-registry-manifest-v1.json','utf8'));assert.equal(manifest.rules.randomJsonReadsByKernelAllowed,false);for(const rel of Object.values(manifest.registries)){assert.ok(rel.startsWith('content/interpretation/'));assert.ok(fs.existsSync(rel),`missing ${rel}`);}const files=fs.readdirSync('functions/interpretation-runtime').filter(x=>x.endsWith('.js'));for(const f of files){if(f==='registry-loader.js')continue;const src=fs.readFileSync(path.join('functions/interpretation-runtime',f),'utf8');assert.equal(/readFileSync|readFile\(|createReadStream/.test(src),false,`${f} reads registry JSON directly`);}console.log('✓ MIR-5 directory + single registry loader foundation passed.');
