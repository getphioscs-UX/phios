import assert from 'node:assert/strict';import fs from 'node:fs';const json=p=>JSON.parse(fs.readFileSync(p,'utf8'));const base='content/civilization-atlas/reconfiguration/';const manifest=json(base+'atlas-manifest-v2.json'),sections=json(base+'book-vi-sections-v1.json'),cases=json(base+'reconfiguration-case-registry-v1.json'),windows=json(base+'reconfiguration-windows-v1.json'),snapshots=json(base+'world-reconfiguration-snapshots-v1.json'),dossiers=json(base+'contemporary-runtime-dossiers-v1.json'),lived=json(base+'lived-reality-dimensions-v1.json'),rels=json(base+'book-vi-atlas-relationships-v2.json'),visuals=json(base+'book-vi-visual-asset-status-v1.json');assert.equal(manifest.bookCode,'BOOK-6');assert.equal(manifest.partCode,'PART-13');assert.equal(manifest.chapterRange,'13.1–13.85');assert.equal(manifest.architectureBoundary.newParallelRuntime,false);assert.equal(sections.sections.length,85);assert.equal(new Set(sections.sections.map(x=>x.id)).size,85);assert.equal(sections.sections.at(-1).id,'13.85');assert.equal(cases.cases.length,60);assert.equal(new Set(cases.cases.map(x=>x.id)).size,60);assert.equal(windows.windows.length,24);assert(windows.windows.every(x=>x.startYear<=x.endYear));assert(windows.windows.every(x=>x.version&&x.createdAt&&x.updatedAt&&Object.hasOwn(x,'supersedes')&&Object.hasOwn(x,'supersededBy')));assert.equal(snapshots.snapshots.length,12);assert.equal(new Set(snapshots.snapshots.map(x=>x.id)).size,12);assert(snapshots.snapshots.every(x=>x.version&&x.createdAt&&x.updatedAt&&Object.hasOwn(x,'supersedes')&&Object.hasOwn(x,'supersededBy')));for(const s of snapshots.snapshots){assert.equal(s.asset.assetRole,'STATIC_ATLAS_BASE_VISUAL');assert.equal(s.asset.htmlOverlayRequired,true);assert.equal(s.asset.dynamicDataEmbedded,false);assert.equal(s.asset.legendEmbedded,false);assert.equal(s.asset.currentDataEmbedded,false);}assert.equal(snapshots.snapshots.find(x=>x.year===2026).asset.resolutionStatus,'MISSING');assert.equal(lived.dimensions.length,14);assert(lived.dimensions.every(x=>x.scoreAllowed===false));assert.equal(dossiers.dossiers.length,12);assert(dossiers.dossiers.every(x=>x.version&&x.createdAt&&x.updatedAt&&Object.hasOwn(x,'supersedes')&&Object.hasOwn(x,'supersededBy')));for(const d of dossiers.dossiers){if(d.dataClass==='CURRENT_DATA')assert(d.asOfDate&&d.sourceDate&&d.lastReviewedAt);assert.notEqual(d.status,'CURRENT_VERIFIED');}assert.equal(rels.relationships.length,85);assert.equal(visuals.expectedCoreAssets,23);assert.deepEqual(visuals.snapshotSummary,{expected:12,present:11,missing:1,missingAssetId:'WORLD_RECONFIGURATION_SNAPSHOT_2026'});const banned=/\b(best country|worst country|best civilization|advanced civilization|backward civilization|country score|civilization score|winner|loser|happiest country)\b/i;for(const path of ['reconfiguration-case-registry-v1.json','contemporary-runtime-dossiers-v1.json','lived-reality-dimensions-v1.json'])assert(!banned.test(fs.readFileSync(base+path,'utf8')),path);const ui=fs.readFileSync('assets/js/pages/civilization-atlas/reconfiguration-renderer.js','utf8');assert.match(ui,/BOOK_SECTION/);assert.match(ui,/LIVED_REALITY/);assert.match(ui,/dossierCompare/);assert.match(ui,/slice\(0,4\)/);console.log('✓ Book VI Reconfiguration Atlas structure PASS: 85 sections, 60 cases, 24 windows, 12 snapshot records, 12 dossier targets, 14 lived-reality dimensions, unified search and 2–4 comparison surfaces.');console.log('  Snapshot R2 evidence state: 11 PRESENT by owner evidence; WORLD_RECONFIGURATION_SNAPSHOT_2026 = MISSING.');console.log('  Browser acceptance NOT_RUN; human acceptance PENDING.');
