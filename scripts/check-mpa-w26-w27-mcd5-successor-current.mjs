import assert from 'node:assert/strict';import fs from 'node:fs';const json=p=>JSON.parse(fs.readFileSync(p,'utf8'));const base='content/professional/method-production-activation';const eligibility=json(`${base}/contracts/mpa-production-eligibility-decision-v1.json`);const execution=json(`${base}/contracts/mpa-production-execution-gate-v1.json`);const mcd1=json(`${base}/successors/mpa-mcd-1-production-authority-successor-v1.json`);const mcd5=json(`${base}/successors/mpa-w30-mcd5-projection-successor-v1.json`);const projectionSource=fs.readFileSync('functions/method-client-delivery/canonical-projection-runtime.js','utf8');const executionSource=fs.readFileSync('functions/method-client-delivery/execution-runtime.js','utf8');assert.equal(eligibility.status,'ACTIVE_CANONICAL_ELIGIBILITY_AUTHORITY_FAIL_CLOSED');assert.equal(execution.status,'ACTIVE_FAIL_CLOSED_PRODUCTION_EXECUTION_GATE');for(const code of ['AST','BZR','NUM'])assert.equal(mcd1.methods.find(x=>x.pluginCode===code).dispatchAllowed,true);assert.equal(mcd1.methods.find(x=>x.pluginCode==='HDR').dispatchAllowed,false);assert.equal(mcd5.projectionBoundary.mpaEvaluatedBeforeProjection,true);assert.equal(mcd5.projectionBoundary.hdrProductionClientProjectionAllowed,false);assert(projectionSource.includes('executeMcd4Request'));assert(executionSource.includes('getMcd1MpaSuccessorDecision'));assert(executionSource.indexOf('getMcd1MpaSuccessorDecision')<executionSource.indexOf('validateCanonicalBirthInput'));console.log('✓ MPA-W26/W27 Current → MCD-5 projection-gate successor passed.');console.log('  MPA remains prior to input, Core execution and canonical projection; HDR cannot reach a Production projection.');
