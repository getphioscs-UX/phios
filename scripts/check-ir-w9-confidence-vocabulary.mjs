import assert from 'node:assert/strict';import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('content/interpretation/governance/interpretation-confidence-vocabulary-v1.json','utf8'));assert.deepEqual(x.classes,['Observed','Calculated','Derived','Inferred','Projected','Interpreted','Hypothesized','Unknown']);assert.equal(x.invariants.HypothesizedEqualsFact,false);assert.equal(x.invariants.UnknownMayBeFilledByAI,false);console.log('✓ IR-W9 interpretation confidence vocabulary passed: Hypothesized ≠ Fact, Unknown remains explicit.');
