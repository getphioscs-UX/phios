import assert from 'node:assert/strict';import fs from 'node:fs';const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const s=j('content/health/health-reflective/symptoms/health-reflective-symptom-registry-v1.json'),b=j('content/health/health-reflective/body-areas/health-reflective-body-area-registry-v1.json');const all=[...s.entries,...b.entries];assert.ok(all.length>=20);assert.ok(all.every(x=>x.claimType==='SOURCE_ATTRIBUTED_SYMBOLIC'&&x.medicalAuthority===false&&x.causalityAllowed===false));console.log(`✓ Health reflective symbol registry passed: ${all.length} source-attributed entries.`);
