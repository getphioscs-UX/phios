import assert from 'node:assert/strict';
import crypto from 'node:crypto';
import fs from 'node:fs';
const read=p=>JSON.parse(fs.readFileSync(p,'utf8')); const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const BASE='3eea58913bb7188089a2912ba6de1a0a5bf91bf0'; const PROD='content/knowledge/production-planning/visual-production/book3-b03-a17-pre-collapse-window-map-v1.json', REVIEW='content/knowledge/production-planning/review/book3-b03-a17-pre-collapse-window-map-visual-review-v1.json', MAN='content/knowledge/production-planning/production/book3-b03/manifest-v1.json';
const prod=read(PROD), review=read(REVIEW), man=read(MAN); assert.equal(prod.baselineCommit,BASE); assert.equal(prod.status,'ASSET_PRODUCED_MACHINE_BOUND_HUMAN_VISUAL_REVIEW_PENDING_PUBLICATION_REQUIREMENT_NOT_YET_RESOLVED'); assert.equal(prod.articlePlanId,'B3-ART-017'); assert.deepEqual(prod.canonicalNodeCodes,['KN-B3-P8-121','KN-B3-P8-122']); const a=prod.productionAsset; assert.ok(fs.existsSync(a.path)); assert.equal(sha(a.path),a.sha256); assert.equal(fs.statSync(a.path).size,a.byteLength); const magic=fs.readFileSync(a.path).subarray(0,12); assert.equal(magic.subarray(0,4).toString(),'RIFF'); assert.equal(magic.subarray(8,12).toString(),'WEBP'); assert.equal(review.status,'PENDING_HUMAN_VISUAL_REVIEW'); assert.equal(review.decision,'PENDING'); assert.equal(review.reviewedAssetSha256,a.sha256); assert.equal(prod.reviewGate.machineAssetBinding,'ACCEPTED'); assert.equal(prod.reviewGate.humanVisualApproval,'PENDING'); assert.equal(prod.reviewGate.publicationBlockingRequirementProduced,true); assert.equal(prod.reviewGate.publicationBlockingRequirementResolved,false); assert.equal(man.requiredVisualProductionPath,PROD); assert.equal(man.visualReviewPackagePath,REVIEW); assert.equal(man.gates.visualProduction,'W10_DISPOSITION_COMPLETE_A17_REQUIRED_ASSET_MACHINE_BOUND_HUMAN_VISUAL_PENDING_PUBLICATION_BLOCKED_FOR_A17');
console.log('✓ BOOK-3 B03 A17 required Pre-Collapse Window Map passed: asset produced, machine-bound, and awaiting explicit human visual acceptance.');
console.log('✓ A17 publication remains blocked until visual human acceptance; no publication or customer-projection authority has been created.');
