import fs from 'node:fs';
import {METHODS,METHOD_LABELS,METHOD_SLUGS,diversityCases,buildDiversityCase,selectW19Cases,W18_BASELINE_COMMIT} from './smr-diversity-support.mjs';
const campaign=JSON.parse(fs.readFileSync('content/customer-experience-rebuild/r12r4b/smr/campaign/smr-structural-diversity-campaign-v1.json','utf8'));
const selectedRows=selectW19Cases(campaign.cases);
const criteria=['ADAPTER_FIDELITY','PRIORITY_INHERITANCE','THEME_COMPOSITION','DEDUP','INFORMATION_GAIN','LAYOUT','READABILITY','CUSTOMER_VALUE','LINEAGE'];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const caseLookup=new Map();for(const m of METHODS)for(const c of diversityCases[m])caseLookup.set(c.caseId,{methodId:m,c});
fs.mkdirSync('content/customer-experience-rebuild/r12r4b/smr/review/w19-cases',{recursive:true});
const cases=[];
function renderReport(methodId,b,row){const sections=b.ia.sections.filter(s=>s.eligibility==='SECTION_ELIGIBLE');return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(row.caseId)} · ${esc(METHOD_LABELS[methodId])}</title><style>
:root{--ink:#181817;--muted:#6c6a63;--line:#ded9cf;--paper:#faf9f5;--card:#fff;--soft:#f2efe8}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:16px/1.58 system-ui,-apple-system,"Segoe UI",sans-serif}.shell{max-width:980px;margin:auto;padding:32px 22px 70px}.eyebrow{font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:var(--muted)}h1,h2{font-family:Georgia,serif;font-weight:500}h1{font-size:clamp(34px,5vw,56px);line-height:1.05;margin:.25em 0}.lead{font-size:19px;max-width:760px}.meta{display:flex;gap:8px;flex-wrap:wrap}.badge{border:1px solid var(--line);border-radius:999px;padding:4px 9px;font-size:12px;background:#fff}.section,.panel,details{background:var(--card);border:1px solid var(--line);border-radius:18px;margin:18px 0}.panel,.section{padding:22px}.first{display:grid;grid-template-columns:1fr 280px;gap:18px}.item{border-top:1px solid #eee9df;padding:14px 0}.item:first-child{border-top:0}.source{font-size:11px;color:var(--muted);word-break:break-all}summary{padding:18px 22px;cursor:pointer;font-weight:650}.detail{padding:0 22px 22px}.boundary{padding:16px 18px;background:var(--soft);border-left:3px solid #87837a}.machine{display:grid;grid-template-columns:repeat(auto-fit,minmax(160px,1fr));gap:10px}.metric{border:1px solid var(--line);border-radius:12px;padding:12px}.metric strong{font-size:22px}@media(max-width:720px){.first{grid-template-columns:1fr}.shell{padding:18px 12px 48px}}@media print{body{background:#fff}.shell{max-width:none;padding:0}.section,.panel,details{break-inside:avoid}}
</style></head><body><main class="shell"><header><div class="eyebrow">PHI OS · SMR-R2 · W19 human review case</div><h1>${esc(METHOD_LABELS[methodId])}</h1><p class="lead">${esc(b.methodResult.summary)}</p><div class="meta"><span class="badge">${esc(row.caseId)}</span><span class="badge">${esc(b.priority.claims[0]?.questionRelevance?.intentId||'OPEN')}</span><span class="badge">Not live customer</span></div></header><section class="first"><div class="panel"><div class="eyebrow">First screen</div>${b.layout.firstScreen.blocks.map(block=>{if(block.slot==='READING_TITLE')return `<p><strong>${esc(METHOD_LABELS[methodId])} governed reading</strong></p>`;if(block.slot==='EXPLORE_FULL_READING_CTA')return `<p>Continue into the full reading ↓</p>`;const refs=new Set(block.sourceRefs||[]);return sections.flatMap(s=>s.items).filter(i=>refs.has(i.sourceRef)).map(i=>`<p>${esc(i.text||'')}</p>`).join('')}).join('')}</div><aside class="panel"><div class="eyebrow">Question</div><p><strong>${esc(caseLookup.get(row.caseId).c.customerIntent.prompt)}</strong></p><p>Question relevance may change priority. It cannot create method meaning.</p></aside></section>${sections.map((s,i)=>`<section class="section"><div class="eyebrow">${String(i+1).padStart(2,'0')} · ${esc(s.sectionId.replaceAll('_',' '))}</div><h2>${esc(s.customerLabel)}</h2>${s.items.map(x=>`<article class="item"><p>${esc(x.text||x.itemType)}</p><div class="source">${esc(x.sourceRef||x.claimRef||'')}</div></article>`).join('')}</section>`).join('')}<details><summary>Why this reading / lineage</summary><div class="detail"><p><strong>Reading authority:</strong> <code>${esc(b.envelope.readingAuthorityRef)}</code></p><p><strong>Interpretation units:</strong> ${b.envelope.interpretationUnitRefs.map(x=>`<code>${esc(x)}</code>`).join(' · ')}</p><p><strong>Rule lineage:</strong> ${b.envelope.ruleLineage.map(x=>`<code>${esc(x)}</code>`).join(' · ')}</p></div></details><section class="section"><div class="eyebrow">Machine integration evidence</div><div class="machine"><div class="metric"><strong>${row.counts.claims}</strong><br>claims</div><div class="metric"><strong>${row.counts.themes}</strong><br>themes</div><div class="metric"><strong>${row.counts.eligibleSections}</strong><br>eligible sections</div><div class="metric"><strong>${row.counts.renderedDuplicateTexts}</strong><br>duplicate texts</div></div></section><p class="boundary"><strong>Boundary.</strong> This case is selected from the 40-case structural diversity campaign. Machine evidence does not substitute W19 human review. Raw projection and layout do not create customer meaning.</p></main></body></html>`}
for(const row of selectedRows){const found=caseLookup.get(row.caseId);const b=await buildDiversityCase(found.methodId,found.c);const filename=`${METHOD_SLUGS[found.methodId]}-${row.caseId.toLowerCase()}.html`;fs.writeFileSync(`content/customer-experience-rebuild/r12r4b/smr/review/w19-cases/${filename}`,renderReport(found.methodId,b,row));cases.push({caseId:row.caseId,methodId:found.methodId,sourceCampaignCaseId:row.caseId,report:`w19-cases/${filename}`,inputDigest:row.inputDigest,nativeStructureSignature:row.nativeStructureSignature,priorityProfileSignature:row.priorityProfileSignature,themeDistributionSignature:row.themeDistributionSignature,supportTensionSignature:row.supportTensionSignature,sectionEligibilitySignature:row.sectionEligibilitySignature,machineEvidence:{renderedDuplicateTexts:row.counts.renderedDuplicateTexts,firstScreenBlocks:row.counts.firstScreenBlocks,firstScreenThemes:row.counts.firstScreenThemes},humanCriteria:Object.fromEntries(criteria.map(k=>[k,'PENDING'])),decision:'PENDING'})}
const reviewCases={schemaVersion:'PHI-OS-SMR-R2-W19-HUMAN-REVIEW-CASES-v1.0.0',baselineCommit:W18_BASELINE_COMMIT,workCode:'R2-W19',required:{total:20,perMethod:{AST:4,BZR:4,ZWR:4,NUM:4,ECR:4},criteria},selectionRule:'FOUR_CASES_PER_METHOD_FROM_W18_DIVERSITY_SET_WITH_DISTINCT_INPUTS_AND_DIVERSE_PROFILES',cases,governance:{machinePassDoesNotEqualHumanPass:true,liveCustomerIndividuallyHumanReviewed:false,rendererCreatesMeaning:false},next:'R2-W19 HUMAN DECISION'};
const results={schemaVersion:'PHI-OS-SMR-R2-W19-HUMAN-REVIEW-RESULTS-v1.0.0',baselineCommit:W18_BASELINE_COMMIT,workCode:'R2-W19',status:'PENDING_HUMAN_REVIEW',results:cases.map(c=>({caseId:c.caseId,methodId:c.methodId,criteria:Object.fromEntries(criteria.map(k=>[k,'PENDING'])),decision:'PENDING',reviewerNote:null})),summary:{accepted:0,rejected:0,pending:20},productionCutoverAllowed:false,next:'R2-W20 BLOCKED UNTIL 20/20 HUMAN ACCEPTED'};
fs.writeFileSync('content/customer-experience-rebuild/r12r4b/smr/review/smr-w19-human-review-cases-v1.json',JSON.stringify(reviewCases,null,2)+'\n');
fs.writeFileSync('content/customer-experience-rebuild/r12r4b/smr/review/smr-w19-human-review-results-v1.json',JSON.stringify(results,null,2)+'\n');
const cards=cases.map((c,i)=>`<section class="case" data-case="${esc(c.caseId)}"><div class="case-head"><div><span>${String(i+1).padStart(2,'0')} · ${esc(c.methodId)}</span><h2>${esc(c.caseId)}</h2></div><a href="${esc(c.report)}" target="_blank">Open full case ↗</a></div><div class="criteria">${criteria.map(k=>`<label><input type="checkbox" data-criterion="${esc(k)}"> ${esc(k.replaceAll('_',' '))}</label>`).join('')}</div><div class="decision"><button data-decision="ACCEPTED">Mark case accepted</button><button data-decision="REJECTED">Reject</button><span class="state">PENDING</span></div></section>`).join('');
const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>SMR-R2 W19 · 20-case Human Review</title><style>:root{--ink:#191918;--muted:#716f68;--line:#ded9cf;--paper:#faf9f5;--card:#fff;--good:#245d40;--bad:#8a2f2f}*{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:15px/1.55 system-ui,-apple-system,"Segoe UI",sans-serif}.shell{max-width:1050px;margin:auto;padding:32px 20px 72px}h1,h2{font-family:Georgia,serif;font-weight:500}h1{font-size:clamp(34px,5vw,58px);margin:.15em 0}.lead{max-width:780px;font-size:18px;color:#403f3b}.summary{position:sticky;top:0;background:rgba(250,249,245,.96);backdrop-filter:blur(10px);z-index:4;border-bottom:1px solid var(--line);padding:12px 0;margin:20px 0}.case{background:var(--card);border:1px solid var(--line);border-radius:18px;padding:20px;margin:16px 0}.case-head{display:flex;justify-content:space-between;gap:16px;align-items:flex-start}.case-head span{font-size:11px;letter-spacing:.12em;color:var(--muted)}.case-head h2{margin:.2em 0}.case-head a{color:inherit}.criteria{display:grid;grid-template-columns:repeat(3,1fr);gap:8px 14px;margin:15px 0;padding:14px;background:#f5f2eb;border-radius:12px}.decision{display:flex;align-items:center;gap:10px;flex-wrap:wrap}button{padding:8px 12px;border:1px solid var(--line);border-radius:9px;background:#fff;cursor:pointer}.case.accepted{border-color:#86aa96}.case.rejected{border-color:#c69a9a}.state{font-weight:700}.note{border-left:3px solid #88837a;padding:14px 18px;background:#f2efe8}.small{font-size:13px;color:var(--muted)}@media(max-width:720px){.criteria{grid-template-columns:1fr}.case-head{display:block}.case-head a{display:inline-block;margin-top:6px}}</style></head><body><main class="shell"><div class="small">PHI OS · CX-R12R4B · SMR-R2</div><h1>W19 · Product-integration human review</h1><p class="lead">20 structurally distinct cases: AST 4 / BZR 4 / ZWR 4 / NUM 4 / ECR 4. Review the full reading, not just the machine counters.</p><p class="note"><strong>No export is required.</strong> This page saves marks only in this browser for convenience. After reviewing, you can simply tell ChatGPT that all 20 are accepted, or identify any rejected case IDs. W20 remains blocked until the human result is 20 accepted / 0 rejected / 0 pending.</p><div class="summary"><strong id="counter">0 accepted · 0 rejected · 20 pending</strong></div>${cards}</main><script>const key='phios-smr-w19-review';let state={};try{state=JSON.parse(localStorage.getItem(key)||'{}')}catch{};const cases=[...document.querySelectorAll('.case')];function save(){localStorage.setItem(key,JSON.stringify(state));render()}function render(){let a=0,r=0;for(const el of cases){const id=el.dataset.case,s=state[id]||{};el.classList.toggle('accepted',s.decision==='ACCEPTED');el.classList.toggle('rejected',s.decision==='REJECTED');el.querySelector('.state').textContent=s.decision||'PENDING';for(const cb of el.querySelectorAll('input[data-criterion]'))cb.checked=!!s.criteria?.[cb.dataset.criterion];if(s.decision==='ACCEPTED')a++;if(s.decision==='REJECTED')r++}document.getElementById('counter').textContent=a+' accepted · '+r+' rejected · '+(20-a-r)+' pending'}for(const el of cases){const id=el.dataset.case;for(const cb of el.querySelectorAll('input[data-criterion]'))cb.onchange=()=>{state[id]??={criteria:{}};state[id].criteria??={};state[id].criteria[cb.dataset.criterion]=cb.checked;save()};for(const b of el.querySelectorAll('button[data-decision]'))b.onclick=()=>{state[id]??={criteria:{}};if(b.dataset.decision==='ACCEPTED'&&[...el.querySelectorAll('input[data-criterion]')].some(x=>!x.checked)){alert('Mark all nine review criteria before accepting this case.');return}state[id].decision=b.dataset.decision;save()}}render();</script></body></html>`;
fs.writeFileSync('content/customer-experience-rebuild/r12r4b/smr/review/smr-w19-human-review.html',html);
console.log(`Generated W19 review pack: ${cases.length} cases; ${results.summary.accepted}/${results.summary.rejected}/${results.summary.pending}.`);
