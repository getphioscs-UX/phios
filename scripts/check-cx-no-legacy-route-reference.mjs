import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const roots=['assets/customer-ui','customer-ui-preview'];const forbidden=[['/knowledge-search','LEGACY_ASK'],['/reality-dashboard','LEGACY_REALITY_DASHBOARD'],['/my-reality','LEGACY_MY_REALITY'],['/personal-runtime','LEGACY_PERSONAL_RUNTIME'],['/professional/personal-runtime','LEGACY_PROFESSIONAL_PERSONAL_RUNTIME'],['/financial-reality','LEGACY_FINANCIAL_REALITY']];
const files=[];for(const rel of roots){const abs=path.join(root,rel);if(!fs.existsSync(abs))continue;const walk=d=>{for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory())walk(p);else if(/\.(html|js|css)$/.test(e.name))files.push(p)}};walk(abs)}
for(const file of files){const text=fs.readFileSync(file,'utf8');for(const [token,label] of forbidden)assert.equal(text.includes(token),false,`${label} forbidden in CX source: ${path.relative(root,file)}`);if(file.endsWith('navigation.js'))assert.equal(text.includes("id:'REALITY_JOURNEY'"),false,'Reality Journey cannot be primary CX navigation');}
console.log(`✓ CX legacy-route guard passed across ${files.length} clean-room customer source files.`);