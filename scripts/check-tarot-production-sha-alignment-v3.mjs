import assert from 'node:assert/strict';import fs from 'node:fs';import {LIVE_EVIDENCE_V3_PATH,readJson} from './lib/tarot/production-sha-alignment-v3.mjs';assert.ok(fs.existsSync(LIVE_EVIDENCE_V3_PATH),'TPA-L v3 live evidence pending. Run npm run verify:tarot-production-sha after exact M-R commit deploys.');const e=readJson(process.cwd(),LIVE_EVIDENCE_V3_PATH);assert.equal(e.alignment?.verified,true);assert.equal(e.checks?.canonicalRuntimeRoutePreserved,true);assert.equal(e.checks?.remoteRuntimeAssetsMatch,true);console.log(`✓ TPA-L v3 live SHA + canonical Tarot route alignment passed for ${e.alignment.deployedCommit}.`);
