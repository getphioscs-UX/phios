import assert from 'node:assert/strict'; import fs from 'node:fs'; const r=p=>JSON.parse(fs.readFileSync(p,'utf8')); const x=r('content/interpretation/source/book-2-conscious-runtime-source-inventory-v1.json'); assert.equal(x.status,'HUMAN_ACCEPTED_MANUSCRIPT_SOURCE_RECONCILED'); assert.equal(x.scope,'BOOK-2/P5 Conscious Runtime'); assert.equal(x.recordCount,65); const allowed=new Set(x.allowedClaimClasses); for(const e of x.records){assert.equal(e.humanAcceptance,'ACCEPTED');assert.ok(e.sourceSectionRefs.length>0);assert.ok(allowed.has(e.sourceClaimType));assert.equal(e.metaphorMayBecomeRuntimeRule,false);} console.log('✓ IR-W4 Book II Conscious Runtime source reconciliation passed (65/65 P5 nodes grounded in KAU-R4 accepted manuscript coverage).');
