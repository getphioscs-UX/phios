import fs from 'node:fs';import assert from 'node:assert/strict';const a=JSON.parse(fs.readFileSync('content/customer-experience-rebuild/r12r4b/smr-r2/admission/smr-r2-production-admission-v1.json','utf8'));assert.equal(a.singleApi,'/api/customer-personal-reality');assert.equal(a.methodSpecificApiCreated,false);assert.equal(a.liveCustomerIndividuallyHumanReviewed,false);if(a.customerCutoverAllowed!==true){assert.equal(a.status,'BLOCKED_PENDING_R2_W19_HUMAN_ACCEPTANCE');assert.equal(a.productionAllowed,false);assert.equal(a.humanReviewedProductComposition,false);assert.equal(a.currentApiMutationApplied,false);assert.equal(a.blocker,'R2_W19_20_OF_20_HUMAN_ACCEPTANCE_REQUIRED');console.log('✓ R2-W20 is truthfully fail-closed pending W19 20/20 human acceptance; API cutover not applied.');}else{assert.equal(a.status,'PRODUCTION');assert.equal(a.productionAllowed,true);assert.equal(a.humanReviewedProductComposition,true);assert.equal(a.currentApiMutationApplied,true);for(const x of Object.values(a.methods))assert.equal(x.state,'PRODUCTION');console.log('✓ R2-W20 production admission active.');}
