import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import {spawnSync} from 'node:child_process';const r=p=>JSON.parse(fs.readFileSync(p,'utf8'));const h=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const e=r('content/reconciliation/mir/mir-5-to-mir-6-baseline-reconciliation-v1.json');assert.equal(e.status,'RESTORED_BYTE_IDENTICAL_MIR5_GOVERNANCE_ARTIFACTS');for(const x of e.restoredArtifacts){assert.equal(h(x.path),x.expectedSha256,x.path);assert.equal(x.byteIdenticalToMIR5Freeze,true);}const p=spawnSync(process.platform==='win32'?'npm.cmd':'npm',['run','check:mir-5'],{stdio:'inherit'});assert.equal(p.status,0);console.log('✓ MIR-5 → MIR-6 baseline reconciliation passed: six omitted MIR-5 governance artifacts restored byte-identically; MIR-5 authority unchanged.');
