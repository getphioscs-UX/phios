import assert from 'node:assert/strict';import fs from 'node:fs';import {deriveAgencyContext,deriveIdentityContext} from '../functions/interpretation-runtime/domain-derivers-v1.js';const r=p=>JSON.parse(fs.readFileSync(p,'utf8'));assert.equal(r('content/interpretation/agency/agency-derivation-contract-v1.json').decisionExistsEqualsExecutionReady,false);const a=deriveAgencyContext({expression:{},decision:'D',executionReadiness:false});assert.equal(a.decisionExistsEqualsExecutionReady,false);const i=deriveIdentityContext({repeatedAgency:[a],continuity:{}});assert.equal(i.socialLabelAuthority,false);const c=r('content/interpretation/identity/identity-derivation-contract-v1.json');assert.equal(c.identityEqualsProfession,false);assert.equal(c.identityEqualsSocialLabel,false);console.log('✓ IR-W62–W75 agency/identity passed: Decision ≠ execution readiness; Identity ≠ profession/role/nationality/social label.');
