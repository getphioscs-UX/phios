import assert from 'node:assert/strict';import {resolveProfile} from '../functions/method-runtime/personal-structure/profile-resolver.js';const p=resolveProfile([{layer:'PERSONALITY',bodyCode:'SUN',line:5,status:'CALCULATED'},{layer:'DESIGN',bodyCode:'SUN',line:1,status:'CALCULATED'}]);assert.equal(p.profileCode,'5/1');assert.equal(p.personalityLine,5);assert.equal(p.designLine,1);assert.equal(resolveProfile([]).status,'UNKNOWN');console.log('✓ Personal Structure Profile passed: only Personality Sun line / Design Sun line create Profile.');
