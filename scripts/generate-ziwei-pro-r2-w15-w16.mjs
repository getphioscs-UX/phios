import fs from 'node:fs';
import crypto from 'node:crypto';
import {buildZiweiFullProductionCustomerRuntime} from '../functions/zi-wei-full-production/ziwei-full-production-customer-runtime.js';
import {adaptZiweiPersonalRealityProduct} from '../functions/personal-reality-product/adapters/ziwei-production-adapter.js';
import {buildZiweiW9W11RenderPlan} from '../assets/customer-ui/js/specialists/ziwei/ziwei-specialist-workspace.js';

const BASE='content/professional/zi-wei-professional-reading-r2';
const SOURCE='content/customer-experience-rebuild/ziwei-cx-r1/campaign/ziwei-cx-r1-w14-real-api-dom-machine-campaign-v1.json';
const BASELINE='dae24c1dd8de49a6c238ddffb8d52b388e8da10d';
const sha=v=>crypto.createHash('sha256').update(typeof v==='string'?v:JSON.stringify(v)).digest('hex');
const arr=v=>Array.isArray(v)?v:[];
const write=(p,v)=>{fs.mkdirSync(p.slice(0,p.lastIndexOf('/')),{recursive:true});fs.writeFileSync(p,typeof v==='string'?v:JSON.stringify(v,null,2)+'\n');};
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

function executionRequest(c){
  const consentRecordId=`CONSENT-ZIWEI-PRO-R2-W15-${c.caseId}`;
  const canonicalInput={birthDate:c.input.birthDate,birthTime:c.input.birthTime,birthPlace:{displayName:'Hong Kong',countryCode:'HK',latitude:22.3193,longitude:114.1694},timezone:{iana:'Asia/Hong_Kong',utcOffsetAtBirth:'+08:00',source:'GOVERNED_RESOLUTION',confidence:'HIGH'},timeAccuracy:'EXACT',locale:c.input.locale,consent:{recordId:consentRecordId,granted:true,purposeCode:'PERSONAL_RUNTIME_METHOD_PROJECTION',persistence:'NONE'},inputVersion:'MCD-3-CANONICAL-BIRTH-INPUT-v1.0.0'};
  return {schemaVersion:'PHI-OS-MCD-METHOD-EXECUTION-REQUEST-v1.0.0',methodCode:'ZI_WEI_DOU_SHU',methodVersion:'1.0.0',capability:'CALCULATION',purposeCode:'PERSONAL_RUNTIME_METHOD_PROJECTION',canonicalInput,executionParameters:{traditionalCalculationSex:c.input.traditionalCalculationSex},consentRecordId,requestId:`REQ-ZIWEI-PRO-R2-W15-${c.caseId}`};
}
const targetContext={targetDate:'2026-08-30',targetTime:'12:00',targetTimezone:{iana:'Asia/Kuala_Lumpur',utcOffsetAtTarget:'+08:00'},source:'EXPLICIT_REQUEST'};

function inspectQuality({c,pro,plan}){
  const mainHtml=`${plan.navigationHtml}${plan.visualHtml}${plan.readingHtml}`;
  const forbidden=['未载明','Unspecified','独立星意尚未准入','Standalone meaning not admitted','这项进入计算但尚未解释','This item enters calculation but is not yet interpreted','Source pending','Reading Units','96/96','24/24'];
  const genericStructureOnly=[/^[^。.!?]{0,25}(落在|位于|placed at)\s*[^。.!?]{0,25}[。.!?]?$/i,/^(星曜包括|Stars include)[:：]/i];
  const requiredPalace=['palacePurpose','whatIsHere','composition','networkEffect','transformationEffect','alternativeExpression','timingActivation','realityCheck'];
  const requiredTopic=['coreStructure','entryPattern','interactionPatterns','stabilityConditions','pressurePositions','currentTiming','realityCheck'];
  const palaces=arr(pro.palaces),topics=arr(pro.topicsProfessional),years=arr(pro.timingProfessional?.navigationWindow?.years),density=pro.chartProfessionalDensity;
  const meaningfulPalaces=palaces.filter(p=>requiredPalace.every(k=>String(p.professionalUnit?.[k]||'').trim().length>=12)).length;
  const topicMeaningful=topics.filter(t=>requiredTopic.every(k=>String(t.sections?.[k]||'').trim().length>=12)).length;
  const compositions=palaces.map(p=>String(p.professionalUnit?.composition||'').trim());
  const duplicateCompositionCount=compositions.length-new Set(compositions).size;
  const interpretationKeys=['composition','alternativeExpression','timingActivation','realityCheck'];const structureOnlyCount=palaces.reduce((n,p)=>n+interpretationKeys.map(k=>p.professionalUnit?.[k]).filter(v=>genericStructureOnly.some(r=>r.test(String(v).trim()))).length,0);
  const timingInterpretive=years.length===6&&years.every(y=>String(y.interpretation||'').trim().length>=35&&!/^(\d{4}|大限|流年|Da Xian|Liu Nian)[^。.!?]*$/i.test(String(y.interpretation||'').trim()));
  const densityPalaces=arr(density?.palaces);
  const starChipExpected=densityPalaces.reduce((n,p)=>n+arr(p.stars).length,0);
  const stateVisible=densityPalaces.flatMap(p=>arr(p.stars)).filter(s=>s.stateCustomerVisible).length;
  const densityOk=Boolean(density?.birth?.date&&density?.birth?.time&&density?.birth?.timezone&&density?.fiveElementBureau?.label&&densityPalaces.length===12&&densityPalaces.every(p=>p.stemBranchLabel&&p.daXianAgeRange)&&starChipExpected===28&&arr(density?.deferredAuthorityFields).length===2);
  const navNoTechnical=!/(Evidence|Boundary|证据与边界|证据|边界)/i.test(plan.navigationHtml);
  const technicalOnlyAbout=/About this reading|关于这份读取/.test(plan.technicalHtml)&&!/96\/96|24\/24|Reading Units|Source pending/.test(mainHtml);
  const forbiddenHits=forbidden.filter(x=>mainHtml.includes(x));
  const noGuaranteedEvents=!/(必定发生|一定会发生|必然富贵|guaranteed wealth|will definitely happen|is guaranteed to happen|is guaranteed to occur)/i.test(mainHtml);
  const metrics={palaces:palaces.length,meaningfulPalaces,topics:topics.length,meaningfulTopics:topicMeaningful,priority:arr(pro.priority).length,timingYears:years.length,duplicateCompositionCount,structureOnlyCount,forbiddenHits,navNoTechnical,technicalOnlyAbout,densityOk,densityPalaces:densityPalaces.length,starHierarchyCount:starChipExpected,stateVisibleCount:stateVisible,timingInterpretive,noGuaranteedEvents};
  const passed=palaces.length===12&&meaningfulPalaces===12&&topics.length===8&&topicMeaningful===8&&arr(pro.priority).length===5&&years.length===6&&duplicateCompositionCount===0&&structureOnlyCount===0&&forbiddenHits.length===0&&navNoTechnical&&technicalOnlyAbout&&densityOk&&timingInterpretive&&noGuaranteedEvents;
  return {metrics,passed};
}

const source=JSON.parse(fs.readFileSync(SOURCE,'utf8'));
if(arr(source.cases).length!==96)throw new Error('W15_SOURCE_NOT_96');
const results=[];const snapshots=[];
for(let i=0;i<source.cases.length;i++){
  const c=source.cases[i];
  const full=await buildZiweiFullProductionCustomerRuntime({executionRequest:executionRequest(c),targetContext,locale:c.input.locale});
  const product=adaptZiweiPersonalRealityProduct({publicationEnvelope:full.customerProduct,locale:c.input.locale});
  const pro=arr(product.visuals).find(v=>v.type==='ZIWEI_PRO_R2_PROFESSIONAL_PRESENTATION')?.payload;
  if(!pro)throw new Error(`W15_PRO_MISSING:${c.caseId}`);
  const plan=buildZiweiW9W11RenderPlan(product);
  const q=inspectQuality({c,pro,plan});
  results.push({caseId:c.caseId,input:c.input,lifeBranch:c.structure?.lifeBranch||null,bodyBranch:c.structure?.bodyBranch||null,metrics:q.metrics,professionalDigest:pro.professionalDigest,chartDensityDigest:pro.chartProfessionalDensity?.densityDigest||null,passed:q.passed});
  snapshots.push({caseId:c.caseId,input:c.input,lifeBranch:c.structure?.lifeBranch||null,bodyBranch:c.structure?.bodyBranch||null,density:{birth:pro.chartProfessionalDensity?.birth,fiveElementBureau:pro.chartProfessionalDensity?.fiveElementBureau,lifePalace:pro.chartProfessionalDensity?.lifePalace,bodyPalace:pro.chartProfessionalDensity?.bodyPalace,currentDaXian:pro.chartProfessionalDensity?.currentDaXian,currentLiuNian:pro.chartProfessionalDensity?.currentLiuNian},priority:arr(pro.priority).map(x=>({rank:x.rank,title:x.title,whyItMatters:x.whyItMatters,counterEvidence:x.counterEvidence,realWorldCheck:x.realWorldCheck})),palaces:arr(pro.palaces).map(x=>({palaceCode:x.palaceCode,title:x.title,branchLabel:x.branchLabel,chartDensity:x.chartDensity,professionalUnit:x.professionalUnit})),topics:arr(pro.topicsProfessional).map(x=>({topicCode:x.topicCode,title:x.title,sections:x.sections,boundary:x.boundary})),timing:{currentDaXian:pro.timingProfessional?.currentDaXian,currentYear:pro.timingProfessional?.currentYear,years:arr(pro.timingProfessional?.navigationWindow?.years).map(y=>({calendarYear:y.calendarYear,direction:y.direction,annualFocusLabel:y.annualFocusLabel,daXianFocusLabel:y.daXianFocusLabel,classification:y.classification,interpretation:y.interpretation}))},professionalDigest:pro.professionalDigest});
  if((i+1)%16===0)console.log(`W15 ${i+1}/96`);
}
const pass=results.filter(x=>x.passed).length;
const campaignCore={schemaVersion:'PHI-OS-ZIWEI-PRO-R2-W15-PROFESSIONAL-QUALITY-CAMPAIGN-v1.0.0',work:'ZIWEI-PRO-R2-W15',baselineCommit:BASELINE,sourceInputCampaignRef:SOURCE,status:pass===96?'MACHINE_VERIFIED_96_OF_96':'MACHINE_GATE_FAILED',purpose:'Measure professional interpretation quality rather than button existence.',criteria:['12/12 palaces have meaningful eight-slot interpretation units','8/8 topics have actual professional reading sections','default customer surface has zero generic 未载明 / unadmitted placeholders','no structure-only coordinate sentence masquerades as interpretation','12 palace composition paragraphs are distinct','timing has six interpretive navigation years rather than coordinates only','W13 main navigation is reading-only','W14 chart density is calculated and complete without fabricating Life Master / Body Master','no guaranteed-event language'],summary:{total:96,passed:pass,failed:96-pass,locales:Object.fromEntries(['zh-Hans','en'].map(l=>[l,results.filter(x=>x.input.locale===l).length])),sex:Object.fromEntries(['MALE','FEMALE'].map(s=>[s,results.filter(x=>x.input.traditionalCalculationSex===s).length])),meaningfulPalaces:results.reduce((n,x)=>n+x.metrics.meaningfulPalaces,0),meaningfulTopics:results.reduce((n,x)=>n+x.metrics.meaningfulTopics,0),duplicatePalaceCompositions:results.reduce((n,x)=>n+x.metrics.duplicateCompositionCount,0),structureOnlyFindings:results.reduce((n,x)=>n+x.metrics.structureOnlyCount,0),forbiddenCustomerSurfaceHits:results.reduce((n,x)=>n+x.metrics.forbiddenHits.length,0),timingInterpretiveCases:results.filter(x=>x.metrics.timingInterpretive).length,w14DensityCases:results.filter(x=>x.metrics.densityOk).length},cases:results,boundaries:{machineAcceptanceDoesNotSubstituteForHumanReview:true,w16HumanReviewRequired:true,w17CutoverAllowed:false}};
campaignCore.campaignDigest=sha(campaignCore);
write(`${BASE}/campaign/ziwei-pro-r2-w15-professional-quality-campaign-v1.json`,campaignCore);
write(`${BASE}/acceptance/ziwei-pro-r2-w15-machine-acceptance-v1.json`,{schemaVersion:'PHI-OS-ZIWEI-PRO-R2-W15-MACHINE-ACCEPTANCE-v1.0.0',work:'ZIWEI-PRO-R2-W15',baselineCommit:BASELINE,status:pass===96?'MACHINE_VERIFIED_96_OF_96':'BLOCKED',campaignRef:`${BASE}/campaign/ziwei-pro-r2-w15-professional-quality-campaign-v1.json`,campaignDigest:campaignCore.campaignDigest,required:96,passed:pass,failed:96-pass,professionalQualityGatePassed:pass===96,humanReviewSubstituted:false,w16HumanReviewAllowed:pass===96,w17CutoverAllowed:false});

// W16: one zh-Hans + one en case for each of the 12 life branches.
const selected=[];const seen=new Set();
for(const branch of [...new Set(snapshots.map(x=>x.lifeBranch))]){
  for(const locale of ['zh-Hans','en']){
    const x=snapshots.find(v=>v.lifeBranch===branch&&v.input.locale===locale&&!seen.has(v.caseId));
    if(x){selected.push(x);seen.add(x.caseId);}
  }
}
if(selected.length!==24)throw new Error(`W16_SELECTION_NOT_24:${selected.length}`);
const cases={schemaVersion:'PHI-OS-ZIWEI-PRO-R2-W16-PROFESSIONAL-USEFULNESS-REVIEW-CASES-v1.0.0',work:'ZIWEI-PRO-R2-W16',baselineCommit:BASELINE,status:'REVIEW_READY_24_PENDING',selectionPolicy:'ONE_ZH_HANS_AND_ONE_EN_CASE_PER_LIFE_BRANCH',criteria:['专业深度 / Professional depth','可理解度 / Comprehensibility','命盘对应度 / Chart specificity','十二宫差异性 / Palace differentiation','Topic usefulness','Timing usefulness','重复度 / Repetition (lower is better)','空话率 / Empty-language rate (lower is better)'],reviewQuestion:'看完是否知道这张盘在说什么？ / After reading, do you know what this chart is actually saying?',cases:selected};
write(`${BASE}/review/ziwei-pro-r2-w16-human-review-cases-v1.json`,cases);
const template={schemaVersion:'PHI-OS-ZIWEI-PRO-R2-W16-HUMAN-REVIEW-RESULTS-v1.0.0',work:'ZIWEI-PRO-R2-W16',baselineCommit:BASELINE,campaignRef:`${BASE}/review/ziwei-pro-r2-w16-human-review-cases-v1.json`,reviewStatus:'PENDING_0_OF_24',requiredAccepted:24,acceptedCount:0,needsRevisionCount:0,rejectedCount:0,pendingCount:24,items:selected.map(x=>({caseId:x.caseId,decision:'PENDING',ratings:{professionalDepth:null,comprehensibility:null,chartSpecificity:null,palaceDifferentiation:null,topicUsefulness:null,timingUsefulness:null,repetition:null,emptyLanguage:null},notes:''})),productionAdmissionAllowed:false,w17CutoverAllowed:false};
write(`${BASE}/review/ziwei-pro-r2-w16-human-review-results-template-v1.json`,template);
write(`${BASE}/acceptance/ziwei-pro-r2-w16-human-review-status-v1.json`,{schemaVersion:'PHI-OS-ZIWEI-PRO-R2-W16-HUMAN-REVIEW-STATUS-v1.0.0',work:'ZIWEI-PRO-R2-W16',baselineCommit:BASELINE,status:'PENDING_0_OF_24',machineCampaign:'PASS_96_OF_96',requiredHumanAccepted:24,humanAccepted:0,pending:24,humanReviewWasMachineSubstituted:false,w17CutoverAllowed:false});

const ratingFields=[['professionalDepth','专业深度 / Professional depth'],['comprehensibility','可理解度 / Comprehensibility'],['chartSpecificity','命盘对应度 / Chart specificity'],['palaceDifferentiation','十二宫差异性 / Palace differentiation'],['topicUsefulness','Topic usefulness'],['timingUsefulness','Timing usefulness'],['repetition','重复度 / Repetition（1=低，5=高）'],['emptyLanguage','空话率 / Empty language（1=低，5=高）']];
const cards=selected.map((x,i)=>`<article class="case" data-id="${esc(x.caseId)}"><header><span>${i+1}/24 · ${esc(x.caseId)} · ${esc(x.input.locale)} · ${esc(x.input.traditionalCalculationSex)}</span><h2>${esc(x.density.lifePalace?.palaceLabel||'Life')} ${esc(x.density.lifePalace?.branchLabel||'')} · ${esc(x.density.fiveElementBureau?.label||'')}</h2><p>${esc(x.input.birthDate)} ${esc(x.input.birthTime)} · ${esc(x.density.currentDaXian?.ageRange||'')} · ${esc(x.density.currentLiuNian?.year||'')}</p></header><section><h3>先看五件事 / Five priorities</h3>${x.priority.map(p=>`<div class="read"><strong>${esc(p.rank)}. ${esc(p.title)}</strong><p>${esc(p.whyItMatters)}</p><p class="counter">${esc(p.counterEvidence)}</p><p class="check">${esc(p.realWorldCheck)}</p></div>`).join('')}</section><details><summary>十二宫专业读取 / 12-palace professional reading</summary>${x.palaces.map(p=>`<div class="pal"><h4>${esc(p.title)} · ${esc(p.chartDensity?.stemBranchLabel||p.branchLabel)} · ${esc(p.chartDensity?.daXianAgeRange||'')}</h4>${Object.values(p.professionalUnit||{}).map(v=>`<p>${esc(v)}</p>`).join('')}</div>`).join('')}</details><details><summary>8 Topic professional readings</summary>${x.topics.map(t=>`<div class="topic"><h4>${esc(t.title)}</h4>${Object.values(t.sections||{}).map(v=>`<p>${esc(v)}</p>`).join('')}</div>`).join('')}</details><details open><summary>大限 × 流年 / Timing navigation</summary>${x.timing.years.map(y=>`<div class="year"><strong>${esc(y.calendarYear)} · ${esc(y.direction)}</strong><p>${esc(y.interpretation)}</p></div>`).join('')}</details><fieldset><legend>看完是否知道这张盘在说什么？</legend><div class="ratings">${ratingFields.map(([k,label])=>`<label>${esc(label)}<select data-rating="${k}"><option value="">—</option>${[1,2,3,4,5].map(n=>`<option value="${n}">${n}</option>`).join('')}</select></label>`).join('')}</div><div class="decision"><button type="button" data-d="ACCEPT">ACCEPT</button><button type="button" data-d="NEEDS_REVISION">NEEDS REVISION</button><button type="button" data-d="REJECT">REJECT</button><strong class="state">PENDING</strong></div><textarea placeholder="Notes / what felt useful, repetitive, generic, or mismatched"></textarea></fieldset></article>`).join('');
const html=`<!doctype html><html lang="zh-Hans"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>ZIWEI-PRO-R2 W16 Professional Usefulness Human Review</title><style>body{margin:0;background:#f4f1e9;color:#1e1c18;font:15px/1.65 system-ui}.shell{max-width:1180px;margin:auto;padding:30px 18px 90px}h1,h2,h3,h4{font-family:Georgia,'Noto Serif SC',serif}.intro{max-width:880px}.bar{position:sticky;top:0;z-index:5;padding:12px 0;background:#f4f1e9ee;backdrop-filter:blur(10px);border-bottom:1px solid #d8d0c1;display:flex;gap:12px;align-items:center;flex-wrap:wrap}.case{background:#fff;border:1px solid #d8d0c1;border-radius:18px;padding:22px;margin:18px 0}.case.accept{border-color:#4e8a68}.case.revise{border-color:#aa803d}.case.reject{border-color:#a45353}.read,.pal,.topic,.year{padding:11px 13px;margin:9px 0;background:#faf8f3;border-radius:11px}.counter{border-left:3px solid #8c7a52;padding-left:10px}.check{font-weight:650}.ratings{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px 14px}.ratings label{display:flex;justify-content:space-between;gap:12px;padding:8px;border-bottom:1px solid #eee}.decision{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:14px}button,select{padding:8px 12px}textarea{width:100%;min-height:80px;margin-top:12px;box-sizing:border-box;padding:10px}@media(max-width:700px){.ratings{grid-template-columns:1fr}}</style></head><body><main class="shell"><p>PHI OS · ZIWEI-PRO-R2</p><h1>W16 · 24-case Professional Usefulness Human Review</h1><p class="intro">W15 has machine-verified 96/96 professional-quality cases. This page is a separate Human gate. The core question is: <strong>看完是否知道这张盘在说什么？</strong> Rate professional depth, comprehensibility, chart specificity, palace differentiation, Topic usefulness, Timing usefulness, repetition and empty-language rate. Nothing is pre-approved.</p><div class="bar"><strong id="count">0 accepted · 0 revision · 0 rejected · 24 pending</strong><button id="export" type="button">Export review JSON</button></div>${cards}</main><script>const cards=[...document.querySelectorAll('.case')];function update(){const ds=cards.map(c=>c.dataset.decision||'PENDING'),n=x=>ds.filter(v=>v===x).length;document.querySelector('#count').textContent=n('ACCEPT')+' accepted · '+n('NEEDS_REVISION')+' revision · '+n('REJECT')+' rejected · '+n('PENDING')+' pending';}cards.forEach(c=>c.querySelectorAll('[data-d]').forEach(b=>b.onclick=()=>{c.dataset.decision=b.dataset.d;c.classList.remove('accept','revise','reject');c.classList.add(b.dataset.d==='ACCEPT'?'accept':b.dataset.d==='NEEDS_REVISION'?'revise':'reject');c.querySelector('.state').textContent=b.dataset.d;update()}));document.querySelector('#export').onclick=()=>{const items=cards.map(c=>({caseId:c.dataset.id,decision:c.dataset.decision||'PENDING',ratings:Object.fromEntries([...c.querySelectorAll('[data-rating]')].map(s=>[s.dataset.rating,s.value?Number(s.value):null])),notes:c.querySelector('textarea').value||''}));const n=x=>items.filter(v=>v.decision===x).length,obj={schemaVersion:'PHI-OS-ZIWEI-PRO-R2-W16-HUMAN-REVIEW-RESULTS-v1.0.0',work:'ZIWEI-PRO-R2-W16',baselineCommit:'${BASELINE}',reviewStatus:n('ACCEPT')===24?'HUMAN_ACCEPTED_24_OF_24':n('REJECT')?'HUMAN_REVIEW_REJECTED':n('NEEDS_REVISION')?'HUMAN_REVIEW_NEEDS_REVISION':'PENDING_HUMAN_REVIEW',requiredAccepted:24,acceptedCount:n('ACCEPT'),needsRevisionCount:n('NEEDS_REVISION'),rejectedCount:n('REJECT'),pendingCount:n('PENDING'),items,productionAdmissionAllowed:n('ACCEPT')===24,w17CutoverAllowed:n('ACCEPT')===24};const blob=new Blob([JSON.stringify(obj,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='ziwei-pro-r2-w16-human-review-results-v1.json';a.click();URL.revokeObjectURL(a.href)};update();</script></body></html>`;
write(`${BASE}/review/ziwei-pro-r2-w16-human-review.html`,html);
write('tools/review/ZIWEI-PRO-R2-W16-HUMAN-REVIEW.html',html);
console.log(`W15 campaign generated: ${pass}/96 PASS; W16 review cases: ${selected.length}/24 pending.`);
