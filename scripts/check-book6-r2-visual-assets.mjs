import fs from 'node:fs';import assert from 'node:assert/strict';const v=JSON.parse(fs.readFileSync('content/civilization-atlas/reconfiguration/book-vi-visual-asset-status-v1.json','utf8'));assert.equal(v.assets.length,23);const snaps=v.assets.filter(x=>x.kind==='STATIC_ATLAS_BASE_VISUAL');assert.equal(snaps.length,12);assert.equal(snaps.filter(x=>x.status==='PRESENT').length,11);assert.equal(snaps.filter(x=>x.status==='MISSING').length,1);assert.equal(snaps.find(x=>x.assetId==='WORLD_RECONFIGURATION_SNAPSHOT_2026')?.status,'MISSING');console.log('✓ Book VI R2 truth contract: 11 PRESENT / 1 MISSING; remaining core visuals may stay UNVERIFIED');