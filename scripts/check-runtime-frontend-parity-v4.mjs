import assert from 'node:assert/strict';import fs from 'node:fs';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const exists=p=>fs.existsSync(p);
const current=j('content/governance/production-capability-matrix/reconciliation/production-capability-current-successor-v4.json');const registry=j(current.currentRegistry);const projection=j(current.currentStatusProjection);const old=j(current.predecessorRegistry);
assert.equal(current.status,'CURRENT_REGISTRY_POINTER');assert.equal(current.predecessorMutated,false);assert.equal(registry.registryVersion,'4.0.0');
const expected={HDR:['BLOCKED','Blocked'],AST:['AVAILABLE','Available'],BZR:['AVAILABLE','Available'],NUM:['AVAILABLE','Available'],ICH:['REGISTERED_NOT_IMPLEMENTED','Coming later'],TAR:['REGISTERED_NOT_IMPLEMENTED','Coming later']};
for(const [plugin,[classification,status]] of Object.entries(expected)){const c=registry.capabilities.find(x=>x.methodRuntime.pluginCode===plugin);assert(c,plugin);assert.equal(c.classification,classification);assert.equal(c.statusProjection,status);for(const e of c.evidence||[])assert(exists(e),`missing v4 evidence ${e}`);}
const ast=registry.capabilities.find(x=>x.methodRuntime.pluginCode==='AST');for(const gate of ['implemented','validated','projected','meaningReady','readingReady','userExecutable','productionAccepted'])assert.equal(ast[gate],true,`AST ${gate}`);assert.equal(ast.capabilityAvailability,'AVAILABLE');assert.equal(ast.deploymentVerified,false);assert.equal(ast.frontendRoute,'/personal-runtime');assert(exists('assets/js/pages/ast-production-meaning.js'));assert(exists('functions/api/ast-structural-execute.js'));assert(exists('functions/canonical-meaning-production/api-method-meaning-handler.js'));
const act=j('content/professional/canonical-meaning-production/successors/canonical-meaning-production-activation-v3.json');assert.equal(act.methods.find(x=>x.pluginCode==='AST').productionActivated,true);const rrp=j('content/products/runtime-reading/successors/runtime-reading-method-availability-successor-v4.json');assert.equal(rrp.methods.find(x=>x.methodCode==='AST').availability,'AVAILABLE');const oldAst=old.capabilities.find(x=>x.methodRuntime.pluginCode==='AST');assert.equal(oldAst.classification,'LIMITED_SCOPED');assert.equal(projection.source,current.currentRegistry);assert.equal(projection.items.find(x=>x.pluginCode==='AST').status,'Available');
console.log('✓ PCM v4 runtime/frontend parity successor passed.');console.log('  AST is Available only after v2 projection + Production meaning + Reading + successor frontend + acceptance; PCM v3 remains frozen Limited.');
