import {spawnSync} from 'node:child_process';const checks=['check:symbolic-public-ux','check:symbolic-sensitive-domain-guard','check:symbolic-browser','check:symbolic-human-review-gate','check:symbolic-production-candidate','check:future-method-contract'];for(const c of checks){const r=spawnSync('npm',['run',c],{stdio:'inherit',shell:true});if(r.status!==0)process.exit(r.status??1);}console.log('✓ PHASE 10–12 symbolic UX / production-candidate / future-method governance passed.');