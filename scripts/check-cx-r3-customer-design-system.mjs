import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';const required=['tokens.css','base.css','typography.css','layout.css','components.css','motion.css','utilities.css','surfaces/preview.css'];for(const rel of required)assert.equal(fs.existsSync(path.join('assets/customer-ui',rel)),true,`missing customer UI file: ${rel}`);const preview=fs.readFileSync('customer-ui-preview/index.html','utf8');assert.match(preview,/data-cx-surface="CUSTOMER_UI_PREVIEW"/);assert.equal(/assets\/css\//.test(preview),false,'preview imports legacy CSS');assert.equal(/<style[\s>]/i.test(preview),false,'preview uses page-local style block');const css=required.map(x=>fs.readFileSync(path.join('assets/customer-ui',x),'utf8')).join('\n');for(const old of ['.puxr-','.public-','.rs-','.pr-','.px2-','.wpr-','.phi-public-'])assert.equal(css.includes(old),false,`legacy selector leaked into CX CSS: ${old}`);for(const token of ['--cx-bg','--cx-container','--cx-font-serif','.cx-button','.cx-card','.cx-workspace','.cx-dialog'])assert.ok(css.includes(token),`CX design primitive missing: ${token}`);console.log(`✓ CX-R3 passed: ${required.length} clean-room CSS layers, cx-* namespace only, preview surface ready.`);