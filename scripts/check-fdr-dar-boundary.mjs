import assert from 'node:assert/strict';
import {base, readJson, requireFile} from './fdr-check-lib.mjs';
const fdrPath=`${base}/contracts/fdr-dar-cross-consumption-contract-v1.json`, bindingPath=`${base}/contracts/fdr-dar-cross-consumption-binding-v1.json`, consentPath=`${base}/contracts/financial-data-consent-contract-v1.json`, darAuthority='content/document-assembly/authority/dar-authority-baseline-v1.json', willAssets='content/legal/will/registries/will-asset-type-registry-v1.json', acceptance=`${base}/acceptance/fdr-w0-w24-acceptance-v1.json`;
[fdrPath,bindingPath,consentPath,darAuthority,willAssets,acceptance].forEach(requireFile);
const fdr=readJson(fdrPath),binding=readJson(bindingPath),consent=readJson(consentPath),dar=readJson(darAuthority),assets=readJson(willAssets),acc=readJson(acceptance);
assert.equal(binding.status,'ACTIVE_EXPLICIT_READ_ONLY_BINDING_PRE_DAR_PRODUCTION'); assert.equal(binding.requiredPurposeScope,'WILL_ASSEMBLY'); assert.equal(binding.bindingRules.darMayReadAuthorizedFdrFacts,true); assert.equal(binding.bindingRules.darMayMutateFdrFacts,false); assert.equal(binding.bindingRules.darMayCreateSecondAssetFactRegistry,false); assert.equal(binding.bindingRules.currentDarProductionEnabledByThisBinding,false);
assert.equal(fdr.consumptionRules.fdrAssetFactMayBeReferencedByDar,true); assert.equal(fdr.consumptionRules.darInstructionMayMutateFdrAssetFact,false); assert.equal(fdr.consumptionRules.darMayInventMissingFdrFact,false); assert.equal(fdr.consumptionRules.darMayReplaceUnknownWithAssumption,false); assert.equal(fdr.consumptionRules.darUseRequiresW18ConsentScope,'WILL_ASSEMBLY'); assert.equal(consent.rules.financialPlanningConsentAuthorizesWillAssembly,false);
for(const forbidden of ['guess beneficiary','guess executor','silently rebalance shares','infer jurisdiction','silently replace missing data']) assert.ok(dar.mayNot.includes(forbidden),`DAR boundary missing ${forbidden}`);
assert.equal(assets.status,'ACTIVE_TAXONOMY_NO_LEGAL_TREATMENT_AUTHORITY'); assert.equal(assets.rules.classificationDoesNotProveOwnership,true); assert.ok(assets.assetTypes.every(x=>x.legalTreatment==='UNDETERMINED_BY_TAXONOMY'));
assert.equal(acc.proofs.noDuplicateWillAssetRegistry,true); assert.equal(acc.proofs.consentScopedReuse,true);
console.log('✓ FDR↔DAR boundary passed: DAR may reference consent-authorized FDR facts but cannot own, infer, repair, mutate, or convert them into legal distribution authority.');
