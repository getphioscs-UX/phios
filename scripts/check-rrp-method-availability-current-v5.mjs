import assert from 'node:assert/strict';import fs from 'node:fs';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const r=j('content/products/runtime-reading/successors/runtime-reading-method-availability-successor-v5.json');assert.equal(r.status,'CURRENT_SUCCESSOR_NUM_BZR_AST_ZWR_AVAILABLE');assert.equal(r.predecessorMutated,false);for(const code of ['NUM','BZR','AST','ZWR']){const m=r.methods.find(x=>x.methodCode===code);assert(m,code);for(const k of ['calculation','projection','meaning','reading','availability'])assert.equal(m[k],'AVAILABLE',`${code}:${k}`);assert.equal(m.professional,'UNAVAILABLE');assert.equal(m.selfServiceAllowed,true);}const hdr=r.methods.find(x=>x.methodCode==='HDR');assert.equal(hdr.availability,'UNAVAILABLE');console.log('✓ RRP current Method availability v5 passed: NUM/BZR/AST/ZWR Available; HDR unavailable.');
