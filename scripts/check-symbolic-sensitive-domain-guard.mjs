import assert from 'node:assert/strict';import {detectSensitiveDomains,assertSymbolicSensitiveDomainBoundary,SENSITIVE_DOMAINS} from '../functions/symbolic-method-public-ux/symbolic-sensitive-domain-guard.js';import fs from 'node:fs';
const cases={MEDICAL:'I am reviewing a medical treatment decision',FINANCIAL:'I am uncertain about an investment',LEGAL:'I am reviewing a legal contract',DEATH:'I am afraid someone may die',PREGNANCY:'I am uncertain about pregnancy',RELATIONSHIP:'I am reviewing my relationship with my partner',MENTAL_HEALTH:'I am reviewing mental health and anxiety'};assert.deepEqual(Object.keys(cases),[...SENSITIVE_DOMAINS]);for(const [d,q] of Object.entries(cases)){assert.ok(detectSensitiveDomains(q).includes(d));const r=assertSymbolicSensitiveDomainBoundary({question:q,generatedOutput:'This introduces a possible lens. Compare it with current evidence.'});assert.equal(r.createsFact,false);assert.equal(r.createsProfessionalAdvice,false);}for(const bad of ['You will die','You should invest your money','Stop treatment','This proves you have a disease'])assert.throws(()=>assertSymbolicSensitiveDomainBoundary({question:'sensitive',generatedOutput:bad}),/FORBIDDEN/);const a=JSON.parse(fs.readFileSync('content/production/symbolic-method/acceptance/symbolic-sensitive-domain-acceptance-v1.json','utf8'));assert.equal(a.status,'MACHINE_ACCEPTED');console.log('✓ PROD-W4 Sensitive Domain Guard passed for medical, financial, legal, death, pregnancy, relationship and mental-health cases.');