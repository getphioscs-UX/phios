import assert from 'node:assert/strict';
import {readJson,readText,BASELINE,W9,PUBLIC_ASSET_REGISTRY} from './lib/web-production/wpr-assets-v1.mjs';
const c=readJson(W9), registry=readJson(PUBLIC_ASSET_REGISTRY); assert.equal(c.baselineCommit,BASELINE); assert.equal(c.work,'WPR-W9'); assert.equal(c.runtime.variantAuthority,'PUBLIC_ASSET_REGISTRY_EXPLICIT_METADATA_ONLY'); assert.ok(c.runtime.supportedVariantCodes.includes('ORIGINAL'));
for(const key of ['variantNameDoesNotCreateObjectKey','filenameConventionDoesNotCreateVariantAuthority','srcsetUsesRegisteredVariantsOnly','unverifiedVariantIsNotRenderable']) assert.equal(c.rules[key],true,key);
assert.equal(c.currentState.registeredAssetsHaveExplicitVariants,registry.assets.some(a=>Array.isArray(a.variants)&&a.variants.length)); assert.equal(c.currentState.registeredAssetsHaveExplicitVariants,false); assert.equal(c.currentState.responsiveMode,'ORIGINAL_SINGLE_SOURCE_UNTIL_VARIANTS_REGISTERED');
const source=readText('assets/js/runtime/web-production/asset-resolver.js'); const mod=await import(`data:text/javascript;base64,${Buffer.from(source).toString('base64')}`); const fixture={bucket:'phios-public-assets',assets:[{asset_code:'RESP',object_key:'original.webp',verification:'verified',variants:[{code:'CARD',object_key:'card.webp',width:640,height:480,verification:'verified'}]}]}; const resolved=mod.resolvePublicAsset({registry:fixture,assetCode:'RESP',publicBaseUrl:'https://assets.example.test',variant:'CARD'}); assert.match(resolved.src,/card\.webp$/); assert.match(resolved.srcset,/640w/); assert.throws(()=>mod.resolvePublicAsset({registry:fixture,assetCode:'RESP',publicBaseUrl:'https://assets.example.test',variant:'HERO'}),e=>e.code==='PUBLIC_ASSET_VARIANT_NOT_REGISTERED');
console.log('✓ WPR-W9 Responsive Asset Runtime passed.');
console.log('✓ Responsive variants are registry-only; current canonical assets remain ORIGINAL single-source until explicit variants are governed.');
