import assert from 'node:assert/strict';import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('content/interpretation/grammar/derivation-edge-contract-v1.json','utf8'));assert.deepEqual(new Set(x.authorityStatus),new Set(['PROVEN','CANONICAL','CONDITIONAL','HYPOTHESIS','EXPERIMENTAL','REJECTED']));assert.equal(x.invariants.sequenceOrderIsCausality,false);assert.equal(x.invariants.hypothesisMayAutoCanonicalize,false);assert.equal(x.edges.length,0);assert.ok(x.requiredFields.includes('alternativeEdges'));console.log('✓ IR-W8 derivation edge contract passed (schema foundation only; no fabricated canonical edges).');
