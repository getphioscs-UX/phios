import assert from 'node:assert/strict';import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('content/health/health-knowledge/labs/health-lab-knowledge-registry-v1.json','utf8'));assert.ok(x.entries.length>=13);assert.ok(x.entries.every(e=>e.diagnosticInterpretationAllowed===false&&e.referenceRangeBundled===false));console.log(`✓ Health lab corpus passed: ${x.entries.length} lab concepts.`);
