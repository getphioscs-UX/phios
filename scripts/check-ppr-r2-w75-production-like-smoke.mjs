import assert from 'node:assert/strict';import fs from 'node:fs';const e=JSON.parse(fs.readFileSync('content/personal-reading/production-cutover/evidence/w75-production-like-smoke-evidence-v1.json','utf8'));assert.equal(e.status,'PRODUCTION_LIKE_MACHINE_SMOKE_PASS_HUMAN_GATE_PENDING');assert.equal(Object.values(e.coverage).every(Boolean),true);assert.equal(e.machineEvidence.w70_ordinary24,true);assert.equal(e.machineEvidence.w70_profile24,true);assert.equal(e.machineEvidence.w70_relationship24,true);assert.equal(e.externalLiveEvidence.liveOpenAIProviderExercised,false);assert.equal(e.externalLiveEvidence.livePaymentProviderExercised,false);assert.equal(e.externalLiveEvidence.productionPersistentStorageBindingExercised,false);assert.equal(e.humanGate.hardCutoverAllowed,false);console.log('✓ W75 production-like smoke evidence current; no live external provider/payment/storage claim is fabricated.');
