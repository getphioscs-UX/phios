import assert from 'node:assert/strict';import fs from 'node:fs';const x=JSON.parse(fs.readFileSync('content/health/health-knowledge/safety/health-warning-signal-registry-v1.json','utf8'));assert.ok(x.signals.some(s=>s.responseClass==='EMERGENCY'));assert.equal(x.governance.diagnosisAllowed,false);assert.equal(x.governance.routerPurpose,'CARE_URGENCY_NOT_DIAGNOSIS');console.log('✓ Health safety corpus passed.');
