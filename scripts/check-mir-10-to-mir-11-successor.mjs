import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import {spawnSync} from 'node:child_process';
const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const r=spawnSync(process.execPath,['scripts/check-pca-mir10-successor.mjs'],{stdio:'inherit'});assert.equal(r.status,0);
const rec=j('content/reconciliation/mir/mir-10-to-mir-11-successor-revalidation-v1.json');assert.equal(rec.status,'SUCCESSOR_REVALIDATED_PUBLICATION_COVERAGE_GROWTH_ONLY');for(const d of rec.drift){assert.equal(sha(d.path),d.currentSha256);assert.notEqual(d.currentSha256,d.historicalSha256);assert.ok(d.currentRecordCount>=d.historicalRecordCount);assert.equal(d.authorityMutation,false);}assert.equal(rec.rules.rewriteHistoricalMir10Hashes,false);assert.equal(rec.rules.canonicalTruthMutationAllowed,false);console.log('✓ MIR-10 → MIR-11 successor revalidation passed: historical MIR-10 hashes remain frozen while publication/coverage growth is classified as downstream production growth only.');
