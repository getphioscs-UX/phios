import fs from 'node:fs';import assert from 'node:assert/strict';
const read=p=>JSON.parse(fs.readFileSync(p,'utf8'));
const cases=read('content/customer-experience-rebuild/r12r4b/smr/review/smr-w19-human-review-cases-v1.json');const results=read('content/customer-experience-rebuild/r12r4b/smr/review/smr-w19-human-review-results-v1.json');const ready=read('content/customer-experience-rebuild/r12r4b/smr/acceptance/smr-w19-review-readiness-v1.json');
assert.equal(cases.cases.length,20);assert.equal(results.results.length,20);for(const m of ['AST','BZR','ZWR','NUM','ECR'])assert.equal(cases.cases.filter(x=>x.methodId===m).length,4);assert.deepEqual(results.summary,{accepted:20,rejected:0,pending:0});for(const row of results.results)assert.equal(row.decision,'HUMAN_ACCEPTED');assert.equal(ready.status,'READY_FOR_HUMAN_REVIEW_20_CASES');assert.equal(ready.productionCutoverAllowed,false);assert.equal(fs.existsSync('content/customer-experience-rebuild/r12r4b/smr/review/smr-w19-human-review.html'),true);for(const c of cases.cases){assert.equal(fs.existsSync(`content/customer-experience-rebuild/r12r4b/smr/review/${c.report}`),true);assert.equal(c.machineEvidence.renderedDuplicateTexts,0);assert.ok(c.machineEvidence.firstScreenBlocks<=8);assert.ok(c.machineEvidence.firstScreenThemes<=3)}console.log('✓ R2-W19 review pack integrity passed: original 20 cases preserved and human results are 20/20 accepted.');
