import {assert,fs,all,style,renderSvg,svgPath} from './figure/figure-check-lib.mjs';const st=style();for(const {id,spec} of all()){assert.ok(fs.existsSync(svgPath(id)),`${id} stored SVG missing`);assert.equal(fs.readFileSync(svgPath(id),'utf8'),renderSvg(spec,st),`${id} non-deterministic stored SVG`);}console.log('✓ FIG determinism checker: 57/57 stored SVGs are byte-stable against canonical renderer.');
