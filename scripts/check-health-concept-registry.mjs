import assert from 'node:assert/strict';import fs from 'node:fs';const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const x=j('content/health/health-knowledge/concepts/health-concept-registry-v1.json');assert.ok(x.conceptCount>=50);assert.equal(x.conceptCount,x.concepts.length);assert.equal(new Set(x.concepts.map(c=>c.conceptId)).size,x.concepts.length);console.log(`✓ Health concept registry passed: ${x.conceptCount} canonical concepts.`);
