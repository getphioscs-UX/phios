import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import {spawnSync} from 'node:child_process';const read=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const run=s=>{const x=spawnSync(process.platform==='win32'?'npm.cmd':'npm',['run',s],{stdio:'inherit'});assert.equal(x.status,0,`${s} failed`);};for(const s of ['check:mir-4:mir-5-successor','check:ir-w3','check:ir-w4','check:ir-w6','check:ir-w7','check:ir-w8','check:ir-w9','check:mir-5:personal-structure','check:mir-5:governance','check:mir-5:authority-conflict','check:mir-5:loader'])run(s);const e=read('content/reconciliation/mir/mir-5-canonical-interpretation-foundation-v1.json');assert.equal(e.status,'COMPLETE_FOUNDATION_CANONICAL_DERIVATION_EXECUTION_DEFERRED_TO_MIR6');for(const [p,h] of Object.entries(e.criticalPredecessorSha256))assert.equal(sha(p),h,`frozen predecessor drift: ${p}`);for(const [p,h] of Object.entries(e.artifactSha256)){if(p==='scripts/check-mir-5-canonical-interpretation-foundation.mjs')continue;assert.equal(sha(p),h,`MIR-5 artifact drift: ${p}`);}assert.equal(e.authority.secondInterpretationRuntimeCreated,false);assert.equal(e.authority.canonicalMeaningMigrated,false);assert.equal(e.authority.mir6KernelExecutionCreated,false);console.log('✓ MIR-5 Canonical Interpretation Foundation + Governance passed: manuscript-grounded source inventories, boundary-only figure successor, operator/edge/confidence governance, human-review gates, authority conflict protection and one registry loader; MIR-6 derivation execution remains deferred.');
