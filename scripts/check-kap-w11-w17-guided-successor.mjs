import assert from 'node:assert/strict';
import fs from 'node:fs';
import crypto from 'node:crypto';
import {spawnSync} from 'node:child_process';
import {onRequestGet} from '../functions/api/ask-phios.js';
const read=p=>fs.readFileSync(p,'utf8');const j=p=>JSON.parse(read(p));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const pkg=j('package.json');
for(const step of ['11','12','13','14','15','16','17']){const alias=`check:kap-w${step}`;const command=pkg.scripts[alias];assert.ok(command,`MISSING:${alias}`);const [exe,...args]=command.split(' ');assert.equal(exe,'node');const run=spawnSync(process.execPath,args,{cwd:process.cwd(),encoding:'utf8'});assert.equal(run.status,0,`${alias}\n${run.stdout}\n${run.stderr}`);process.stdout.write(run.stdout);}
const freeze=j('content/knowledge/answer-projection/freeze/kap-w11-w17-answer-composition-freeze-v1.json');const successor=j('content/knowledge/answer-projection/reconciliation/kap-w17-w18-guided-reading-surface-successor-v1.json');const cka=j('content/web-production/reconciliation/hpc2-w5-cka-w0-w4-current-successor-v1.json');const ckaAcceptance=j('content/client/knowledge-ask/acceptance/cka-w0-w4-batch-a-acceptance-v1.json');
assert.equal(freeze.status,'FROZEN_ASK_PHIOS_DETERMINISTIC_PRODUCTION_NO_AI_PROVIDER_NO_READING_ESCALATION');assert.equal(successor.status,'ACTIVE_ADDITIVE_SURFACE_SUCCESSOR');
const ckaCurrent=new Map(cka.clientSurfaceTransition.artifacts.map(item=>[item.path,item]));
for(const item of [...freeze.predecessorEvidence,...freeze.frozenOutputs]){const transition=ckaCurrent.get(item.path);if(item.path==='assets/js/pages/knowledge-search.js'){assert.equal(item.sha256,successor.authorizedDrift.predecessorSha256);continue;}if(item.path==='functions/api/ask-phios.js'){assert.equal(item.sha256,cka.runtimeTransition.askApi.predecessorSha256);continue;}if(transition){assert.equal(item.sha256,transition.predecessorSha256,`CKA_PREDECESSOR_MISMATCH:${item.path}`);continue;}assert.equal(sha(item.path),item.sha256,`KAP_W11_W17_FROZEN_DRIFT:${item.path}`);}
assert.equal(successor.authorizedDrift.predecessorSha256,freeze.frozenOutputs.find(x=>x.path==='assets/js/pages/knowledge-search.js').sha256);assert.equal(successor.authorizedDrift.successorSha256,ckaCurrent.get(successor.authorizedDrift.path).predecessorSha256);assert.equal(successor.authorityBoundary.askPhiosSemanticsChanged,false);assert.equal(successor.authorityBoundary.guidedReadingIsSeparateCapability,true);assert.equal(cka.status,'ACTIVE_ADDITIVE_CKA_HOMEPAGE_ENTRY_SUCCESSOR_W5_HISTORY_PRESERVED');assert.equal(sha('functions/api/ask-phios.js'),cka.runtimeTransition.askApi.currentSuccessorSha256);for(const item of cka.clientSurfaceTransition.artifacts)assert.equal(sha(item.path),item.currentSuccessorSha256,`CKA_CURRENT_SURFACE_DRIFT:${item.path}`);assert.equal(cka.clientSurfaceTransition.guidedReadingActivatedByCkaW0W4,false);assert.equal(ckaAcceptance.humanAcceptance.claimed,false);assert.equal(ckaAcceptance.browserAcceptance.claimed,false);
const ASSETS={fetch:async req=>{const rel=decodeURIComponent(new URL(req.url).pathname.replace(/^\/+/,''));try{return new Response(fs.readFileSync(rel),{status:200,headers:{'content-type':'application/json'}})}catch{return new Response('',{status:404})}}};
const request=new Request('https://phios.local/api/ask-phios?q='+encodeURIComponent('为什么人工智能是文明能力长期累积的结果')+'&locale=zh-Hans&depth=STANDARD&entrySurface=KNOWLEDGE_SEARCH&mode=GLOBAL');const response=await onRequestGet({request,env:{ASSETS}});const payload=await response.json();assert.equal(response.status,200);assert.equal(payload.ok,true);assert.equal(payload.capability,'ASK_PHIOS');assert.equal(payload.ai.providerInvoked,false);assert.equal(payload.production.independentlyDeliverable,true);assert.equal(payload.production.requiresGuidedReading,false);assert.equal(payload.governance.persistentCaseCreated,false);assert.equal(payload.cka.entryContext.entrySurface,'KNOWLEDGE_SEARCH');assert.equal(payload.cka.clientAnswer.governance.createsCanonicalAuthority,false);assert.equal(payload.cka.governance.secondAnswerRuntimeCreated,false);assert.equal(payload.cka.governance.persistentHistoryCreated,false);
assert.equal(pkg.scripts['check:kap-answer'],'node scripts/check-kap-w11-w17-guided-successor.mjs');
console.log('✓ KAP-W11-W17 answer semantics preserved through W18 and the additive CKA client-surface successor.');
