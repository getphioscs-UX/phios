import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';import path from 'node:path';
const read=p=>fs.readFileSync(p,'utf8');const json=p=>JSON.parse(read(p));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const c=json('content/web-production/successors/knowledge-spine-batch2-presentation-successor-v1.json');const r=json('content/web-production/registries/client-visual-asset-registry-v1.5.json');
assert.equal(c.status,'PRESENTATION_SUCCESSOR_NO_KNOWLEDGE_AUTHORITY');assert.equal(sha(c.predecessorVisualRegistry.path),c.predecessorVisualRegistry.sha256,'KNOWLEDGE_SPINE_PREDECESSOR_REGISTRY_DRIFT');for(const v of Object.values(c.boundaries))assert.equal(v,false);
const css=read(c.sharedPresentation.stylesheet),js=read(c.sharedPresentation.visualHydrator);assert.match(js,/resolvePublicAssetForWeb/);assert.match(js,/startsWith\('ILL-'\)/);assert.doesNotMatch(js,/localStorage\.setItem|sessionStorage\.setItem|method:\s*['"]POST/i);assert.match(css,/\.ks-hero/);assert.match(css,/\.ks-article-hero/);assert.match(css,/\.ks-book-hero/);
for(const s of c.surfaces.filter(x=>!x.path.includes('*'))){const html=read(s.path);assert.match(html,/\/assets\/css\/knowledge-spine\.css/,`KNOWLEDGE_SPINE_CSS_MISSING:${s.path}`);if(!s.path.endsWith('library.html')||true)assert.match(html,/\/assets\/js\/pages\/knowledge-spine-visuals\.js/,`KNOWLEDGE_SPINE_VISUAL_JS_MISSING:${s.path}`);assert.match(html,new RegExp(`data-knowledge-spine-surface="${s.surface}"`),`KNOWLEDGE_SPINE_SURFACE_MARKER_MISSING:${s.path}`);if(['LIBRARY','ARTICLES','FIGURES','FIGURE','BOOKS'].includes(s.surface))assert.match(html,new RegExp(`data-ks-asset="${s.hero}"`),`KNOWLEDGE_SPINE_HERO_MISSING:${s.path}`);}
const articleRenderer=read(c.articleMaster.masterRenderer),articleController=read(c.articleMaster.pageController);assert.match(articleRenderer,/renderArticleDocument/);assert.match(articleController,/hydrateKnowledgeSpineVisuals/);assert.match(js,/decorateArticle/);assert.match(js,/HERO-004/);const articleFiles=fs.readdirSync('articles').filter(f=>f.endsWith('.html'));assert.ok(articleFiles.length>0);for(const f of articleFiles){const h=read(path.join('articles',f));assert.match(h,/\/assets\/js\/pages\/article\.js/);}
const bookJs=read(c.bookDetailMaster.renderer);assert.match(bookJs,/loadCanonicalBooks/);assert.match(bookJs,/canonicalPartsForBook/);assert.match(js,/decorateBook/);for(const [book,hero] of Object.entries(c.bookDetailMaster.fiveHeroMapping)){assert.ok(js.includes(`'${book}':'${hero}'`),`BOOK_HERO_MAPPING_MISSING:${book}`);}
for(const s of c.surfaces){const cov=r.surfaceCoverage.find(x=>x.htmlFile===s.path&&x.surfaceCode===s.surface);assert.ok(cov,`KNOWLEDGE_SPINE_VISUAL_COVERAGE_MISSING:${s.surface}`);assert.equal(cov.status,'CURRENT_CANONICAL_CONSUMER');}
assert.equal(sha('package.json'),c.packageFreeze.packageJsonSha256,'KNOWLEDGE_SPINE_PACKAGE_JSON_DRIFT');assert.equal(sha('package-lock.json'),c.packageFreeze.packageLockSha256,'KNOWLEDGE_SPINE_PACKAGE_LOCK_DRIFT');
console.log('✓ Knowledge Spine Batch 2 presentation successor passed.');console.log(`  Library → Articles → Article master → Figures → Figure → Books → five Book Detail surfaces share one governed visual system; ${articleFiles.length} published article shells inherit the master without per-file mutation.`);

