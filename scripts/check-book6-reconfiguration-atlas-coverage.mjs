import fs from 'node:fs';import assert from 'node:assert/strict';const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const r=j('content/civilization-atlas/reconfiguration/book-vi-reconfiguration-atlas-registry-v2.json');const c=j('content/civilization-atlas/reconfiguration/cases/reconfiguration-case-registry-v1.json');const w=j('content/civilization-atlas/reconfiguration/windows/reconfiguration-windows-v1.json');const s=j('content/civilization-atlas/reconfiguration/snapshots/world-reconfiguration-snapshots-v1.json');const d=j('content/civilization-atlas/reconfiguration/dossiers/contemporary-runtime-dossiers-v1.json');const l=j('content/civilization-atlas/reconfiguration/lived-reality/lived-reality-dimensions-v1.json');assert.equal(r.sections.length,7);assert.equal(r.chapters.length,85);assert.equal(r.figures.length,8);assert.ok(c.cases.length>=60);assert.ok(w.windows.length>=24);assert.equal(s.snapshots.length,12);assert.equal(d.dossiers.length,12);assert.equal(l.dimensions.length,14);console.log('✓ Book VI coverage',JSON.stringify({chapters:r.chapters.length,cases:c.cases.length,windows:w.windows.length,snapshots:s.snapshots.length,dossiers:d.dossiers.length,dimensions:l.dimensions.length}));