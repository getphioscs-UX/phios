import assert from 'node:assert/strict';import crypto from 'node:crypto';import fs from 'node:fs';
const j=f=>JSON.parse(fs.readFileSync(f,'utf8'));const norm=s=>s.replace(/^\uFEFF/,'').replace(/\r\n?/g,'\n');const digest=f=>crypto.createHash('sha256').update(norm(fs.readFileSync(f,'utf8')),'utf8').digest('hex');const BASE='021007b80fa20739a726fb28bcda4a9369af48e4';
const c=j('content/web-production/contracts/wpr-freeze-contract-v1.json');assert.equal(c.work,'WPR-W30');assert.equal(c.baselineCommit,BASE);assert.equal(c.freezeScope,'WPR-W0-W30');assert.equal(c.frozenAuthority.projectionOnly,true);assert.equal(c.frozenAuthority.professionalJudgmentAuthority,false);assert.equal(c.operationalState.currentProductionState,'LIMITED_PRODUCTION');assert.equal(c.operationalState.freezePromotesProductionState,false);
const w29=j('content/web-production/acceptance/wpr-w29-full-production-acceptance-v1.json');assert.equal(w29.accepted,true);assert.equal(w29.fullProductionPromotion,false);
const manifest=j('content/web-production/registries/wpr-checker-manifest-v1.json');assert.equal(manifest.freezeCode,'WPR-v1.0.0-FROZEN');assert.equal(manifest.entries.length,31);for(const e of manifest.entries){assert.ok(fs.existsSync(e.implementationFile),e.implementationFile);assert.equal(digest(e.implementationFile),e.sha256,`WPR_CHECKER_DIGEST:${e.workCode}`);}
const freeze=j('content/web-production/freeze/wpr-v1-freeze-v1.json');assert.equal(freeze.freezeCode,'WPR-v1.0.0-FROZEN');assert.equal(freeze.scope,'WPR-W0-W30');assert.equal(freeze.productionStateAtFreeze,'LIMITED_PRODUCTION');assert.equal(freeze.productionPromotionPerformed,false);assert.equal(freeze.checkerManifestReference,'content/web-production/registries/wpr-checker-manifest-v1.json');assert.equal(freeze.deploymentObservationFrozenAsAuthority,false);assert.ok(freeze.openOperationalGates.includes('DEPLOYMENT_SHA_REVALIDATION_REQUIRED'));
const pkg=j('package.json');assert.equal(pkg.scripts['check:wpr-w30'],'node scripts/check-wpr-w30-freeze.mjs');assert.equal(pkg.scripts['check:wpr-freeze'],'npm run check:wpr-w30');assert.equal(pkg.scripts['check:wpr-final'],'npm run check:wpr');assert.equal(pkg.scripts['check:web-production-runtime'],'npm run check:wpr-final');const post=String(pkg.scripts.postcheck).split('&&').map(x=>x.trim()).filter(Boolean);assert.equal(post.filter(x=>x==='npm run check:web-production-runtime').length,1);assert.equal(post.at(-1),'npm run check:web-production-runtime');assert.equal(String(pkg.scripts.postcheck).includes('check:wpr'),false,'Historical WPR pre-W30 guard string remains untouched by neutral final alias.');
const a=j('content/web-production/acceptance/wpr-w30-freeze-acceptance-v1.json');assert.equal(a.accepted,true);assert.equal(a.productionPromotionPerformed,false);assert.equal(a.centralPostcheckIntegrated,true);assert.equal(a.authorityExpansionGranted,false);
console.log('✓ WPR-W30 WPR v1 Freeze passed.');console.log('  WPR-W0-W30 authority is frozen; central postcheck now reaches full WPR through the neutral web-production-runtime alias while operational deployment/R2/domain/browser gates remain explicit.');
