import assert from 'node:assert/strict';import fs from 'node:fs';import crypto from 'node:crypto';const j=p=>JSON.parse(fs.readFileSync(p,'utf8'));const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');
const freeze=j('content/professional/method-client-delivery/freeze/mcd-7-personal-runtime-result-surface-freeze-v1.json');assert.equal(freeze.status,'FROZEN_CANONICAL_RESULT_SURFACE_HDR_NO_PRODUCTION_TAB');for(const item of freeze.frozenOutputs)assert.equal(sha(item.path),item.sha256,`MCD-7 surface drift: ${item.path}`);assert.equal(freeze.authorityFreeze.dispatchOwner,'MPA');assert.equal(freeze.authorityFreeze.clientResultContract,'CanonicalMethodProjection');assert.equal(freeze.authorityFreeze.hdrProductionResultTabAllowed,false);assert.equal(freeze.authorityFreeze.readingInterpretationAllowed,false);console.log('✓ MCD-7 Personal Runtime Result Surface freeze passed.');
