import http from 'node:http';import fs from 'node:fs';import path from 'node:path';
const args=process.argv.slice(2);const val=f=>{const i=args.indexOf(f);return i>=0?args[i+1]:null};const batch=val('--batch')||'BATCH-003';if(batch!=='BATCH-003'){console.error('Only BATCH-003 C2 review is materialized in this delta.');process.exit(2)}
const root=process.cwd();const file=path.join(root,'content/production/bilingual-final-approval/progression-v2/review/BATCH-003-c2-review.html');const server=http.createServer((req,res)=>{if(req.url==='/'||req.url==='/review.html'){res.writeHead(200,{'content-type':'text/html; charset=utf-8'});res.end(fs.readFileSync(file));return}res.writeHead(404);res.end('Not found')});server.listen(4177,'127.0.0.1',()=>console.log('✓ BFA-PROG C2 Review: http://127.0.0.1:4177/review.html'));
