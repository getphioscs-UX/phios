# DAR Fixtures

Will scenario fixtures live under `content/legal/will/fixtures/`.

Renderer determinism uses in-memory **non-legal test components** in `scripts/check-dar-w15-w24-document-assembly-runtime.mjs` so test text cannot be mistaken for persisted clause authority.
