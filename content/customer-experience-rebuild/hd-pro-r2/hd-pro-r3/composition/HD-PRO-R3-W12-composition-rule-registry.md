# HD-PRO-R3-W12｜Composition Rule Engine

W12 registers eleven composition families. Ten are semantically executable from the admitted W4–W11 corpus. `PERSONALITY_GATE_X_DESIGN_GATE` is deliberately structural-only because W9 preserved the lack of distinct value-specific Personality/Design Gate semantics as `SOURCE_PENDING`.

Composition is not string concatenation. Each rule owns an interaction question, produces one composition claim, preserves all source refs, and assigns a precedence class for W13. A complete Channel remains primary over component Gate and generic Center material. Authority always retains final decision ownership. Variable/PHS is always an advanced modifier.

The canonical verification fixture includes `Generator × Emotional Authority` and requires an interaction claim whose core meaning is that engagement response and decision timing are separate processes: a response does not automatically mean the decision is complete.
